# Routes

Express serves one EJS page (`client/index.ejs`, route `/`). No client router. Deck builder = overlay workspace within that page, opened via sidebox header button (`client/src/initialization/document-event-listeners/sidebox/../sidebox/header-buttons.js`). Board/game mat is the other main surface.
