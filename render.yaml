services:
      - type: web
        name: ptcg-sim
        runtime: node
        plan: free
        buildCommand: npm install
        startCommand: node server/server.js
        healthCheckPath: /
        envVars:
          - key: NODE_ENV
            value: production
    