services:
      - type: web
        name: ptcg-sim
        runtime: node
        plan: free
        buildCommand: npm install --prefix server
        startCommand: node server/server.js
        healthCheckPath: /
        envVars:
          - key: NODE_ENV
            value: production
    