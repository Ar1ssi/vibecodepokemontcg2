# Issues — the tracked backlog: bugs, debt, deferred work. One line each, newest first.
# If "we should fix that later" isn't a line here, it will be forgotten. Journal `flag:` lines are
# the informal inbox; maintain.md promotes the ones that matter into this file and drops the rest.
# Scan Open before designing or when picking what to work on; FIXING an issue is routed like any
# other request (patch/debug/feature) — this file only tracks, it never carries the work itself.
# Format: `I<n> <YYYY-MM-DD> P<1|2|3> [scope] symptom or task — evidence/repro (refs: D<n>, design, S<n>)`
#   P1 broken for users now · P2 wrong or risky, schedule it · P3 debt/idea, fix when passing.
#   Next <n> = highest number anywhere in this file + 1. One line; at most one indented detail
#   line. Needs more? Stub a design in .agent/designs/ and reference it — don't bloat this file.
# Close = move the line under Closed and append ` → closed <YYYY-MM-DD> S<n>: <fix or wontfix + why>`.
# Caps: Open ≤40 (over → merge duplicates, close the stale, demote to P3 or drop) ·
# Closed ≤100 (maintain.md deletes the oldest lines; git history keeps everything forever).

## Open (newest first — scan this section only)
- I151 2026-09-24 P2 [rules] Reactive Tool conditions are never checked: attachedToolOnDamageEffects/attachedToolOnKoEffects skip toolConditionMet, so Adversity Policy (Weakness), Punk Helmet ({D}), Team Rocket's Hypnotizer (TR Pokémon), Box of Disaster (full HP V), Heavy Baton (Retreat 4), Farewell Bell (VMAX) fire on any holder (refs: design 035 slice 9, design 038, S289)
- I150 2026-09-24 P3 [tooling] `pnpm audit:trainers` only compares gap tags + play condition: a card that loses a parsed step (still executable) passes, and a baseline key that disappears (text edit re-hashes the key) is never reported — checkTrainerGate walks the corpus, not the baseline (refs: design 035 slice 12, D123, S288 review)
- I149 2026-09-24 P2 [rules] "If you go first, you may use this card during your first turn" Supporters (Carmine, Beauty, Team Rocket's Proton) are blocked on turn 1: trainerPlayBlockReason's blanket turn-1 Supporter rule has no exception (pre-existing, not slice 11) (refs: design 035, S288 review)
- I148 2026-09-24 P3 [rules] Blaine's Last Resort parses as `lastCardInHand`, so holding 2 copies blocks it; printed text allows other copies ("other than Blaine's Last Resort") (refs: design 035 slice 11, S288 review)
- I147 2026-09-24 P3 [rules] opponentChoosesFromTop (Riley/Rival) pushes `cardsRevealed` again on the resume call (event sits before the `ctx.selection` branch), so the log shows the reveal twice (refs: design 035 slice 6, S288 review)
- I146 2026-09-24 P3 [rules] Focus Band: a tails flip consumes RNG but emits no `coinFlipped` (reduce.mjs:4210 only inside `prevented`); the bench-damage KO path (reduce.mjs:360) passes no flipCoin, so Focus Band never saves a sniped Pokémon (refs: design 035 slice 9, S288 review)
- I145 2026-09-24 P2 [rules] Chaos Gym coin lives only in the playTrainer apply case; a Tool attached straight from hand via `attachCard` (reduce.mjs:5215) never flips (refs: design 035 slice 10b, S288 review)
- I144 2026-09-24 P2 [rules] activeTargetDamage (reduce.mjs:513) omits defenderTrailingPrizes/attackerTrailingPrizes/attackerPrizesRemaining, so Defiance Vest/Band never apply to a chosen-target clause hitting the Active (Vest over-applied before slice 4, never applies now) (refs: design 035 slice 4, S288 review)
- I143 2026-09-24 P2 [rules] Hisuian Heavy Ball / Beast Ball auto-take the first matching Prize (`prizes.find`, trainer-steps.mjs lookAtFaceDownPrize): no choice between two Basics and no way to decline the printed "you may" (refs: design 035 slice 6, S288 review)
- I142 2026-09-24 P2 [rules] shufflePokemonIntoDeck: Mr. Fuji ("a Pokémon on your Bench") also offers the Active; the shuffled stack keeps damage/Special Conditions/markers (probe: 60 dmg + Poisoned survive in deck), unlike atkShuffleSelf (refs: design 035 slice 5, S288 review)
- I141 2026-09-24 P2 [rules] Hidden info leaks: events go unredacted to every socket, and Daisy's Help `cardsRevealed` names all 6 Prizes, Amulet of Hope's KO search names the searched cards, Peonia's blind Prize pick shows names in pendingChoice options (refs: design 035 slices 6/9, S288 review)
- I137 2026-09-24 P3 [rules] Design 035 leftovers: unparsed play conditions (Lt. Surge, Kahili, Glass Trumpet, Cyrus Prism Star, Green's Exploration, Anthea & Concordia, Luxury Ball, Nugget/Dream Ball) and step/tool gaps (Gladion Prize shuffle, Peeking Red Card, Evil Deeds "up to 2", Daisy's Help draw, attachAttackTool qualifiers, "Your turn ends.", Spell Tag/Spirit Mask/Heavy Baton/Berries, Counter Gain cost gate, Chaos Gym opponent use)
  detail: each is listed with its reason in design 035's Deviations (slices 4, 6, 7, 8, 9, 10b, 11) (refs: design 035, S286)
- I136 2026-09-24 P2 [rules] 26 parsed Trainer step kinds (one card each: Alph Lithograph, Buddy-Buddy Rescue, Caitlin, Eneporter, Fan of Waves…) have no server executor, so the authoritative game skips them — `pnpm audit:trainers` lists every `server-missing:<step>` (refs: design 035, S286)
- I127 2026-09-23 P3 [rules] The card inspector's retreat tile ignores bench abilities: retreatGateFor passes `zoneCards: []` and the printed cost to canRetreat, so a Latias ex Skyliner free retreat still renders as unpaid (refs: S277)
- I126 2026-09-23 P3 [rules] Team-wide retreat-cost abilities with an energy condition (Metal Bridge, Thunderclap Zone, Aqua Tube, Dark Cloak) are not applied: teamNoRetreatCostForActive handles only unconditional "in play have no Retreat Cost" wordings (refs: S277)
- I125 2026-09-23 P3 [rules] A period inside the attacker's name breaks sentence splitting: Lt. Surge's Raichu Mega Shock gated recoil is not parsed — node .agent/scratch/i118/gateprobe2.mjs (refs: design 032, S271)
- I124 2026-09-23 P3 [rules] Lightning Sphere (discard Energy for extra damage on a coin) runs no discard step — gateprobe2.mjs (refs: design 032, S271)
- I123 2026-09-23 P3 [rules] Ungated self discards that name the card (Fire Spin "attached to Charizard") stay with the text helper; only gated ones are steps — check the helper matches every name form (refs: design 032, S271)
- I122 2026-09-23 P3 [rules] Blast Burn "if you can't, this attack does nothing" (cost-conditioned discard) is not enforced — gateprobe2.mjs (refs: design 032, S271)
- I121 2026-09-23 P3 [rules] Bowed Whip "that Pokémon" (target chosen by an earlier clause) has no gated step — gateprobe2.mjs (refs: design 032, S271)
- I87 2026-09-23 P2 [netcode] #5 "basic prompt gone in most recent match" not reproduced: no match logs exist and the prompt is unidentified; waiting on the user's description (refs: S264, design 028).
- I86 2026-09-23 P2 [feature] 30th Celebration (`30th`) + Classic Collection (`30th-c`) support — design 029 drafted, awaiting user approval (legality: TCGdex says Expanded only) (refs: design 029, S264).
- I85 2026-09-23 P1 [feature] Match logging (user: IMPORTANT) — design 028 drafted, awaiting user approval (refs: design 028, S264).
- I84 2026-09-23 P3 [rules] Legacy mode (:4000) runs Dynamotor/Sinister Surge/Adrena-Brain through client chat-buttons executors, not the S264 engine fixes; unverified there (refs: S264, feedback_legacy_mode_untested).
- I81 2026-09-22 P3 [deck-builder] The deck builder's cards autosave on every render and the sleeve/coin/mat pickers commit on change, so the new explicit Save button is a no-op whenever a deck is loaded — it exists for the no-deck-loaded case and for user-visible confirmation. Decide whether autosave should stay (Save becomes cosmetic) or be dropped in favour of explicit saves with a dirty indicator. (refs: S252)
- I80 2026-09-22 P3 [board-ui] Fossil Items (`fossilItem` step) cannot reach the Bench in legacy/solo mode: `trainer-execution.js:1207-1218` moves board→bench without transforming the card, while the server (`trainer-steps.mjs:868-885`) stamps `supertype:'Pokémon'`/`stage:'Basic'` first, so `move-card.js:197-221`'s structural guard refuses the move and the Item strands on the board. Fix: apply the same transform client-side (or exempt fossil items in the guard). (refs: S251 audit)
- I79 2026-09-22 P3 [rules] Client-authoritative (legacy) mode enforces no GX once-per-game: `rulesState` has no `gxUsed`/`vstarUsed` store and `chat-buttons.attack()` never checks `isGxAttack`, so a GX attack can be reused every turn in solo/legacy 2P while the server refuses it. Needs a game-scoped `oncePerGame` store (reset only in `startGame`/`resetRulesSessionState`, never in `resetTurnFlags`) plus an attack-entry gate and a regression test. (refs: S251 audit; contrast reduce.mjs:2159)
- I78 2026-09-22 P3 [rules] Between-turns Stadium damage resolves a simultaneous double-KO as a win, not the tiebreak: `applyBetweenTurnsStadiumDamage` (reduce.mjs:947-967) breaks its KO loop as soon as `isGameConcluded` is true after the first victim, so the other side's equally-lethal Active is never processed and `handleKnockout`'s per-KO ways comparison cannot see the tie. Fix: collect all lethal victims from the sweep, apply every KO, then evaluate the win once. (refs: S251 audit)
- I74 2026-09-21 P3 [board-ui] "Clear battle log" is local-only: `clearChatboxContent` (export-chat.js:23-26) clears this client's container and never notifies the peer, so one player's log empties while the opponent's persists (bottom-buttons.js:68-72). (refs: S230)
- I73 2026-09-21 P3 [netcode] Battle logs diverge on reconnect: append-message.js:4 skips appending (and emitting) while `systemState.isCatchingUp`, and peer-log catch-up holds that flag across the whole replay (socket-event-listeners.js:669-677 → applyPeerAction → acceptAction), so missed opponent lines are never backfilled on the reconnecting client; the two players' exported battle-log.txt stop matching after any disconnect. (refs: design 002 slice 1.1, S230)
- I72 2026-09-21 P3 [board-ui] Dead rules battle-log UI: `#rulesBattleLog` and 9 `.battle-log-*` classes (index.css:4266-4317; duplicate in tracked index.css.bak:3350-3402) have no DOM element and no JS assigning the classes (grep `rulesBattleLog`/`battle-log-` = CSS only; append-message.js:9-15 sets only 'announcement'/'self-text'/'opp-text'), so the intended attack/KO/turn/warn/win coloring never applies. Delete the block (and the tracked `.bak`). (refs: S230)
- I70 2026-09-20 P3 [rules] Special-energy client triggers — browser verification. The authoritative engine now executes every trigger (I69), but the legacy client paths (rules-bridge on-attach/evolve/damaged/end-turn, move-card on-discard, KO watcher, chat-buttons on-damaged) are unverified in a real browser; there is also a minor client-only gap: the Legacy Energy once-per-game guard is enforced in `toolPrizeCountAdjust`/`handleKnockout` on the authoritative path but not in the client `koWithToolPrizes` path (client has no per-game flag store). Verify with Playwright (`test-card-inspector-e2e.mjs`-style) and/or add the client guard. (refs: I69, D86, S222–S223)
- I67 2026-09-20 P3 [deck-builder] `client/src/setup/deck-builder/core/coins.mjs.bak-urls` is a tracked 65 KB backup of an old coin catalog — dead weight and a confusing second source of coin data. Delete it (git history keeps it). (refs: S219)
- I65 2026-09-20 P3 [rules] Stadium attack GRANTS are evaluated against the in-play root (the Basic), not the top evolution card: `stadiumExtraAttacks` calls `stadiumGrantedAttacks(stadiumCard, root)`, so an evolved Pokémon whose {Delta Species} (Holon Lake) or Dark/Rocket's marker is only on the evolution card is denied the granted attack. Fix by passing `topPokemonCard(zoneCards, root)` to `stadiumGrantedAttacks` (server and the client's `stadiumExtraAttacksFromZone` alike). No corpus repro yet — no evolved δ/Dark fixture in tests. (refs: D78, S207)
- I64 2026-09-19 P3 [rules] The attack panel (`attack-window.mjs`/`attack-preview.js`) does not reflect the spent once-per-game GX attack - a GX attack already used this game still shows as usable; the server rejects it with "Only one GX attack can be used per game." Thread the GX/VSTAR used state into `listAttacks` opts like `abilityUsed` and surface the reason in the preview. (refs: design 018, S193; renumbered from I61 on the PR #171/#172/#173 merge so the tiebreak keeps I61)
- I63 2026-09-20 P3 [rules] V-UNION full play rules unimplemented: `isVUnionCard` classifies the 4 pieces (rule box, 3 prizes) but nothing sets them from the discard, treats the set as one Pokémon, or enforces once-per-game-per-name. (refs: rulebook-30c-implementation-plan.md 2.4 follow-on, S198)
- I62 2026-09-20 P3 [rules] Pokémon LEGEND two-cards-played-together representation: `isLegendCard`/2-prize classification exist, but the "play both halves as one Pokémon" rule and its hand/board pairing are not modelled. (refs: rulebook-30c-implementation-plan.md 5.4 follow-on, S198)
- I61 2026-09-20 P3 [rules] Tiebreak has no fresh six-prize sudden-death mat: a genuine simultaneous tie is resolved by click-resolution only (D71/D62); a real mat needs deck reconstruction + client UX. (refs: D62, D71, rulebook-30c-review-fix-plan.md Phase 1.2, S200)
- I60 2026-09-19 P3 [rules] Design 015's zone guard covers only the `useAbility` path. `useVStarGX` has the same reachability (`validateReferences` admits both active and bench for it) but its `validateLegality` case never resolves the card — it checks only the VSTAR/GX used flags — so a VSTAR power printed as a positional conditional would still fire from the Bench, and the inspector's Greys only come from `listAbilities`, which this path does not call. Fix needs a `findCard` lookup in that case. Deliberately not widened without evidence: no positional VSTAR power was found in the corpus, and the guard trades a legal-ability regression against an over-permissive one. (refs: design 015 D2, S182)
- I59 2026-09-18 P2 [rules] The server engine enforces no evolution timing at all: nothing tracks the turn a Pokémon entered play or last evolved, so `searchEvolve` (Salvatore, Grand Tree) and `evolveStage2` (Rare Candy) can evolve a Pokémon put into play this turn. The legacy client gates this through `canEvolve`/`rulesState`. Needs per-card turn metadata on server state plus reducer wiring. (refs: design 013, D30, S173) → closed 2026-09-18 S174: per-card enteredPlayTurn & lastEvolvedTurn metadata tracked in moveCard, attachCard, searchEvolve, and evolveStage2; attachCard and Rare Candy legality checks enforce turn-1/2 ban, same-turn ban, once-per-turn evolved flag, stage sequence progression, and evolution line matching; unit tests verified.
- I58 2026-09-18 P3 [rules] Stadium once-per-turn kinds still unimplemented server-side: `hand-to-deck-top`, `switch-type`, `discard-to-bench`, `energy`, and the generic `search` fallback all parse in `stadium-effects.mjs` but have no branch in `executeStadium`, so those Stadiums silently fall through to `parseTrainerEffect` or do nothing under server authority. Same gap class design 013 closed for `search-evolve`. (refs: design 013, S173) → closed 2026-09-18 S174: implemented executeStadium branches for hand-to-deck-top, switch-type, discard-to-bench, and generic search fallback; unit tests verified.
- I56 2026-09-18 P2 [netcode] Keybinds that still read `mouseClick.card.image` throw a TypeError on a server-drawn card: `c` (look/stop looking), `z`/alt-z (hide/reveal shortcut), `e`/`q` highlight, and the `keyBinds` move keys resolve through the empty legacy zone arrays. Design 012 fixed the counter/condition/rotate/type/ability keys only; the rest need the same registry-sourced treatment (reveal/hide shortcuts are position-addressed server commands, so they also need an index from the view). (refs: design 012, S171)
- I55 2026-09-18 P3 [netcode] Under server authority a peeked deck card can only be taken into hand, and `changeType` on an opponent's card changes type without the legacy move-to-board (the server refuses moves of another player's cards). Legacy allowed any destination and always moved the card to the board. Revisit if players miss it. (refs: design 012, S171)
- I54 2026-09-17 P2 [rules] Audit A-1 (High) still open: `specialCondition` is one string, so adding Asleep
    to a Poisoned Pokémon wipes Poison, and resolveCheckup's else-if chain applies only one condition. Fix needs a
    condition-set model across reduce.mjs, view/zone-hash, apply-view.js, status.mjs (~150 refs, 23 files) — pair
    with A-2 — client status.mjs already allows Poisoned+Burned since S168; server must catch up. (refs: server-authority-and-rules-engine-audit.md, S167) → closed 2026-09-17 S169: design 011, D45.
- I53 2026-09-17 P1 [rules] Audit finding B-1c still open: `takePrizesByIndex` (reduce.mjs:851 legality,
    :1669 apply) keeps the bare bounds check and no turn gate, so `indices:[0..5]` wins the game off-turn —
    B-1's fix (prize entitlement + turn gate, D43) applies verbatim; the entitlement is granted as a count,
    so the indices variant must check `indices.length` against it (refs: D43, S166) → closed 2026-09-17 S167: turn-gated, entitlement-checked, integer/non-negative/unique indices.
- I47 2026-09-16 P2 [netcode] `test:flip` (flip-gate-test.mjs, SERVER_AUTHORITATIVE=1 :4100) fails on unmodified main: step 1 "both clients joined" passes, then "timeout waiting for page predicate" — debug showed client A hand 9 (drew before setup settled), B hand 7 (refs: S137) → closed 2026-09-20 S214: under serverAuthoritative the server executes turn-start draws authoritatively (setupGame for turn 1, advanceTurn on subsequent turns); hookTurnStartDraw & shouldEmitTurnStartDraw now guard against serverAuthoritative so client no longer emits redundant draw command (which caused starter to reach 9 cards); maybeBeginServerTurnOrder now enforces openingSetupReadyForCoinFlip so setupPrizes and dealOrder finish settling before drawOpeningHand executes; flip-gate-test.mjs checks hands dealt (8 starter, 7 defender) without 9-card overdraw. All unit tests (2533) and e2e tests green.
- I46 2026-09-14 P3 [board-ui] Design 009 hand: #hand's horizontal scrollbar sits in the off-screen band (hidden via scrollbar-width: none), so a 15+ card hand scrolls only by trackpad/shift+wheel — map vertical wheel to scrollLeft or add arrows (refs: design 009, S131)
- I45 2026-09-14 P3 [netcode] 2-browser Playwright E2E (legacy/non-authoritative mode): after a
    UI-driven attack() call that runs to completion (no cmdRejected, attack option correctly drops
    from the acting client's own legal-move list afterward), the attacking client's own observe()
    of opp.active.damage still read 0 several seconds later. Not reproduced via targeted debug
    (S127); likely a stale-view read in this ad-hoc harness, not the attack-engine itself (1362
    unit tests incl. damage application are green). Needs a session with time to trace with
    SERVER_AUTHORITATIVE on/off. (refs: design 008 manual verification, S127)
    RENUMBERED from I44 on merge into claude/cpu-testing-live-render-anr7fy: this branch
    already used I44 for the attack-inheritance issue below.
- I44 2026-09-14 P3 [rules] Attack inheritance never inherits — `mergeInheritedAttacks` is dead in practice
    because the only live call site passes `priorAttacks: []` (rules-bridge.js:305); that call site is
    deleted by design 008 slice 6, which also drops its per-refresh chat announcement (refs: 008 R8/R7, S123)
    RENUMBERED from I43 on merge into claude/cpu-testing-live-render-anr7fy: both branches
    independently claimed I43. The design 008 doc still refers to it as I43.
- I43 2026-09-11 P2 [netcode] Client per-turn rules flags were never synced from the authoritative view — FIXED S89, recorded because it affected real players, not only the bot. Under `SERVER_AUTHORITATIVE` the legacy bodies that set `rulesState.flags` (markRetreated, markSupporterPlayed, the energy-attach and attack markers) are gated away, so the flags stayed frozen while the server tracked the real ones; `canPerformAction` reads them, so the client offered moves the server then rejected ("Already retreated this turn." after one retreat). `reconcileTurnState` (apply-view.js) already existed for exactly this class of staleness and synced turn player/number/phase but not flags; it now merges `view.you/them.flags` too. Verified: bot 8/8 games in authoritative mode, was 0/8 (refs: design 002 slice 3.12, S89).
    RENUMBERED from I36 on merge into this branch: the two sessions ran concurrently and both
    claimed I36/S89. This branch's numbering is the incumbent; the fix/bot-authoritative commit
    message still says I36.
- I39 2026-09-11 P2 [netcode] flip-gate-test.mjs fails right after both hands are dealt (timeout waiting for a page predicate) on main fe0ad88 with S102's changes stashed — pre-existing; the server-authoritative exit gate is currently red. (refs: S102)
- I41 2026-09-11 P3 [netcode] legacy mode: after both players leave and join a fresh room, the second player once stayed in setup with prizes but no opening hand (1 sample, S102 second-game probe). Untriaged. (refs: S102)
- I34 2026-09-10 P1 [netcode] Turn ownership deadlocks and the turn counter drifts between clients: both clients simultaneously report `turnPlayer: 'opp'` — each believing it is the other's turn — so neither ever acts and the game wedges. Caught with A at `turnNumber: 13` and B at `turnNumber: 8`, i.e. B missed five turn advances; both still `phase: 'main'`, neither with `gameEndedInfo`. Repro `--scorer=coverage --seed=2026 --games=10`, 1/10 games; dump `out/playtest/2026-7-13.json` carries both clients' turnState (refs: I29, I33, S87).
    This is what was left after I33's three harness defects were fixed — it is a REAL client-state divergence, not a reporting artifact, and it is the same signature I29 was closed under in S82 as "a harness race". That closure now looks premature: with honest reporting in place the deadlock still occurs, at a much lower rate. Turn advance is driven by `endTurn()` inside `endTurnWithBanner`, reached from both `pass()` and `attack()`; a replayed action that returns early skips it, which is exactly the I31/I32 defect class — audit every acceptAction target in chat-buttons.js for `canPerformAction`-on-mirror before assuming a new cause.
- I23 2026-09-09 P2 [netcode] `VSTARGXFunction` has no UI caller and no DOM element: nothing in client markup or JS defines `GXButton`/`VSTARButton`, so the legacy body would throw on `button.classList` if it were ever reached locally; the action is reachable only via the `acceptAction` relay. Left ungated by design 003 slice 5 (a gate on a dead local path adds risk without behavior). Either restore the buttons or delete the action (refs: design 003, S60).
    S231 audit + S232: design-018's "keep the client buttons greyed" rationale is dead — view.mjs:42-50 projects `oncePerGame` into `flags` and apply-view.js:1253-1267 (+ its test) reconcile `#GXButton`/`#VSTARButton`, none of which exist in index.ejs (grep GXButton/VSTARButton = the dead sender, the projection, and test stubs only). S232 removed the redundant advanceTurn/useVStarGX flags copies, so the view projection is now the only server consumer of those flags.
- I20 2026-09-09 P3 [netcode] type2/enteredPlayTurn (evolution-timing, dual-type tracking in
    move-card.js) have no server-side equivalent (grep of shared/engine: zero hits) — purely
    legacy rules-bridge.js-local fields. Design 003 routes gated actions around moveCard entirely
    so this can't cause cmdRejected/desync (server enforces real legality independently), but the
    *legacy* fallback path (flag off, or any ungated action) still depends on rules-bridge.js,
    which stays out of scope until that module's own migration (refs: design 003, MAP.md's
    "migration's cost centre" note, S54)
- I15 2026-09-09 P2 [netcode] Authoritative renderer has no interaction or overlay parity: createOrUpdateCardElement emits a bare <img class="card-image"> while legacy Card.buildImage attaches seven listeners (click/dblclick/drag x4/contextmenu), damage counters are sibling <div>s (damage-counter.js:182-203) and Cover is a separate image — wiring getZone in as-is yields a non-interactive board (refs: design 002 Phase 3B, S39)
- I10 2026-09-09 P1 [netcode] Stadium is wiped from both boards on any successful server command: applyView's reconcileStadium clears #stadium when view.stadium is null, and #stadium is the only zone its DOM fallback can reach (apply-view.js:259-281) — repro: play a Stadium, then draw (refs: design 002, S32)
- I11 2026-09-09 P1 [netcode] Client syncInstance (0-based per player) is sent as the server's instanceId (1-based global), so every authoritative card command targets the wrong card or the wrong player's card (build-deck.js:13-19 vs shadow.mjs:29-68; dual-run-bridge.js:205-208) (refs: design 002, S32)
- I12 2026-09-09 P1 [netcode] Reconnect recovery is dead in both modes: slice 8 deleted the replay stack but left the emitters — no client listener exists for resyncActions/catchUpActions/requestBoardSnapshot, and requestView returns a view the renderer cannot paint (socket-event-listeners.js:211) (refs: design 002, S32)
- I13 2026-09-09 P2 [netcode] applyView renders nothing in production: zones live in iframes, window.__getZone is never assigned, and only tests inject options.getZone — so the renderer has never run against the real DOM (apply-view.js:91-111) (refs: design 002, S32)
- I14 2026-09-09 P2 [netcode] SERVER_AUTHORITATIVE defaults on (server.js:21-23) though design 001 slice 8's flip was never completed: 14 of 58 actions translate, IDs do not match, renderer is blind (refs: design 002, I11, I13, S32)
- I1 2026-09-07 P2 [rules] Turn start auto-draw: both players draw a card when turn is started (ref: ISSUES.txt)
- I2 2026-09-07 P2 [rules] End Turn button logic: +Turn needs rework to end active player's turn (ref: ISSUES.txt)
- I3 2026-09-07 P2 [rules] Garland Ray energy discard parsing fail on multiplier attacks (ref: ISSUES.txt)
- I4 2026-09-07 P2 [rules] Prize card selection on KO fails (ref: ISSUES.txt) → closed 2026-09-17 S170: server prize pendingChoice + fly-up prize fan (D46).
- I6 2026-09-07 P2 [netcode] Multiplayer desyncs are architectural: two client simulations, no arbiter — 54 of last 200 commits are sync/replay/RNG patches (refs: design 001)
- I5 2026-09-07 P2 [rules] Deck inspection allowed during rules mode (ref: ISSUES.txt)
    Root cause is data, not UI: both clients hold the full opponent deck. Design 001 closes it by redacting the view server-side — don't fix separately.


## Closed (append-only history; grep it, never load it wholesale)
- I140 2026-09-24 P2 [rules] Handheld Fan moves 2 Energy when its holder is Knocked Out: the damage-site loop and applyToolOnKoEffects both apply `moveEnergyOnKo from:'attacker'` (probe: 2 moved, expected 1) (refs: design 035 slice 9, S288 review) → closed 2026-09-24 S289: design 038 slice 1 — phase split keeps Handheld Fan/Rugged Helmet out of the KO path (one move); also fixed the Rugged Helmet damage-site TypeError
- I139 2026-09-24 P1 [rules] Lucky Egg draws to 7 on any damage, not only on KO: the damage-site loop (reduce.mjs:4293) applies `drawUntil` with no KO gate (probe: a 10-damage hit drew p2's whole 4-card deck) (refs: design 035 slice 9, S288 review) → closed 2026-09-24 S289: design 038 slice 1 — parseToolOnDamageEffect `phase` ('damage'|'ko'); the damage loop runs only the damage phase, so Lucky Egg draws only on KO
- I138 2026-09-24 P1 [rules] handleKnockout's attacker-side prize loop (reduce.mjs:1011, meant for Beast Bringer) applies every attacker Tool's "takes N more/fewer Prize" clause: attacker holding Expert Belt/Luxurious Cape takes +1, Life Dew/Hero's Medal −1 (probe: Luxurious Cape 2 prizes for a 1-prize KO); also runs for non-attack KOs (refs: design 035 slice 9, S288 review) → closed 2026-09-24 S289: design 038 slice 1 — parsePrizeModify returns `side`; victim loop takes only victim clauses, attacker loop only imperative ones on a KO of the opponent's Active by the turn player's attack, VSTAR Power texts skipped (tool-on-ko tests)
- I120 2026-09-23 P2 [rules] 75 coin-gated attack sentences (47 kinds) run no server effect on their face: self Energy discard on tails (Power Blast, Aeroscream, Fire Blast…), heads KO (Swinging Sphene), discard-pile recovery (Warp Hole, Reverse Edge), attach-from-list (Freeze Solid, Plasma), opponent Energy discard (Rapids, Hyper Whirlpool)… — list: node .agent/scratch/i118/gateprobe.mjs (refs: design 031, S269). → closed 2026-09-23 S271 (design 032, branch feature/i118-attack-gaps; leftovers I121-I125)
- I119 2026-09-23 P3 [rules] 10 attack printings in the design-031 families still have no server step: Encore (lock, not copy), Mach Wind, Extra Comet Punch, Iron-Clad Roll, Desert Geyser, Psychic Defense, Voltage Shoot, Rocket Splash, Mud Flood, Hidden Power; plus look-opponent-deck, which the local corpus lacks but live TCGdex has (Inkay Mischievous Tentacles, Gothorita Fortunate Eye: look at / reorder opponent's top cards, needs an ordering prompt) — probes .agent/scratch/cov/family-signal.mjs, live-family.mjs (refs: design 031, I118, S269). → closed 2026-09-23 S271 (branch feature/i119-unparsed-printings): design 033 — deck reveal scaling, shuffle-to-deck scaling, hand Energy discard + legality gate, freeRetreat / Metang bonus markers, chained discard→marker blocks, attackLock marker (Encore/Amnesia), Unown T hand swap, opponent deck look/reorder; 25 tests in attack-unparsed-printings.test.mjs.
- I118 2026-09-23 P2 [rules] Server attack never executes 10 classified families (oracle: no effect observed; helpers exist only in client chat-buttons.js): reveal-hand 44, immunity 243, damage-prevention 174, next-turn-bonus 36, copy-attack 34, hp-cap-damage 9, deferred-damage 3, retaliate 2, redirect-damage, look-opponent-deck; shuffle-cost partial 24/46 (refs: S268, I112). → closed 2026-09-23 S269: design 031 — attack markers (immunity, prevention/reduction, next-turn bonus, deferred KO, retaliate), HP-cap, reveal-hand steps, shuffle-cost remainder, copy attacks run on the server; oracle/parser coverage per family in .agent/scratch/cov/family-signal.mjs; audit list updated; 10 unparsed printings → I119.
- I113 2026-09-23 P2 [tooling] ability-audit.mjs:80 EXECUTED_FAMILIES reports pctExecuted 99 and audit-all-pokemon engineFailures counts only errors, so silent executor no-ops pass every gate; promote .agent/scratch/cov/oracle.mjs (per-card state diff) to an execution gate (refs: S265, audit T2). → closed 2026-09-23 S269: design 032 — scripts/audit-oracle.mjs (pnpm audit:oracle) runs the oracle as a gate: per-family observed rates (state change, or dealt damage ≠ printed base) ratcheted vs scripts/oracle-baseline.json; an executed-listed family with 0 observed rows fails unless ORACLE_BLIND_FAMILIES gives a reason. Family lists moved to scripts/lib/executed-families.mjs.
- I112 2026-09-23 P2 [tooling] attack-false-positive-audit.mjs:13 EXECUTED_ATTACK_FAMILIES lists 11 families with 0% server execution (switch 0/154, move-energy 0/135, discard-opponent 0/151, draw-until 0/45, shuffle-cost 0/46, conditional-ko, recover-status, devolve-opponent, mirror-heal, look-own-deck, return-opponent-energy); audit reports 1 unexecuted (refs: S265, audit T1). → closed 2026-09-23 S268: the 11 families now run (S267 attack steps; oracle shows effects or skip events); EXECUTED_ATTACK_FAMILIES re-synced to oracle.mjs, 10 client-only families moved out (→ I118), shuffle-cost partial, lost-zone added.
- I117 2026-09-23 P2 [rules] String damage ("30+", "30×") parsed as base 0, dropping printed damage from scaling attacks (refs: S267) → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): parseAttackDamage reads numeric prefix (4efddd64).
- I116 2026-09-23 P2 [rules] rng.shuffle returns a copy; 14 trainer/ability effect sites discarded it so decks never shuffled (refs: S267) → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): shuffleInPlace (c32ead14).
- I115 2026-09-23 P3 [tooling] Attack family misnames: Lapras V SHF SV110 Body Surf→switch (hand attach), Team Rocket's Moltres ex DRI 229→discard-opponent (self discard), Stonjourner VMAX SSH 205→heal, Mew ex CEL 88→switch; announce/UI only (refs: S265, scratch/attack-ability-coverage-audit.md T4). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): classifier attach-chain rule + CHAIN_TEMPLATES requiresAttach (7b4c62d8).
- I111 2026-09-23 P2 [rules] Server attack phase never executes: discard→hand (Sableye V SSH 194), counters on each opponent Pokémon (Mismagius SIT 064), move counters (Dedenne ex PAL 239), devolve (Espeon ex SVP 175), take Prize (Umbreon ex SVP 176), KO (Giratina VSTAR CRZ GG69), Lost Zone (Rotom V CRZ 045), heal-all GX, own-hand discard (Raging Bolt ex PRE 166 draws without discarding) (refs: S265, audit B13). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): counters/devolve/prize/KO/Lost Zone/heal steps (b016ed93, 4efddd64, 4f7ba758).
- I110 2026-09-23 P3 [rules] "Shuffle this Pokémon and all attached cards into your deck" attacks (Primarina PBL 088 Aqua Return, Mew V CRZ 060; shuffle-cost 0/46) never execute server-side (refs: S265, audit B12). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): atkShuffleSelf step (b016ed93).
- I109 2026-09-23 P3 [rules] deckMillScaling (damage-parser.mjs:1123) returns null without "for each … discarded": plain self-mill costs (Tyranitar ex OBF 211, Salamence ex 30th 156) and plain opponent mill (Mega Heracross ex PFL 108) never happen (refs: S265, audit B11). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): atkMill step (b016ed93).
- I108 2026-09-23 P3 [rules] Attack search-and-attach lands in hand: Mew ex CEL 88 Power Move, Rayquaza HP 16 Delta Search; Mew TRI 97 See Off (Lost Zone target) also lands in hand (refs: S265, audit B10). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): search-attach block attaches instead of hand (b016ed93).
- I107 2026-09-23 P2 [rules] Attack deck-top/discard→Bench placement never executes: Mega Delphox ex PBL 008 Trick Portal, Tatsugiri ex SSP 226, Salamence ex 30th 156 Booming Call, Kingdra ex SVP 131 (refs: S265, audit B9). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): atkBenchFromDeckTop/atkBenchFromDiscard (b016ed93).
- I106 2026-09-23 P2 [rules] "Draw cards until you have N" attacks draw nothing: drawCount returns 0 for "until" (damage-parser.mjs:589), no other path — Jirachi ex 30th 155, Dialga-GX FLI 138; 45 rows (refs: S265, audit B8). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): drawUntil step; drawCount stands down when steps own the clause (b016ed93).
- I105 2026-09-23 P2 [rules] Attack attach-from-discard/hand never executes (Regi Charge ASC 048/107/145, Squawkabilly ex SVP 160, Miraidon ex SVP 143, Pikachu ex 30th 150; ~75 groups); only Aura Jab / Raikou milled attach exist (refs: S265, audit B7). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): atkAttach from discard/hand (b016ed93).
- I104 2026-09-23 P2 [rules] Attack discard-from-opponent (Energy/Tool/hand/deck top) never executes: Decidueye ex POR 100, Klefki PAF 159, Gourgeist ex CRI 102, Mega Excadrill ex PBL 103; discard-opponent 0/151 (refs: S265, audit B6). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): atkDiscardOpp* steps incl. return-to-hand (b016ed93, 4f7ba758).
- I103 2026-09-23 P2 [rules] Attack move-energy never executes server-side (Mega Gengar ex MEP 073, Volcanion ex JTG 182, Gengar ex TEF 193; 0/135); legacy has chat-buttons.js:5253 (refs: S265, audit B5). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): atkMoveEnergy steps (b016ed93).
- I102 2026-09-23 P2 [rules] Attack switch/gust never executes server-side (Mega Zeraora ex MEP 089 Zepto Turn, Lapras ex POR 022, Kyogre ex SVP 178; 0/154); legacy has rules-bridge.js:1487 (refs: S265, audit B4). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): design 030 step executor: atkSwitchSelf/atkGust (b016ed93).
- I95 2026-09-23 P3 [rules] 81 activated abilities parse to passive-only steps and can never resolve (Galarian Mr. Rime SHF SV021, Gardevoir ex CEL 93 Imprison, Munchlax UNM 173); Zamazenta VSTAR CRZ 099 needs review (VSTAR route) (refs: S265, audit A8). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): template fallback resolves passive-parsed activated abilities (dfbd981a).
- I89 2026-09-23 P2 [rules] 30 non-passive ability step types have no executeSteps case; `default:` no-ops and executeAbility still marks the ability used — 470 printings no-op, 521 partial (Dewgong POR 019 moveEnergy, Team Rocket's Orbeetle DRI 198, Latios MEG 101, Alomomola BLK 108); legacy has executors for several (refs: S265, audit A2, appendix-unhandled-ability-steps.tsv). → closed 2026-09-23 S267 (branch audit/attack-ability-coverage): executor default now emits effectStepSkipped; abilities fall back to shared effect templates (dfbd981a).
- I114 2026-09-23 P3 [tooling] attack-false-positive-audit.mjs:73 spawns with `cwd: new URL('.', import.meta.url).pathname` = `/C:/…` on Windows → spawnSync ENOENT, prints `undefined`, exit 1 (refs: S265, audit T3). → closed 2026-09-23 S266: cwd via fileURLToPath; spawn errors printed. Verified: exit 0 on Windows.
- I101 2026-09-23 P3 [rules] drawCount treats "Your opponent draws a card" as an attacker draw (Comfey GRI 93 Sweet Kiss) (refs: S265, audit B3). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): drawCount strips "your opponent … draws" clauses.
- I100 2026-09-23 P2 [rules] allBenchDamage (damage-parser.mjs:982) ignores "your opponent's": self-bench recoil ("30 damage to each of your Benched Pokémon" — Copperajah ex PAL 245, Dragonite V SWSH154; 11 groups) hits the opponent's Bench instead (refs: S265, audit B2). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): bench spread parsed per side; ownBenchDamage recoil on the attacker's Bench.
- I99 2026-09-23 P2 [rules] parseAttackEnergyDiscard (attack-effects.mjs:907) returns null for `{R}` symbols, "all {M} Energy", "any amount": live TCGdex "Discard 2 {R} Energy from this Pokémon" (Arcanine ex SVI 032) never pays the cost; 50 groups (refs: S265, audit B1). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): parseAttackEnergyDiscard reads {X}, basic, typed all, two-type costs; player-choice and coin-gated costs return null.
- I98 2026-09-23 P3 [rules] parseUnlimitedHandEnergyAcceleration type-letter map `[wlfmpdgyn]` lacks `r`: Emboar Inferno Fandango ({R}) accelerates any Energy type (ability-executors.mjs:817) (refs: S265, audit A11). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): {R} added to the type-letter map.
- I97 2026-09-23 P2 [rules] Ability "flip a coin. If heads …" never flips server-side, the effect always resolves (Florges TWM 088, Porygon-Z PAR 214, Galarian Slowking CRE 098; 55 groups) (refs: S265, audit A10). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): executeAbility flips once for a single heads-gated effect; tails spends the ability.
- I96 2026-09-23 P3 [rules] Team Rocket's Crobat ex DRI 242 Biting Spree / Team Rocket's Golbat Sneaky Bite parse as passive onOpponentEvolveAbility — damage counters never placed (refs: S265, audit A9). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): own-evolve trigger no longer read as opponent-evolve; damageCounters supports "choose N … on each".
- I94 2026-09-23 P2 [rules] useAbility has no passive/trigger gate: 343 passive/trigger texts execute on demand via the context-menu ability button — thorns abilities place counters at will (Maushold ex PAR 233), KO triggers search the deck (Pecharunt ASC 143 Final Chain), "can't be healed" heals own (Abomasnow PAF 101) (refs: S265, audit A7). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): useAbility legality requires activated wording (isActivatedAbility), corpus-checked.
- I93 2026-09-23 P2 [rules] Attach-then-bonus abilities lose the attach: Teal Mask Ogerpon ex PRE 177 Teal Dance / Xatu PAF 152 parse drawAbility only, Hydrapple ex PRE 011 heal only — bonus with no attach (refs: S265, audit A6). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): hasVerbAttach matches the word; "If you attached Energy … in this way" orders attach first, gates the bonus, heals "that Pokémon", folds counters into the attach.
- I92 2026-09-23 P2 [rules] Draw abilities drop their discard cost: "You must discard a card … in order to use" (N's Zoroark ex ASC 286, Team Rocket's Porygon-Z DRI 155) and "discard your hand and draw N" (Rayquaza VMAX CRZ 102) draw for free; 21 groups (refs: S265, audit A5). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): hand-card discard cost parsed, cost runs before the draw; "discard your hand and draw N" → discardHandThenDraw. Qualified costs (Mad Party, Ultra Beast) still unread.
- I91 2026-09-23 P2 [rules] Search-then-attach abilities put Energy in hand (Toxtricity template): Rillaboom SHF SV006, Pawmot PAF 144, Forretress ex PAF 212 (KO clause also dropped), Solgaleo 30th 105; 23 groups; only the Sinister Surge wording gets destination:'attach' (refs: S265, audit A4, S264). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): search→attach reads "attach them to 1 of your / this Pokémon / your Pokémon in any way you like" (per-card targets); "{G} Energy" keeps type. Forretress ex KO clause still unread.
- I90 2026-09-23 P2 [rules] Non-discard attachAbility is guidance-only (executor.mjs:1526): once-per-turn hand attach (Quaquaval SVI 054, Ethan's Ho-Oh ex ASC 026, Oricorio ex PFL 110; 48 groups) spends the ability with no effect (refs: S265, audit A3). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): parseHandAttach + attachFromHand handler (Energy then target, per-card for "in any way you like"); "as often as you like" left to the normal-attach accelerator.
- I88 2026-09-23 P2 [rules] statusAbility schema mismatch: parser emits target:'opponent' + lowercase status + coinFlip, executor (executor.mjs:1419) reads 'opponentActive'/condition — every status ability poisons the user's OWN Active (Brambleghast PFL 047 Prison Panic verified; 396 printings); legacy reads 'opponent' correctly (refs: S265, audit A1). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): executor normalizes statusAbility (opponent→opponentActive, lowercase status, coin flip); parser reads paralyzed.
- I83 2026-09-23 P3 [rules] Adrena-Brain moves the max available up to 3 counters; the printed "up to" count choice is not offered. Server `variableDraw` also lacks opponentPokemonInPlay/opponentBenchBasic/allBench (draw 0) (refs: S264). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): Adrena-Brain count prompt (pseudo-options -101..); server variableDraw gains opponentPokemonInPlay/opponentBenchBasic/allBench. Legacy count choice untouched.
- I82 2026-09-23 P3 [rules] searchAbility destination `Bench` (capital, abilities.mjs) never equals executor's `bench`, so bench-limit checks skip for ability deck→Bench searches (refs: S264). → closed 2026-09-23 S266 (branch audit/attack-ability-coverage): executor lowercases searchAbility destination; deck→Bench picks now land on the Bench (were going to hand).
- I71 2026-09-21 P2 [board-ui] Battle log in the multiplayer window narrates almost nothing under SERVER_AUTHORITATIVE=1. → closed 2026-09-21 S233 (design 021): new pure `client/src/setup/netcode/server-battle-log.mjs` (+ DOM caller) maps ~20 ordinary-play server events to perspective-phrased lines, composing `attack-announcements.mjs`; server adds an additive `trainerPlayed` event and gates `cardAttached` to non-Pokémon so evolves don't double-log; old `attack-announcements.js` caller deleted. 17 mapper unit tests + 6 headless `applyCommand→events→mapper` tests; full suite 2635/2635. Live 2P not runnable here (no socket.io/express in node_modules).
- I77 2026-09-21 P3 [rules] GX-attack coverage gap on the design-018 hotspot. → closed 2026-09-21 S232 (branch `fix/gx-once-per-game`): 3 tests added to once-per-game.test.mjs — a GX attack with a deck-search clause spends `gxUsed` on the initial resolve (pendingChoice open, turn not ended), a `useVStarGX kind:'gx'` marker blocks a later GX `attack`, and a legacy flags-only spent GX blocks it. once-per-game.test.mjs 17/17; full suite 2613/2613.
- I76 2026-09-21 P3 [rules] `player.flags.{gxUsed,vstarUsed}` write-only once-per-game mirror. → closed 2026-09-21 S232 (branch `fix/gx-once-per-game`): dropped the flags writes from `advanceTurn` (the mirror loop) and the `useVStarGX` apply case; `player.oncePerGame` is now the single writer and `view.mjs:50` still projects it into the exposed `flags`. Full suite 2613/2613.
- I75 2026-09-21 P3 [rules] "A GX attack is spent" had two disagreeing checks. → closed 2026-09-21 S232 (branch `fix/gx-once-per-game`): the `attack` GX gate now calls `oncePerGameUsed(player,'gx')` (the same helper `useVStarGX` uses), so a legacy flags-only spent GX blocks a second GX attack; regression test added. Full suite 2613/2613.
- I69 2026-09-20 P2 [rules] Special-energy triggers — authoritative/server parity. → closed 2026-09-20 S223: new `shared/engine/effects/special-energy.mjs` executes the `planSpecialEnergyTriggers` actions against the draft (draw/drawUntil/heal/removeDamage/addDamage/clearStatus/returnBasicEnergy/devolve/search/switch + on-discard returnToHand/reattach + on-KO returnToHand/drawUntil + endTurn damage). Wired in `reduce.mjs`: `attachCard` (attach + evolve), `resolveAttackEffectPhase` (damaged, via `draft.__attackEffectPhase`), `handleKnockout` (KO + attached-energy Recycle), `resolveCheckup` (end-of-turn, both players), `moveCard`→discard, and `trainer-steps.discardCard`; `resolveChoice` resumes search/switch via a `specialEnergy` resume token. Legacy once-per-game reduction now guarded (`toolPrizeCountAdjust` `skipSpecialEnergy` + `victimPlayer.flags.legacyPrizeReductionUsed`). `getAttachedSpecialEnergies`/`hostHasBasicEnergy` made model-agnostic (`attachedTo` or `image.relative`). 11 new headless tests in `special-energy-authoritative.test.mjs`; suite 2587/0. Browser verification split to I70.
- I68 2026-09-20 P2 [rules] Special-energy on-attach/on-KO/on-discard triggers had no live execution path. → closed 2026-09-20 S221: client wiring — `rules-bridge.js` `rules-energy-attached` now runs parser plans (draw/search/heal/remove-damage/damage-counter/clear-status/switch/return-basic-energy), `move-card.js` rewrites discard destinations for `onDiscardReturnToHand` (Recycle/Nitro) and `onDiscardReattach` (Boomerang/Burning), and the KO watcher applies `onKnockoutReturnToHand` (Splash/Rescue, attached cards discarded) + `onKnockoutDraw` (Gift). Planner `planSpecialEnergyTriggers` is pure + tested (D86). Residual (devolve, Legacy prizeReduction, other triggers, server parity) tracked as I69.
- I65 2026-09-20 P2 [rules] Pokémon ability parse coverage: 199 of 4228 corpus abilities fell through `parseAbility`'s `passiveAbility` fallback (179) or classified `unknown` (62, overlapping). → closed 2026-09-20 S211: `parseAbility` gained ~40 step types (locks, energy/damage moves, transform/self-return, deck peek/Lost Zone/win, condition recovery, coin/attack-cost/misc) and `classifyAbility` gained 18 families + predicate fixes (D82); abilityGaps 199→0, unknown 62→0, passive fallback 465→0, and the 1 unattributed line (Buried Fossil's static evolve clause) is recognized as card text → 0. Caveat: still announce-only — ability effects are not executed.
- I66 2026-09-20 P3 [rules] Attack-effect classifier long tail: 168 of 8690 corpus attacks classified `flat` though the printed text carried an effect. → closed 2026-09-20 S209/S210: `classifyAttackEffect` now returns a specific family for all 8690 attacks (attackGaps 168→0); S208 added six families + broadenings (D80), S210 closed the last six one-offs (Harmonize→conditional-damage, Mach Wind/Extra Comet Punch→next-turn-bonus, Transfer Pain→`move-damage-counter`, Unown I→`neutralize-opponent-energy`, Fire Wall→retaliate) and added `move-damage-counter`/`neutralize-opponent-energy` (D81). Two steals found and fixed (Wobbuffet retaliate, Shaymin Energy Bloom heal). Caveat: this is the classifier/announcement layer only — attack effects are still not executed.
- I22 2026-09-09 P2 [netcode] `VSTARGXFunction` translator/legacy parameter mismatch: `VSTAR-GX.js` sends `[type]` ('GX'|'VSTAR') but `dual-run-bridge.js`'s case reads `[instanceId]`, so `payload.instanceId` is always 0 and the GX/VSTAR distinction is lost — the server's `useVStarGX` reducer sets both `vstarUsed` and `gxUsed` regardless. Pre-existing, not introduced by design 003 (refs: design 003, S60). → closed 2026-09-20 S204: the translator now carries the discriminator — `dual-run-bridge.js:817-829` maps `VSTARGXFunction`'s `[type]` to `payload.kind` ('gx'|'vstar'); `reduce.mjs`'s `effectiveOncePerGameKind` reads `payload.kind` and the apply case spends only the matching allowance (`kind !== 'gx'` → `vstarUsed`, `kind !== 'vstar'` → `gxUsed`), so the distinction is no longer lost.
- I57 2026-09-18 P2 [netcode] Discard-pile viewer is dead under SERVER_AUTHORITATIVE: `coverClick`/`openDiscardPileViewer` (click-events.js, discard-pile-viewer.js) read `getZone(user, 'discard').array`, which is empty under server rendering, so the click returns before opening. Fix like design 012's deck peek: read `getAuthoritativeZoneArray(side, 'discard')` (discard is public, no server round-trip needed). (refs: design 012, S171) → closed 2026-09-18 S172: viewer reads the live pile (discard-pile-source.mjs), clicked card found by instanceId; live probe on both seats.
- I28 2026-09-17 P1 [holo][netcode] MP bench holo animates for owner, looks frozen on the other client's opp view → closed 2026-09-17 S164: self-containers.css lacked the 7 per-generation holo @imports opp-containers.css/index.css had, so owner and opponent rendered different foil effects; imports added, parity test extended, TEMP DEBUG removed
- I48 2026-09-17 P2 [rules] User report: Meowth ex on bench, never used, showed "unusable" in the
    attack-preview ability zone. Log export cut off before the click event, so that exact instance
    wasn't directly reproduced. Source read found a real, confirmed defect on the same mechanism:
    `effects/ability.mjs:69` marked the server's `abilitiesUsed` flag by `card.name` (instanceId
    fallback was dead code — every card has a name), so two same-named Pokémon in play shared one
    used-flag slot; using one blocked the other's separate ability. Inconsistent with
    `rules-state.mjs:592 abilityKey()`, which deliberately prefers instance identity for this
    reason. → closed 2026-09-17 S161: `effects/ability.mjs` now keys by `instanceId` first (name
    only when no instanceId); probe script flipped the collision red→green pre/post fix; new
    regression test `shared/engine/__tests__/ability-execution.test.mjs` ("two Pokémon sharing a
    name track 'used' independently"). Not fully proven to be the user's exact single-Meowth-ex
    case (no duplicate name visible in the supplied log) — flag for a follow-up browser repro if
    it recurs.
- I52 2026-09-16 P3 [rules] Server prices special Energy by name only (rules/server-energy.mjs): Prism/Neo Upper-style "any type" Energy pays only its name type or Colorless, and U Energy etc. are unrecognized. The client guidance (energy-effects.mjs) treats unknown specials as wildcards. Pick one rule set and make both use it (refs: S145). → closed 2026-09-16 S146: server-energy.mjs now reads Energy through energy-effects.mjs (classifyEnergyEffect + resolveAttachedEnergyType), same as client guidance and bot.
- I51 2026-09-16 P3 [netcode] A Trainer dropped on the board before its cardStats (effect text) reaches the server stays a plain move with no effect (reduce.mjs promoteTrainerPlay needs text) (refs: S144, S145). → closed 2026-09-16 S146: the drop is rejected with card_data_pending, so the card stays in hand until cardStats lands.
- I50 2026-09-16 P3 [rules] Server models an evolution as the Evolution card attached under the Basic (reduce.mjs attachCard); the Basic stays the root, so anything reading the root's name/hp/attacks sees the Basic. trainer-steps.mjs reads the top Pokémon card for Salvatore/devolve, but attack/KO/retreat code does not. Also a Trainer whose effect has no valid target (Rare Candy with no Stage 2 in hand, 11x in S144 bot runs) is still playable and is discarded for nothing (refs: S144). → closed 2026-09-16 S145: attack/KO/checkup/retreat read evolvedView (D40); Rare Candy and Tools blocked with no valid target, Rare Candy traces the evolution line (D41); 15 live bot games PASS, 0 rejections.
- I35 2026-09-10 P3 [rules] `shared/engine/effects/executor.mjs` implements ~23 of 40+ parsed
    trainer-effect step kinds from `trainer-effects.mjs` — an unimplemented effect silently
    no-ops instead of erroring, so a headless harness (or `playtest-bot.mjs`'s legacy-mode runs,
    which don't exercise this path but a future authoritative-mode run of it would) can report
    false-green games where a played card did nothing. No coverage gate exists to catch the gap
    (refs: design 004 investigation, S75). RENUMBERED from I33 in the S87 merge: the S86
    maintenance sweep and the S87 debug session each independently claimed I33, on branches that
    had not yet met. This entry is the sweep's; the pass-wedge I33 in Closed is the debug
    session's, and is the one three pushed commit messages reference. → closed 2026-09-16 S144: all 31 missing step kinds implemented in effects/trainer-steps.mjs with tests; parser fixed for 10 real cards; 5 starter decks play to PASS under SERVER_AUTHORITATIVE.
- I49 2026-09-16 P2 [netcode] Trainer drop runs its effect only after the card's effect text reaches the server via cardStats (async TCGdex enrichment); a Trainer dropped before then stays a plain hand -> board move with no effect. Live bot run S144 (Lucario starter deck): 9 hand -> board drops stayed plain moves (likely Tools/Stadiums, which then sit on 'board' unattached/unplaced) and 6 promoted plays emitted no events (likely unimplemented step kinds, I35) — card names not captured (refs: S144). → closed 2026-09-16 S144: Tools/Stadiums promoted too; unplaced board cards were Stadium replacements (cardMoved = old Stadium discarded); 0 rejections over 8 live bot games.
- I40 2026-09-11 P3 [netcode] deal-order.js resetDealOrder() has no callers, so a game's dealOrder/starter survive into the next game in the same tab. Not shown harmful (second-game probe dealt correctly, zero desync after S102), but the documented reset is unwired. (refs: S102) → closed 2026-09-11 S103: wired resetDealOrder() into rules-bridge.js's resetRulesSession, which already runs on the 'room-changed' document event (leave/join) and on every Reset button — the same trigger I41's neighboring rules-session-reset logic uses. 1307/1307 pnpm test (+1 pre-existing deal-order test suite, unchanged pass count).
- I42 2026-09-11 P3 [netcode] syncCheck compares the client's last-applied view with current server state; a command landing in between can report a transient desync. Harmless since S102 (recovery is a silent requestView) but still a false signal. (refs: S102) → closed 2026-09-11 S103: client's heartbeat now tags its zone hashes with the view's stateVersion (getLastRenderedVersion); server's 'syncCheck' handler skips the compare (no desync emitted) when gameRoom.state.stateVersion has moved past that version, since the client's snapshot is stale by design at that point, not diverged. Next heartbeat (30s) re-checks against settled state. pnpm test 1308/1308 (added a stateVersion-forwarding unit test to sync-check.test.mjs; no existing test harness covers the raw server.js socket handler, consistent with the rest of that file).
- I37 2026-09-11 P2 [netcode] AUDITED S95: `shuffleHandThenDraw`/`countShuffleDrawPlus`/`ionoShuffle`/`discardHandThenDraw` (trainer-execution.js:717-758 — Judge/Iono/N/Professor's-Research-style effects) never called `_shuffleZone`, so design 007's specific move-vs-shuffle race couldn't occur here; the real bug was each loop firing `moveCardBundle` un-awaited, letting a later iteration's synchronous `buildMoveCardHints` read the zone before the prior un-awaited call's own splice landed, sending a stale `cardHints.moving`. Also found none of the four ever randomized the deck. → closed 2026-09-11 S97: added `await` to every `moveCardBundle` call in all four cases (sequential, matching design 007's "await the move before the next reads the zone" pattern); added a real `shuffleDeckAfterSearch(...)` call after moving the hand into the deck for `shuffleHandThenDraw`/`countShuffleDrawPlus`/`ionoShuffle` (per-player for `ionoShuffle`), reusing the existing search-shuffle helper instead of inventing a new one. `discardHandThenDraw` needed no shuffle (it discards, doesn't reshuffle into the deck). 1297/1297 `pnpm test`; targeted `npx eslint` on the touched file shows only pre-existing CRLF/prettier noise, 0 errors. Not live 2P verified this session (no repro harness for these specific effects yet) — the fix mirrors design 007's already-live-verified pattern exactly (refs: design 007, S91, S95).
- I36 2026-09-11 P3 [netcode] INVESTIGATED S95, LIKELY BENIGN: KO auto-promotion (chat-buttons.js:1184-1207, `moveCard(oppPlayer, user, 'bench', 'active', 0)`) is called directly — not via `moveCardBundle` — from inside `attack()`, which both clients replay identically off the same seeded RNG bundle (S87's `isMirrorReplayCall` fix). It carries no `cardHints` and is never relayed. → closed 2026-09-11 S97: no code defect found, confirmed safe by design rather than patched. Traced the abort's blast radius: `applyPeerAction` (socket-event-listeners.js:158-171) increments `systemState.oppCounter` UNCONDITIONALLY right after `await acceptAction(...)`, regardless of whether the wrapped `moveCardBundle` returned `true`, `false` (the `hint_mismatch`/`missing_card` mirror-abort), or threw — so an aborted relay never desyncs the action counter or blocks any later action. Combined with the original report's "final boards matched," the abort is the safety net (added by design 007's predecessor work) correctly discarding a stale relay against a board that already converged via deterministic replay — working as intended, not a bug. No repro was available to verify further (refs: S91, S95).
- I24 2026-09-10 P1 [netcode] Legacy (non-authoritative) `moveCardBundle` mirror desync: a `deck`→`bench` move (Piloswine #7, room test1) reached the receiver with no `cardHints` attached, so `needsHintVerification` never triggers (move-card-bundle.js:125-129) and the move applies via raw relay `index` against the receiver's own zone array unchecked (move-card-bundle.js:118,209-227) — grabbing the wrong card. Every later hint-verified move touching that slot then aborts on `hint_mismatch` (move-card-bundle.js:141-159) with no resync (I12), permanently diverging `bench`; ended with Mamoswine ex #23 invisible to ARISSI after a bench→active→bench abort pair. Repro: `ptcg-sync-log_combined_test1_1788985695598.json` seq 108-159 (ARISSI client) (refs: I12, S70) → closed 2026-09-11 S95: live 2P check (design 007, S91/S94 fix) with real Buddy-Buddy Poffin / Ultra Ball / Nest Ball via `?e2e=1&syncLog=1` debug mode across two Playwright clients (`manual-verify-i24.mjs`, legacy server i.e. `SERVER_AUTHORITATIVE` unset). Confirmed: peer (`opp`) sees the exact bench A has after a Buddy-Buddy Poffin/Nest Ball search (`I24 Basic 7` on both sides); sync log shows the `move` (deck→bench) emit before its `shuffleZone` emit on every search; zero `shuffleZone.indices_mismatch` entries. Acceptance criteria from design 007's manual test plan met.
- I33 2026-09-10 P1 [harness] `pass` reported success but never ended the turn, wedging games at turn 10-14 — the bot re-picked pass until the 60-action turn budget ran out, reporting a wedge that explained nothing (refs: I29, design 004, S87) → closed 2026-09-10 S87: THREE harness defects, no engine bug in this issue. (1) `__ptcg.act()` computed `{ ok: ok !== false }`, but every client action signals refusal only by appendMessage'ing a `⛔` line and returning undefined — so a blocked action was indistinguishable from a successful one. act() is now wrapped once to watch the chat this client actually writes to and report the `⛔`. (2) The runner only ever asked client A whether the game had ended, but a deck-out is detected by whichever client fails to draw — when it ended on B, the runner kept driving a finished game. Both clients are now asked. (3) `gameEndedInfo` comes from the `rules-game-ended` event, but a client can reach `phase: 'ended'` without it having fired; the phase is the durable fact and is now trusted too, on both clients. Also fixed the dump's chat capture, which read `#chatbox` while 2P writes to `#p2Chatbox` and so always reported an empty log — that is why the first pass at this issue had nothing to go on. Verified: fixture `--scorer=coverage` 9/10 on a fresh 10-game soak (was wedging routinely), 3/3 on each of seeds 42/99/123, heuristic 5/5, pnpm test 1254/1257.
- I31 2026-09-10 P1 [netcode] Legacy 2P public-board divergence after a Trainer is played — the acting client's Trainer reached its discard while the peer's copy stayed in the `board` staging zone forever, diverging the public board with zero cmdRejected. Repro `--seed=7 --deck=<lucario 60>`; caught with a Pokémon Tool (Air Balloon) in A's discard vs B's `board` (refs: I24, I32, design 004, S86) → closed 2026-09-10 S87: SAME ROOT CAUSE AS I32's MIRROR HALF, in a second action. `attack()` re-ran `canPerformAction` on the replay path; when the mirror's per-turn state disagreed it took the `⛔` branch and returned, skipping everything below it — including the `discardBoard(user, user, false, false)` sweep that empties `board`. That sweep is called with emit=false, so the acting client never relays it: the peer is expected to run its own copy during the attack replay, and a blocked replay silently loses it. Fixed with the same `isMirrorReplayCall` guard used for retreat, skipping ONLY the legality rejection — the status gating below it (asleep/paralyzed/confused) still runs, since it replays the actor's seeded coin flips from rngBundle and must stay in lockstep. NOT the I24 family after all: no card-identity or index-space problem was involved. Verified: seed 7 real-deck divergence gone (board [] and discard 18/18 on both sides), actions per game 27 → 507, distinct mechanics 16 → 25.
- I32 2026-09-10 P1 [rules/netcode] Retreat on a FULL bench desyncs the peer — FIXED S87 (both halves). Root cause of the local half: `retreat()` did two untargeted moves (active→bench, then bench→active), and with 5 on the bench the first one asks for a 6th, so move-card.js's bench-limit gate (move-card.js:279-292, which only applies when no `targetCard` was named) rejected it and returned ok:false — leaving the retreating Pokémon in the active zone beside the newly promoted one, wrecking every later bench play. Fixed S87 by making it ONE targeted move, so the gate is skipped and autoMoveActiveBenchCard's case 3 (auto-move-active-bench-card.js:57-75) performs the swap; the bench never exceeds its limit at any point. My original "high bench index" framing was wrong — index correlated with bench fullness, it was not the variable (refs: I30, design 004, S86, S87).
    STILL OPEN (the mirror half): the acting client's own board is now correct (active=1, bench=5, verified in dumps `out/playtest/42-{0,1,2}-*.json`), but the peer's view of it is not — `--scorer=coverage` still fails 3/3 with "A's own board != B's view of A" on the retreat. HYPOTHESIS, untested: `resolvedBenchIdx` indexes the bench ARRAY, which also contains attached Energy cards, so a different attachment layout on the peer's mirror makes the same integer name a different element there — the I24/I30 index-space family. A stable card identity, not an array index, is likely the real fix. `--scorer=heuristic` is 3/3 and `pnpm test` is 1250/1253, so the local fix is a strict improvement; do not revert it while fixing the rest.
- I30 2026-09-10 P1 [rules] Legacy retreat desync: `retreat()`'s `processAction(user, emit, 'retreat', [])` (chat-buttons.js:2367) sent no target identity, so the peer's replay always defaulted to the *first* `isBoardPokemon` bench card regardless of which bench Pokémon the acting client actually swapped in. Found by design 004's playtest bot (~4/5 fixture-deck games) (refs: design 004 slice 6, S84) → closed 2026-09-10 S85: threaded the resolved bench index through as the processAction parameter (`parseRetreatArgs`, sync-action-args.mjs, mirroring the existing `parseAttackArgs` pattern) instead of a live DOM image, which means nothing on the peer's own DOM. Live-verified: the same seed that previously failed 4/5 games (`--seed=10 --games=5`) now passes 5/5, plus a fresh 6/6 (`--seed=20`).
- I29 2026-09-10 P2 [netcode] Legacy 2P turn-state desync after several turns: driving a real
    game purely through `__ptcg` past I28's fix, both clients eventually appeared to report
    `turnState().turnPlayer === 'opp'` simultaneously, wedging the game with zero `cmdRejected`.
    Narrowed to the attack path (not `pass()`) across S81's two runs (refs: I24, design 004,
    S80-S81) → closed 2026-09-10 S82: **not an engine bug — a smoke-harness race.**
    `.agent/scratch/smoke-i29-realpass.mjs`'s stall-detection re-poll always re-read `a.page`'s
    `turnState()` regardless of which side (`active`) had just been queried for `options()`; when
    B was the active side, the harness polled A (whose state had already correctly flipped and
    would never change again) for up to 3s, concluded "no legal option, nothing changed", and
    stopped the drive loop — while B's own mirrored `attack()` → `endTurnWithBanner` → `endTurn`
    was still completing over the socket round trip. Fixed the harness to poll `active.page`
    itself (whichever side was just checked) with a 6s window instead. Re-ran twice: a fresh
    legacy 2P game now plays 15+ real attacks alternating cleanly (zero `cmdRejected`) through to
    a legitimate deck-out win, both times. No `attack()`/`endTurn()` engine code was touched —
    temporary instrumentation added to `chat-buttons.js`'s `attack()`/`endTurnWithBanner()`
    confirmed the receiving side's mirror (`attack('opp', emit:false, ...)`) always reaches
    `endTurnWithBanner` and flips `turnPlayer` correctly; instrumentation removed before close
    (repo diff is harness-file-only: `.agent/scratch/smoke-i29-realpass.mjs`).
- I28 2026-09-10 P1 [rules] Legacy mode's local win check ends the game after turn 1, every
    game: `evaluateWinCondition`'s `checkWinConditions` (shared/engine/rules/ko-flow.mjs:86-93)
    treats "0 active + 0 bench" as an instant loss for *either* side the moment a turn ends
    (chat-buttons.js:415 `endTurnWithBanner` → `endTurn`), but the side that hasn't had its own
    first turn yet legitimately has 0 Pokémon in play — Basic placement is an ordinary turn-1
    action in this client, no separate "setup" placement phase precedes it. Found live via
    design 004's slice 1-4 smoke run (refs: design 004, S79) → closed 2026-09-10 S81: gated the
    `activeCounts` check in both copies of this logic (chat-buttons.js's `evaluateWinCondition`,
    rules-bridge.js's `evaluateAndApplyWinConditions`) behind `rulesState.turnNumber >= 3` — a
    global ply counter that only reaches 3 once each side has completed one turn.
    `playerTurnCount` doesn't work for this: `endTurn` bumps the *incoming* player's count
    immediately, before they've acted. Live-verified: a fresh 2-page run now plays past turn 1,
    both sides place Pokémon and attack (`.agent/scratch/smoke-004-slice1-4.mjs`). Surfaced a
    separate, unrelated desync further into the game — filed as I29, not fixed here.
- I19 2026-09-09 P3 [netcode] Reveal/hide family (revealCards/hideCards/revealShortcut/hideShortcut)
    has no server-side driver: shared/engine never sets card.revealed=true, so view.mjs's
    revealed-branch rendering is correct but unreachable (refs: design 002 §3.4d, S51) → closed
    2026-09-10 S68: all 4 reclassified 'server_command' in DISPOSITION_TABLE. New COMMAND_SCHEMAS
    entries (position-addressed by zoneId/index, same pattern as takePrizesByIndex — no cardHint
    needed) and reduce.mjs cases flip `card.revealed` on the sender's OWN zone only
    (`draft.players[playerId]` is always the sender's own state; no cross-player targeting was
    added, since view.mjs's redaction is symmetric — flipping the flag once unhides the card in
    both players' views). `revealShortcut`/`hideShortcut` bounds-check the index (`stale_view` if
    out of range); `revealCards`/`hideCards` flip every card in the given zone. dual-run-bridge.js
    gained 4 translator cases; `client/src/actions/general/reveal-and-hide.js`'s 4 functions gated
    with `dispatchAuthoritativeAction` (`user === 'self' && emit` — matches `attack`/`retreat`'s
    no-identity gate shape) right before their legacy body, same accepted-loss precedent as prior
    slices (local chat message skipped when gated). Opponent-triggered reveal (legacy's
    `mouseClick.cardUser === 'opp'` case) intentionally NOT wired to a command — gate requires
    `user === 'self'`, so that path still falls through to the old direct-socket-emit body,
    unchanged from before this fix (same broken/no-op-under-authoritative-rendering state as
    today, not regressed further). **Verified:** 1208/1208 `pnpm test` (16 new tests: 5 in
    dual-run-bridge.test.mjs, 6 in server/game/__tests__/zone-op-commands.test.mjs including a
    `viewFor` cross-player redaction check and an own-zone-only security check). `pnpm test:2p`
    ALL PASS (flag off, live server). Lint clean (0 errors) on every touched file.
- I27 2026-09-09 P2 [netcode] Client coin flip and server turn order are decided independently, so they disagree ~50% of the time: the browsers run their own `rulesCoinCallOverlay` flip, while `server.js:737` invokes the `setup` command with `payload: {}` and `setupGame` (`setup.mjs:136-141`) therefore falls through to `starter = activeRng.next() < 0.5 ? ...`, never receiving the `firstPlayerId` its own signature accepts. Until the first view lands, the client whose flip disagreed believes it is its own turn and its first action is rejected `"It's not your turn."` — self-healing once `reconcileTurnState` (S65) applies a view, but wrong until then. Same class as I17 (client shuffle vs server shuffle) and the same fix shape: pass the coin-flip winner as `setup`'s `firstPlayerId`, or take the starter from the server. Found as flip-gate flakiness; the gate now waits on `turnState().fromServer` to work around it (refs: I17, design 002 3.12, S65). → closed 2026-09-09 S67: same fix shape as I17 — feed the client the server's already-known answer instead of guessing. `server.js`'s post-`setup` broadcast now includes `starter` ('self'/'opp', relative to each recipient) alongside `order` on the `dealOrder` event; `deal-order.js` caches it (`getDealOrderStarter()`), available before the coin-flip UI ever runs since `setupPrizes()` already awaits `dealOrder` earlier in the sequence. `rules-bridge.js`'s `runTurnOrderCoinFlip` overrides its local `decideTurnOrder()` guess with the authoritative starter when in 2P + `serverAuthoritative`; the peer-mirror handler (`turnOrderCoinFlip` rulesEvent) now trusts the sender's broadcast `turnPlayer` (inverted to its own perspective) instead of independently recomputing from call+result, since a recompute couldn't see the sender's override. Flip animation stays cosmetic-only; visual call/result may not narratively match the forced winner when overridden (accepted, same class of cosmetic loss as I17/I24's `deck`-exclusion tradeoffs). **Verified:** 1197/1197 `pnpm test` (4 new tests in `deal-order.test.mjs`). Lint: zero non-prettier errors on all 5 touched files. Not verified against a live two-browser run this session — no code path changed outside the flag-gated `serverAuthoritative` branch, and `flip-gate-test.mjs` already exercises this via its `turnState().fromServer` wait (refs: I17, design 002 3.12, S65). I25 2026-09-09 P2 [netcode] Turn/HUD banner is not refreshed under authoritative rendering: `reconcileTurnState` (S65) syncs `rulesState` from the view but deliberately does not dispatch `rules-turn-began`, because `rules-bridge.js` hangs legacy knockout/deck-out adjudication (`checkKnockouts`, `checkDeckOut`) plus banner `refresh`/`updateBadges` off that one event — firing it would re-introduce the local adjudication design 003 exists to remove. The banner and badges therefore go stale under the flag. Needs a refresh signal that carries no adjudication (refs: design 002 3.12, S65). → closed 2026-09-09 S66: new display-only `rules-turn-view-applied` CustomEvent, dispatched from `reconcileTurnState` (`apply-view.js`) whenever it applies a view's turn state. Wired as an extra listener alongside `rules-turn-began` on the HUD's `refresh()` and status-badge `updateBadges()` in `rules-bridge.js` — `checkDeckOut`/`checkKnockouts` (adjudication) deliberately left off it. 1193/1193 `pnpm test` (1 new test; also fixed 2 pre-existing tests whose mock documents lacked `dispatchEvent`). Lint clean on all touched files.
- I26 2026-09-09 P1 [netcode] Server card model has no `hp` or `attacks`, so knockouts can never fire under `SERVER_AUTHORITATIVE=1`: `loadDeck` (reduce.mjs:2054-2089) builds every server card from the 7-field deck row `[quantity, name, type, imageURL, number, set, tcgId]`, and nothing else ever sets those fields — `createCard` defaults `hp: null`, `attacks: []`. Consequences: reduce.mjs's KO check `koHp > 0 && defender.damage >= koHp` is always false (no KO, no prizes, deck-out is the only reachable win); every attack deals a flat 10 via the `{ name: 'Attack', damage: 10 }` fallback regardless of the printed attack; weakness/resistance/retreat cost never apply. Not a fixture artifact — real decks take the identical path, and client-side TCGdex enrichment is async so the fix has a timing question, not just a plumbing one. **Blocks 002's 3.12 flip.** Repro: `flip-gate-test.mjs` tests 8 and 9 (refs: design 002 3.12, design 003 slice 5 row 9, S65). → closed 2026-09-09 S65: new `cardStats` command (`commands.mjs` schema, `reduce.mjs` apply case) carries hp/attacks/types/weakness/resistance/retreatCost/stage, addressed by syncInstance across every zone and applied in place so it is safe mid-game. Client sends its own deck's stats from `build-deck.js` once `ensureCardData` settles (`client/src/setup/netcode/card-stats.js`); the card list is snapshotted at build time because setup deals 13 of 20 cards out of `deck.array` before the send fires. Flip gate now ALL PASS: KO fires at exactly 6 attacks, win reason "no Pokémon in play" instead of deck-out.
- I24 2026-09-09 P1 [netcode] `zoneArrays` (`get-zone.js`) never populated from server views,
    so the 3.11 desync heartbeat and e2e API's `boardHash`/`zone` (both read `getZone(...).array`)
    would report constant false-positive desyncs / no real hash under full authoritative
    rendering, blocking 002's 3.12 flip-gate exit test → closed 2026-09-09 S64: gave both
    consumers a `apply-view.js`-cache-backed source instead of touching legacy `zoneArrays`.
    New `lastAppliedView` cache in `apply-view.js` (set at the end of every accepted `applyView`,
    cleared by `resetRenderState`) exposed via `hasAuthoritativeView`/`getAuthoritativeZoneArray`/
    `getAuthoritativeStadiumArray` — order-correct by construction since it's literally the array
    the server sent, unlike `cardRegistry` (a `Map`, insertion-ordered, not view-ordered). Wired:
    `sync-check.js`'s new `viewBackedGetZone` (used by the heartbeat in
    `socket-event-listeners.js`, which only ever runs under the flag) and `e2e-api.js`'s new
    `liveZoneArray` (prefers the view cache once populated, falls back to legacy `getZone` before
    that / in legacy mode — zero behavior change there). Found a second, deeper gap while wiring
    this: `deck` is redacted to `{ count }` even for its own owner (design O4-A / I5), so a
    client can never produce a real per-card deck hash — even with `zoneArrays` fixed, comparing
    deck would be a permanent false positive. Excluded `deck` from the 3.11 comparison
    specifically (not from `hashState`/`hashBoardSnapshot` generally, which the replay-harness
    and legacy dual-run recording still need full deck fidelity from): client's
    `PLAYER_ZONES` drops it, server's new `excludeOwnerSecretZones` (`sync-check.mjs`) drops it
    from `hashStateZones`'s output before `findFirstDivergentZone` compares. **Verified:**
    1180/1180 `pnpm test` (9 new tests: apply-view cache lifecycle, `viewBackedGetZone`, deck
    exclusion client+server), `pnpm test:2p` ALL PASS (flag off), lint clean on every touched
    file, server boots clean with `SERVER_AUTHORITATIVE=1` (manual alt-port check). 3.12 itself
    still not started — this only unblocks it (refs: I15, design 002 §3.7/3.8/3.10 deviations,
    O4-A, I5, S63).
- I21 2026-09-09 P2 [netcode] Relayed opponent actions still dual-execute: `acceptAction` replays
    every `pushAction` through the legacy body (moveCardBundle mirror path etc.) even under
    `serverAuthoritative`, so the peer's move renders twice — once by the legacy mirror, once by
    the server view. Design 003 slice 1 gates only locally-initiated actions; suppressing the
    mirror is one relay-layer decision that applies to all families, not a per-family gate
    → closed 2026-09-09 S63: `accept-action.js` now checks `DISPOSITION_TABLE` (via new
    `isMirrorSuppressedAction` in `authoritative-dispatch.js`) and skips the legacy body for any
    `server_command`/`manual_override` action relayed as `user === 'opp'` while
    `systemState.serverAuthoritative` is on — every other disposition (server_lifecycle,
    replaced_by_protocol/redaction, ui_local, ...) still runs locally, since the server view
    carries no equivalent for those. 4 new unit tests. 1175/1175 `pnpm test`, `pnpm test:2p`
    green (flag off), lint clean.
- I17 2026-09-09 P1 [netcode] Client's local legacy shuffle and the server's own authoritative
    shuffle (setupGame via activeRng, shared/engine/setup.mjs:60) were two independent RNG
    streams: a card the browser showed in hand could still be in the server's `deck` zone, so a
    client-initiated moveCard referencing it was rejected `stale_view` → closed 2026-09-09 S47:
    server never trusts a client shuffle (D10) — instead, once both decks are actually loaded
    (fixed a second latent bug here too: the old guard fired on the FIRST loadDeck, dealing an
    empty hand for whoever hadn't loaded yet), the server runs 'setup' and emits each player its
    own syncInstance deal order ('dealOrder' event, server.js); client's setupPrizes()
    (actions/general/setup.js) waits for and uses it in server-authoritative 2P instead of a
    local shuffle. New client/src/setup/netcode/deal-order.js holds the wait/resolve plumbing.
    Verified: replay-harness.test.mjs green against 3 fresh live-browser recordings (hand-order
    difference remaining was a pre-existing cosmetic client sort, see canonicalizeHash in the
    test — unrelated, already tracked as I15/design row 10). 1089/1089 pnpm test green.
- I18 2026-09-09 P1 [netcode] No code path ever called the 'setup' command server-side: server.js
    only invoked handleCommand for 'loadDeck', and translateActionToCmd correctly returns null for
    setupPrizes/drawOpeningHand/readyUp (server_lifecycle) — but nothing else ran the equivalent
    server-side deal, so GameRoom state never dealt hands/prizes at all → closed 2026-09-09 S47:
    server.js now calls handleCommand('setup', ...) once both players' decks are loaded (mirrors
    the existing loadDeck interception).
- I16 2026-09-09 P2 [netcode] D6's "undo = commandLog replay minus tail" premise was broken: deck
    load mutated gameRoom.state.zones.deck outside commandLog → closed 2026-09-09 S46: added a
    real `loadDeck` command (shared/engine/reduce.mjs, commands.mjs) so deck bootstrap is logged;
    server.js routes exchangeData/loadDeckData through gameRoom.handleCommand('loadDeck', ...)
    instead of calling initializePlayerDeck directly; `undo` implemented as commandLog-minus-tail
    replay from a fresh seeded state.
- I48 2026-09-16 P2 [netcode] SERVER_AUTHORITATIVE hand renders every card TWICE: after setup client A's #hand held 8 legacy <img>s (no data-instance-id) plus 9 server-rendered ones (apply-view.js); legacy zone arrays (window.__ptcg.zone) still count 9, so harness waits on zone counts are unreliable — likely also behind I47. Repro: .agent/scratch/mp-preview-repro.mjs "hand imgs" dump (refs: S141, I47) — **Closed S142 2026-09-16**: legacy move/sort/shuffle/reveal paths no longer write the DOM of server-drawn zones (server-rendered-zones.mjs, rebuild-zone-dom.js); live repro 53 duplicate <img>s before, 0 after. Not the cause of I47 (test:flip still stalls after join).
- I131 2026-09-23 P1 [rules] KO prize counts wrong for 11 Tools: parsePrizeModify read the text's first number (0 or 101 prizes) (refs: design 035, S279) → closed 2026-09-24 S286: prize-clause-only parser (slice 1)
- I132 2026-09-23 P2 [rules] Fossil Items could never bench their own card: look-at pick hardcoded 'Darkness Pokémon (bench)' (refs: design 035, S279) → closed 2026-09-24 S286: bench pick vocabulary (slice 2)
- I133 2026-09-23 P2 [rules] Conditional Tool modifiers (HP/retreat/damage/prevention/prize) over-applied (refs: design 035, S279) → closed 2026-09-24 S286: tool-conditions.mjs gating (slices 3-4)
- I134 2026-09-23 P2 [rules] 28 parsed Trainer step kinds had no server executor (refs: design 035, S279) → closed 2026-09-24 S286: handlers for the audit's list (slices 5-7); remaining kinds → I136
- I135 2026-09-23 P2 [rules] Passive Trainers did nothing: turn damage bonus wording, drawUntil targets, Tool on-damage/on-KO, triggered Stadiums, play conditions (refs: design 035, S279) → closed 2026-09-24 S286: slices 8-11; leftovers → I137 · S289 correction: the on-KO scope shipped wrong (attacker-side prize loop, damage-phase draws, double Energy move) → I138–I140, fixed in design 038 slice 1
