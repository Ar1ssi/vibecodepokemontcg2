# Journal 2026-09

S1 2026-09-07 bootstrap: adopted repository, initialized harness (CLAUDE.md, MAP.md, PROJECT.md, DECISIONS.md, ISSUES.md), verified 797 native tests pass.
  flag: pnpm lint fails on Windows CRLF line endings and no-undef global variables.

S2 2026-09-07 feature (phases 1-2 only, no code): wrote `.agent/designs/001-server-authoritative-netcode.md`
  — move simulation from two client mirrors to one server authority. 10 decisions, 20 edge cases,
  8 slices, passive shadow-mode rollout; expanded with glossary, hazards H1-H7, command-vocabulary
  dispositions, invariants, two worked examples, and per-slice procedures. Verified every citation
  against source; baseline 797 tests green. Also merged S2 findings into PROJECT.md/MAP.md/ISSUES.md
  (I6) on top of the S1 bootstrap. Design is DRAFT — approval gate not passed.
  flag: D3 claims rules tests use JSDOM; they run on plain stubs with no jsdom. PROJECT.md corrected,
        D3 left alone as a user-authored line — maintain.md should reconcile.
  flag: `chat-buttons.js` (4330 lines) and `rules-bridge.js` (2291) are the two monoliths that make
        the action layer unportable; they are the real cost centre of the migration.

S3 2026-09-07 feature (slice 1): created shared/engine/{rules,zones}, moved 32 modules + legacy-set-ids, rewrote client imports, guarded localStorage (H7), mounted /shared in server.js; 797 tests pass.
S4 2026-09-07 feature (slice 2): pure Card, GameState, mulberry32 PRNG, view redaction, state hashing, invariant guards in shared/engine/; 828 tests pass.
S5 2026-09-07 feature (slice 3): command schemas & disposition catalog in commands.mjs, pure 5-step applyCommand reducer in reduce.mjs, server GameRoom authority behind SERVER_AUTHORITATIVE; 850 tests pass.
S6 2026-09-07 feature (slice 4): passive shadow mode (ShadowSession), relayed RNG consumer (createRelayedRng), legacy action translation (translateLegacyAction), /debug/shadow-report route, mismatch logging to shadow-mismatches.log; 861 tests pass.
S7 2026-09-07 feature (slice 5): implemented turn loop, attack resolution, KO & prize handling, bench promotion, status checkup, and deterministic setupGame in shared/engine/; verified Edge Case 5 and scripted two-player game win condition; 876 tests pass.
S8 2026-09-07 feature (slice 6): PendingChoice protocol, resumable effect executors (shared/engine/effects/), Ultra Ball Worked Example B, Edge Cases 6, 10, 11, 12, 15; 895 tests pass.
S9 2026-09-07 feature (slice 7): client applyView renderer, cmd-emitter with in-flight affordances (.cmd-pending), dual-run bridge, resolveChoice socket handler, Edge Case 8 & Invariant 4/5 verified; 912 tests pass.
S10 2026-09-08 feature (slice 8): Phase 3 flip (SERVER_AUTHORITATIVE=true default), PROTOCOL_VERSION ('2.0.0') negotiation, Phase 4 deletion pass eliminating legacy reconciliation stack (catch-up-actions.js, resync-actions.js, sync-replay.mjs, request-board-snapshot.js, apply-board-snapshot.js, board-hash.js, rules-local-effects.mjs), Edge Cases 7, 14, 16, 17, 18 verified in slice8-edge-cases.test.mjs; 910 tests pass.
S11 2026-09-08 patch: resolved audit findings 1-3 (payload redaction leak in server.js, opponent choice deadlock in reduce.mjs, unique instanceId minting in shadow.mjs); 912 tests pass.
S12 2026-09-08 patch: resolved audit finding 4 (exchangeData/loadDeckData parameter offset in server.js, extractDeckData in shadow.mjs); 915 tests pass.
S13 2026-09-08 patch: resolved audit finding 5 (applyView choice resolver socket/room context options, setDefaultNetcodeContext fallback, async resolver); 917 tests pass.
S14 2026-09-08 patch: resolved audit finding 6 (clientSeq reset on reconnect, lastClientSeq in view broadcasts/requestView, seedClientSeq in cmd-emitter); 919 tests pass.
S15 2026-09-08 patch: resolved audit finding 7 (identity swap & seat hijacking on reconnect via state.players matching in joinGame/userReconnected, seat reservation in disconnectHandler); 927 tests pass.
S16 2026-09-08 patch: resolved audit finding 8 (parameter unpacking in dual-run-bridge.js for retreat, ability, damage counters, and special conditions); 944 tests pass.
S17 2026-09-08 patch: resolved audit finding 9 (bench knockout victim/attachment discard, no illegal auto-promote); 946 tests pass.
S18 2026-09-08 patch: resolved audit finding 10 (initiatorPlayerId attribution in PendingChoice resumeToken, trainer cleanup to initiator discard); 948 tests pass.
S19 2026-09-08 patch: resolved audit finding 11 (resolveChoice deduplication in GameRoom and socket handlers, active choice preservation); 952 tests pass.
S20 2026-09-08 patch: resolved audit finding 12 (gameEnded socket notification broadcast in server.js, getGameEndedPayload in GameRoom, reconcileGameEnded modal/event handling in apply-view.js); 954 tests pass.
S21 2026-09-08 patch: resolved audit finding 13 (Paralysis cured prematurely between turns; resolveCheckup now requires endingPlayerId match before clearing Paralyzed); 961 tests pass.
S22 2026-09-08 patch: resolved audit finding 14 (stadium overwrite & discard in trainer.mjs & reduce.mjs, duplicate name block); 969 tests pass.
S23 2026-09-08 patch: resolved audit finding 15 (bench limit in executor.mjs searchDeck, reduce.mjs playTrainer/stadium-effect validation); 976 tests pass.
S24 2026-09-08 patch: resolved audit finding 16 (Asleep attack legality check in reduce.mjs, eliminated infinite reroll exploit in attack reducer); 980 tests pass.
S25 2026-09-08 patch: implemented special rule for Grand Tree stadium activation in chat-buttons.js and stadium.mjs; 981 tests pass.
S26 2026-09-08 patch: card picker click-to-slot with left-to-right open slot priority (card-picker.js, card-picker-hitbox.mjs, index.css); 984 tests pass.
S27 2026-09-08 patch: card picker drag-vs-tap safety check via press duration and drift thresholds (card-picker.js, card-picker-hitbox.mjs); 986 tests pass.
S28 2026-09-08 patch: reworked pick card menu to only close via Done button; resolved merge conflicts with PR #84 (card-picker.js, card-picker-hitbox.mjs, discard-pile-viewer.js); 988 tests pass.
S29 2026-09-08 patch: parse Collect attack and singular draw ("draw a card"), draw card and end turn (damage-parser.mjs, attack-effects.mjs, chat-buttons.js, reduce.mjs); 990 tests pass.
S30 2026-09-08 patch: hide scrollbar on board zone across container stylesheets (self-containers.css, opp-containers.css, mat-layouts.test.mjs); 991 tests pass.
S31 2026-09-08 fix: resolved 2P multiplayer desync where decks and ready signals were dropped (removed serverAuthoritative early return in pushAction/requestAction; trimmed roomId/username in client/server; fixed peerSocketId handshake and immediate socket listener in rules-bridge); 991 tests green, two-player-sync-test green.






S32 2026-09-09 feature(design phase 1-2): verified an external Opus 5 netcode audit line-by-line against source.
  Corrected its root cause (applyView renders nothing in prod — zones are in iframes, window.__getZone unset), found 3 missed bugs
  (syncInstance != instanceId; reconnect recovery dead in both modes; stadium actively wiped), wrote .agent/designs/002-netcode-repair.md, filed I10-I14.
  flag: design 001 slice 8 recorded the authoritative flip as complete when three of its four preconditions were never met — treat slice-completion Deviations notes as claims, not evidence.
  User delegated the three open picks to the session: recorded as D8 (flag defaults off), D9 (bounded peer-log catch-up + loud fallback), D10 (server-issued syncInstance->instanceId map). Design status now approved; corrected its own 'not a git repository' constraint after the environment reported the repo.

S33 2026-09-09 feature (design 002 slices 0.1-0.2): built on new worktree `../vibecodepokemontcg2-netcode-repair`,
  branch `feature/netcode-repair`. 0.1: SERVER_AUTHORITATIVE now defaults off (server.js:21-23), render.yaml
  sets it explicitly, boot log states the active mode. 0.2: added resolveRenderTargets guard to apply-view.js —
  a blind renderer (no resolvable zone) now returns no_render_target without touching lastRenderedVersion or
  the DOM, closing the stadium-wipe repro from the design. Added non-injected §0.2 guard tests and a
  stadium-wipe repro test to apply-view.test.mjs; fixed dual-run-sync.test.mjs, whose applyView calls had no
  injected zone resolver and were unknowingly exercising the bug (applied:true, rendered nothing) — now
  injects minimal DOM stubs. 993 tests green; changed files lint clean. NEXTSTEPS.md ledger created.
  flag: design edge-case rows 1-2 ticked in 002-netcode-repair.md; Deviations section notes the
        dual-run-sync.test.mjs fix.

S34 2026-09-09 feature (design 002 slice 1.1): same worktree/branch. Replaced the dead
  `resyncActions` reconnect emit (N2 — no listener since slice 8) with a real requestPeerLog/
  peerLog pair: reconnecting client asks its peer for the selfActionData tail past its
  oppCounter, capped at 200 (PEER_LOG_MAX), replayed through the existing pushActionQueue
  chain; past-cap or no answer within 5s falls through to the O2-C "reload and rejoin"
  announcement. New DOM-free client/src/setup/netcode/peer-log-catchup.js (11 tests);
  pushAction handler refactored onto a shared applyPeerAction so live traffic and catch-up
  replay run identical code. server.js relays the two new events. 1004 tests green; changed
  files lint clean; test:2p green against a live server. Fixed a stale MAP.md section still
  listing slice-8-deleted reconciliation files as present.
  flag: design edge-case rows 6-7 ticked; row 7 (5s timeout) has no test harness for socket
        timers — verified by reading the guard, not by an automated test.
S35 2026-09-09 feature(netcode slice 1.2): deleted the moveCardBundle/isCounterOrStatusAction
  exemptions in socket-event-listeners.js's requestAction handler (was: apply anyway against
  the wrong counter). New DOM-free client/src/setup/netcode/request-action-queue.js buffers
  a gapped requestAction by counter and releases the contiguous run once expected catches up
  (5 tests); a gap open past STALE_ACTION_TIMEOUT_MS=2000ms clears the buffer and falls through
  to the 1.1 peer-log catch-up once, not per action. Updated a stale sync-action-args.test.mjs
  grep-assertion that expected the deleted exemption to still exist. Worked directly in the
  primary checkout per user instruction (no new worktree): branched briefly off main as
  feature/netcode-repair-1-2 (feature/netcode-repair was occupied by another worktree),
  merged back to main, deleted the temp branch. 1009 tests, test:2p, and changed-file lint
  all green.
  flag: design edge-case row 9 ("gap never closes -> catch-up triggered once") verified by
        reading the single-timer guard in armRequestActionStaleTimer, not by an automated
        test — same limitation noted for row 7 in S34, no test harness for socket timers yet.
  flag: ../vibecodepokemontcg2-netcode-repair worktree still exists on feature/netcode-repair
        and is now behind main by this slice — reconcile or remove before slice 1.3.

S36 2026-09-09 feature(netcode slice 1.3): deleted dead sync-check scaffolding — emitSyncCheck/
  triggerSyncCheck no-op stubs, their 30s heartbeat and two document-listener call sites, the
  dead syncCheckInterval/syncCheckDebounceTimer vars, and the server relay entries + SHADOW_MODE
  branch for syncCheck/resyncActions/catchUpActions/requestBoardSnapshot/applyBoardSnapshot —
  each grepped first and confirmed to have no live listener. Removed the stale, unused
  ../vibecodepokemontcg2-netcode-repair worktree (clean, behind main) per S35's flag. Worked
  directly in the primary checkout per user instruction (no new worktree): branched off main as
  feature/netcode-repair-1-3, merged back, deleted the temp branch. 1009 tests, test:2p, and
  changed-file lint all green.
  flag: design's dead-scaffolding list for requestSyncLogBundle/syncLogBundle was wrong — grep
        found live listeners in sync-logger-bridge.js backing the sync-log export tool; kept
        them and corrected the design's Deviations section rather than deleting a live feature.

S37 2026-09-09 feature(netcode slice 2.1): sweep grace + roomInfo decoupling (Finding 5).
  GameRoom (room.mjs) gained lastActivityAt, touched by the constructor, addPlayer, and
  handleCommand; server.js also touches it on pushAction ingest. cleanUpEmptyRooms' SERVER_
  AUTHORITATIVE branch now only deletes a GameRoom when its socket maps are empty AND it has
  been idle past new ROOM_GRACE_MS (30 min, env-overridable, 0 = old immediate-sweep behavior),
  and no longer deletes roomInfo from that branch — roomInfo keeps its own username-keyed
  lifecycle. 2 new unit tests in room.test.mjs (activity touch, grace-window sweep eligibility).
  Worked directly in the primary checkout per user instruction (no new worktree): branched off
  main as feature/netcode-repair-2.1, merged back, deleted the temp branch. 1013 tests,
  test:2p, and changed-file lint all green.
  flag: design edge-case row 5 ticked; cleanUpEmptyRooms itself lives inline in server.js
        (never exported/imported anywhere), so the sweep condition is verified via a
        standalone GameRoom-level test replicating that exact boolean, not a call into the
        real function — same class of gap as the socket-timer rows in S34/S35.
S38 2026-09-09 feature(netcode slice 2.2): room.mjs removeSocket clears clientSeqByPlayer
  (#12); cmd-emitter.js resolves PROTOCOL_VERSION via dual dynamic import and stamps the
  cmd envelope (#7/#8); joinGame mismatch now emits leaveRoom for server-side seat release
  (edge row 11); process-action.js routes emitCmd failures to appendMessage/logSync (edge
  row 12). 1014 tests & test:2p green. Merged feature/netcode-repair-2.2 to main.
  flag: rows 11/12 have no unit coverage — socket-event-listeners.js and process-action.js
        both pull in state.js, whose module scope calls browser-only io()/document APIs,
        so neither is importable outside a real browser; verified by reading the guard,
        same class as the socket-timer rows in S34/S35.
S39 2026-09-09 feature(design re-scope): design 002 Phase 3 re-scoped after Phases 0-2 shipped.
  Split into 3A server-correctness (3.1-3.5, no renderer, zero production exposure), an O4 end-state gate,
  3B renderer parity (3.6-3.10), 3C flip (3.11-3.12). Old slice 3.2 was not a slice: verified the
  authoritative renderer emits a listener-less <img> while legacy Card.buildImage attaches seven handlers,
  counters are sibling divs and Cover is a separate image. Added Options O4 (OPEN), edge rows 17-24,
  updated NEXTSTEPS ledger, filed I15. Docs only, no code touched.
  flag: O4 trades cheating-resistance (I5, only closed by full authoritative rendering) against ~5 slices
  of renderer work — a product call the user should make at the gate, not a technical one.

S40 2026-09-09 feature(netcode slices 3.1-3.3): instanceMap round-trip (server mints, emits
  syncInstance->instanceId per player; client resolves every hint through it, drops+logs on miss,
  deletes 3 positional-index guess fallbacks); DISPOSITION_TABLE exhaustiveness gate in
  translateActionToCmd's default case (throws on truly unknown action, warns on known-but-unbuilt
  server_command, silent for permanently relay-only dispositions); normalizeSpecialCondition
  returns null instead of mislabeling unrecognised input as 'Poisoned'. Files: dual-run-bridge.js,
  socket-event-listeners.js, server.js, commands.mjs (verified, no changes needed), plus tests.
  1031/1031 tests green. Built via 3 sequential subagents (same file, one writer at a time).
  UNCOMMITTED per explicit user instruction — no branch, no commit, no PR this session.

S41 2026-09-09 feature(netcode slice 3.4a): zone-op translations. 14 legacy actions have no
  cardHint (unlike moveCardBundle), so position-addressed ops (shuffleIntoDeck, moveToDeckTop,
  switchWithDeckTop) send { from: zoneId, index } and the server resolves against its own zone
  array — same pattern as the existing takePrizesByIndex. Whole-zone ops (shuffleZone,
  shuffleBottom, shuffleAll, discardAll, lostZoneAll, handAll, leaveAll, discardAndDraw,
  shuffleAndDraw, shuffleBottomAndDraw, shufflePrizesToDeckBottom) need no addressing; all
  shuffles use server activeRng, client shuffle indices are read and dropped. New COMMAND_SCHEMAS
  + applyCommand cases in commands.mjs/reduce.mjs, translateActionToCmd cases in
  dual-run-bridge.js, new server/game/__tests__/zone-op-commands.test.mjs (16 tests, added to
  pnpm test). Changed moveToDeckTop's DISPOSITION_TABLE commandType from moveCard to its own
  moveToDeckTop (documented as a deviation — 3.2's note assumed instanceId-addressing that
  doesn't exist here). 1061/1061 tests green, changed-file lint clean (pre-existing no-console
  warnings only). UNCOMMITTED per explicit user instruction — no branch, no commit, no PR.

S42 2026-09-09 feature(netcode slice 3.4b): prize & board translations. discardBoard/handBoard/
  shuffleBoard/lostZoneBoard target the literal 'board' zone (verified via getZone source, not
  a computed active+bench union as 3.2's notes claimed — corrected DISPOSITION_TABLE). New
  COMMAND_SCHEMAS + reduce.mjs cases for the 4 board ops; takePrizes/takePrizesByIndex already
  had schemas+reducer from the original scaffold, only needed a translateActionToCmd case each.
  22 new tests (8 translator unit, 6 GameRoom round-trip incl. takePrizes/takePrizesByIndex).
  1074/1074 tests green, changed-file lint clean. UNCOMMITTED per explicit user instruction —
  no branch, no commit, no PR.

S43 2026-09-09 feature(netcode slice 3.4c): setup & turn translations. Of the 9 legacy actions
  in this group only `takeTurn` needed a translator case — reducer case (shared with `pass`)
  already existed, only COMMAND_SCHEMAS entry + translateActionToCmd case were missing. The
  other 8 (setup/setupPrizes/drawOpeningHand/readyUp/reset/restartGame = server_lifecycle,
  changeCardBack/changePlaymat = client_local) were already correctly classified relay-only —
  confirmed with new disposition-gate tests, not changed. Updated the "no case yet" gate test
  from `takeTurn` (now cased) to `playRandomCardFaceDown`. New test file
  `server/game/__tests__/setup-turn-commands.test.mjs`, 5 new tests total. 1079/1079 tests green,
  changed-file lint clean (pre-existing no-console warnings only). UNCOMMITTED per explicit user
  instruction — no branch, no commit, no PR.

S44 2026-09-09 feature(netcode slice 3.4d): reveal/look family — all 8 actions (lookAtCards,
  stopLookingAtCards, revealCards, hideCards, revealShortcut, hideShortcut, lookShortcut,
  stopLookingShortcut) were already classified replaced_by_redaction/announcement_only in
  DISPOSITION_TABLE (slice 3.2), so the default relay-only branch already returns null for all
  of them — no translator case added, only disposition-gate tests confirming the behavior.
  1081/1081 tests green. UNCOMMITTED per standing user instruction — no branch, no commit, no PR.

S45 2026-09-09 debug/re-gate(netcode slice 3.4e): traced `undo` per D6 ("deterministic commandLog
  replay minus tail against seed") before building it, per the slice's own "confirm or STOP"
  instruction. Found the premise broken: deck load (`initializePlayerDeck`, shadow.mjs:29-70)
  mints instanceId and populates `zones.deck` directly on `gameRoom.state` from a raw pushAction
  in server.js:665-698 — entirely outside `applyCommand`/`commandLog` — and `setupGame` reads
  `zones.deck`, not `deckList`. Replaying `commandLog` alone from a fresh seeded state therefore
  reconstructs empty decks, not the pre-undo game. No code changed. Filed I16, re-gated 3.4e in
  NEXTSTEPS.md pending a user call: (A) fold deck-load into a proper logged command, (B) replay
  bootstrap that re-mints decks from `deckList` in player-registration order before replaying the
  log, or (C) snapshot-based undo instead of event-sourcing. No branch, no commit, no PR.

S46 2026-09-09 feature(netcode slice 3.4e): user picked option A. Added `loadDeck` command
  (shared/engine/commands.mjs schema + DISPOSITION_TABLE entry N/A — internal-only, not a legacy
  client action; reduce.mjs case mints instanceId via the same draft.nextInstanceId counter as
  every other card-creating path, sets zones.deck + deckList, and — critically — runs through
  applyCommand so it lands in commandLog) and legality carve-out (loadDeck only allowed during
  turn.phase 'setup'). server.js's exchangeData/loadDeckData interception now calls
  gameRoom.handleCommand('loadDeck', ...) instead of mutating gameRoom.state directly via
  initializePlayerDeck (import removed, now unused). Implemented `undo` in reduce.mjs: rebuilds
  a fresh createGameState (players scaffolded from username only — zones/decks come back through
  replayed loadDeck commands), replays commandLog.slice(0, -count) through applyCommand with a
  fresh seeded rng, returns the replayed state directly (bypasses the common commandLog-push tail
  so undo itself is never logged — no undo-of-undo bookkeeping needed). Edge Case 18 (undo past
  log start) rejected `nothing_to_undo`, state untouched. Added dual-run-bridge.js translator
  case (undo -> {type:'undo',payload:{}}, ignoring legacy filteredActionData). Updated the stale
  DISPOSITION_TABLE.undo assertion in commands.test.mjs (disposition retired from 'undo' to
  'server_command'). New test file server/game/__tests__/undo-command.test.mjs (6 tests: loadDeck
  logging/instanceId minting, loadDeck phase gate, undo single/double revert incl. commandLog
  length + rngCursor equality, undo past log start, undo count exceeding log). Registered the new
  file in package.json's test script (explicit file list, not a glob). 1088/1088 tests green.
  flag: an `eslint --fix` sweep across the 6 touched files (meant to clean up my own new lines)
  reformatted entire pre-existing file contents (2890 insertions/363 deletions across reduce.mjs,
  commands.mjs, dual-run-bridge.js, server.js, and their tests) — no functional change, tests
  stayed green throughout, user chose to keep it rather than hand-revert. Be aware the working
  tree's whitespace/wrapping style in these 6 files no longer matches the rest of the untouched
  codebase. Closed I16. NEXTSTEPS.md 3.4e marked done (uncommitted). UNCOMMITTED per standing
  user instruction — no branch, no commit, no PR.

S47 2026-09-09 feature/debug(netcode slice 3.5): built record-legacy-2p-fixture.mjs (real 2-browser
  Playwright recorder against a live server) + server/game/__tests__/replay-harness.test.mjs,
  replaying the recorded fixture through a fresh GameRoom via the real translateActionToCmd
  bridge. Found and fixed I18: nothing ever called the server-side 'setup' command, so hands/
  prizes were never dealt (server.js now calls handleCommand('setup',...) once both decks load).
  With I18 fixed, replay still fails 'stale_view' on the first moveCardBundle — filed as I17: the
  client's local legacy shuffle and the server's authoritative shuffle (setupGame's own activeRng)
  are independent RNG streams with no reconciliation. 1088/1088 existing tests still green
  (replay-harness.test.mjs intentionally not added to pnpm test — left red as evidence for I17).
  Asked user how to proceed on the I18 discovery mid-session; user chose "fix now, then build
  harness". Stopped after I17 surfaced per NEXTSTEPS.md's own exit-test instruction ("structural
  mismatch means 3A is not done"). UNCOMMITTED per standing user instruction.
  flag: I17 may mean the 3A/3B split (design 002 §3) isn't as clean as assumed — the client may
  need to see the server's dealt hand before any moveCard from hand can validate, which is 3B-ish
  work. Re-litigate before resuming 3.5.

S47+1 2026-09-09 fix(netcode I17): closed I17. First attempt (loosen moveCard's payload.from
  check in shared/engine/reduce.mjs) broke an existing named invariant test (Edge Case 3:
  stale_view) and didn't fully fix it anyway (card sets still diverged) — reverted clean,
  1088/1088 verified. Real fix: server is sole shuffle authority end to end — server.js emits a
  'dealOrder' event (per-player syncInstance order) once both decks are actually loaded, client's
  setupPrizes() waits for and uses it (client/src/setup/netcode/deal-order.js). Found a second bug
  while verifying manually in the browser: the 'setup' trigger's "both players present" guard
  fired on the FIRST loadDeck (both playerIds exist from addPlayer, before either has a deck),
  dealing one player an empty hand and sending them a 0-length dealOrder that then wiped their
  deck via rearrangeArray — fixed with an allDecksLoaded check. Verified via manual 2-tab browser
  session (javascript_tool) plus 3 fresh live-recorded fixtures through replay-harness.test.mjs,
  all green. Harness's hand-order mismatch (real cards, wrong order) traced to a pre-existing
  client-side hand auto-sort (move-card.js:604-607, cosmetic, already tracked as I15/design row
  10) — harness now compares zone membership order-insensitively (canonicalizeHash), not
  string-exact. replay-harness.test.mjs added to pnpm test's file list. 1089/1089 green.
  UNCOMMITTED per standing user instruction.

S48 2026-09-09 feature(netcode O4 gate + slice 3.6): user ruled O4-A (full authoritative
  rendering) at the gate with 3.5's evidence in hand (1089/1089, replay harness green). Recorded
  D11. Built slice 3.6: client/src/setup/image-logic/build-card-image.js (pure <img> factory, no
  game-state imports) used by both legacy Card.buildImage and apply-view.js's
  createOrUpdateCardElement, killing the two-subtly-different-factories mechanism behind N1.
  client/src/setup/image-logic/card-listener-table.js (CARD_IMAGE_LISTENERS) is the one
  interaction table both consume — apply-view.js can't statically import it (click-events.js/
  drag.js pull in state.js's browser-only module-scope io()/document calls, would break every
  test importing apply-view.js), so it reaches the renderer only via
  setDefaultNetcodeContext({ cardListeners }), wired for real in socket-event-listeners.js (never
  actually imported by any test — confirmed by grep). Real getZone wired the same way
  (setDefaultNetcodeContext({ getZone }), resolveZone adapts you/them -> self/opp); deleted the
  dead window.__getZone branch. Does NOT close functional zone-array parity (a drag actually
  completing a move) — the authoritative .card shim is pure view data, not a legacy Card
  instance, and get-zone.js's per-player zoneArrays still aren't populated from views; tracked
  open in design 002 edge row 20 for slices 3.7-3.10.
  1094/1094 pnpm test green (18/18 new+existing apply-view tests), pnpm test:2p green (flag off,
  confirms new production-wiring imports load in a real browser), lint clean. UNCOMMITTED per
  standing user instruction — no branch, no commit, no PR.

S49 2026-09-09 feature(netcode slice 3.7): counter/status overlay reconciliation. Added
  reconcileDamageOverlay/reconcileSpecialConditionOverlay to apply-view.js, called per-card
  after placeCardInZone: creates/positions/removes the damage-counter/status-marker sibling
  <div>s from cardData.damage/cardData.specialCondition on every applyView, reusing
  img.damageCounter/img.specialCondition as the storage slot (same field legacy
  addDamageCounter/addSpecialCondition use) and the existing shared style helpers
  (damage-counter-style.mjs, special-condition-style-apply.js) so tier/status classes stay
  identical to legacy. Display-only: no contentEditable/input/blur listeners — click-to-edit
  on an authoritative card still needs zoneArray population (same gap 3.6 flagged as open).
  Server stores conditions as full words (normalizeSpecialCondition); added a small word->code
  map at the render boundary since getSpecialConditionClass expects the short editable codes.
  Registry cleanup now also strips these overlays (zone-element siblings, not children of the
  removed image/container). Found+fixed a test-mock gap: apply-view.test.mjs's MockElement had
  no .style object and no .append() — added both, local to the test file. 3 new tests (Row 21).
  1097/1097 pnpm test green, pnpm test:2p green (flag off), lint clean. UNCOMMITTED per standing
  user instruction — no branch, no commit, no PR.

S50 2026-09-09 feature(netcode slice 3.8): covers, hand sort, #10 intra-zone order, #11
  attached-card class. New cover-listener-table.js (COVER_IMAGE_LISTENERS, extracted from
  Cover, mirroring card-listener-table.js); Cover refactored onto the shared buildCardImage
  factory. apply-view.js's reconcileZoneCover builds/updates/removes the deck/discard/
  lostZone elementCover top-card preview per side per view — deck is count-gated back-skin
  only since view.mjs never sends deck card data (secret by design, O4-A/I5). New
  resolveCardBackSrc picks the side's real chosen skin (cardBackSrc/p1OppCardBackSrc/
  p2OppCardBackSrc) instead of one hardcoded default, closing that gap for redacted-card
  backs too. #10/#11 both one-line fixes in placeCardInZone: unconditional appendChild
  reconciles order for free (moves an existing child to the end in real DOM and the test
  mock alike); the play-zone top-level branch now also strips 'attached-card' (previously
  only the leaves-play-zones branch did). New sortZoneCards render-order hook
  (hand-sort-context.js, reusing the existing sortCardsByDeckList) mirrors legacy's forced
  2P deck-list hand ordering — wired in socket-event-listeners.js, absent leaves order
  unchanged. 7 new tests (28/28 apply-view.test.mjs). 1104/1104 pnpm test green, pnpm
  test:2p green (flag off), lint clean. UNCOMMITTED per standing user instruction — no
  branch, no commit, no PR.

S51 2026-09-09 feature (netcode slice 3.9): reveal/look overlays — no production code needed.
  createOrUpdateCardElement's existing isRedacted check already renders card-back vs real art
  generically from whatever fields view.mjs sends, so a card flipping card.revealed between
  views already re-renders correctly. Added apply-view.test.mjs Row 23 test proving the
  redacted→revealed→redacted transition on one registry entry across 3 views.
  flag: I19 — nothing in shared/engine ever sets card.revealed=true, so reveal/hide is a real
  feature regression waiting at the 3.12 flip gate, not just dead code. 1105/1105 pnpm test,
  test:2p green (flag off), lint clean. UNCOMMITTED per standing user instruction.

S52 2026-09-09 feature (netcode slice 3.10): room-change reset + context re-seed.
  resetNetcodeForRoomChange() in socket-event-listeners.js (resetRenderState -> re-invoke
  setDefaultNetcodeContext via new seedNetcodeContext helper -> resetClientSeq(0)) closes the
  apply-view.js:67-71 trap where a bare reset nulls the netcode context. Wired into joinGame
  (new room) and both independent leave-room teardown paths: room-buttons.js's
  leaveRoomButton and a second, design-unnamed duplicate in header-buttons.js's p1Button
  click handler, found while tracing the first. 1105/1105 pnpm test, test:2p green (flag
  off), lint clean on all 3 touched files. No new unit test: all 3 files import state.js
  (browser-only io()/document module scope), same untestable class as rows 7/11/12;
  verified by reading the guard, underlying reset-then-reseed primitive already covered by
  apply-view.test.mjs's "Finding 5" test. UNCOMMITTED per standing user instruction.

S53 2026-09-09 feature (netcode slice 3.11): desync detection routed into the slice-1.1
  peer-log path. New `hashZoneMap`/`hashStateZones` (per-zone hash breakdown, shared/engine)
  + pure `findFirstDivergentZone` (server/game/sync-check.mjs) + client heartbeat
  (client/src/setup/netcode/sync-check.js, 30s, gated on serverAuthoritative/2P/not-spectator
  /connected) wired into socket-event-listeners.js; `desync` handler calls the existing
  requestPeerLogCatchup(), guarded against an in-flight catch-up (row 24). 1122/1122 pnpm
  test (17 new tests, 2 new files), test:2p green (flag off), lint clean on all 10
  touched/new files, manual alt-port boot with SERVER_AUTHORITATIVE=1 confirms clean wiring.
  UNCOMMITTED per standing user instruction.
S54 2026-09-09 feature (scoping only): scoped 3.12 flip gate, found precondition. Traced drag.js
  drop() -> moveCardBundle -> moveCard: legacy engine runs unconditionally, THEN processAction
  separately emits a server cmd when serverAuthoritative — dual-execution today, not hypothetical.
  Wrote .agent/designs/003-authoritative-interaction-routing.md, gated on user approval. Filed I20
  (type2/enteredPlayTurn have no server equivalent, out of scope, rules-bridge.js's own migration).
  No product code touched. NEXTSTEPS/STATE/ISSUES updated.
  flag: 3.12 cannot proceed meaningfully until design 003 lands (~6 slices, own session budget).

S55 2026-09-09 feature(003 slice 0): user delegated the 003 design gate; approved as drafted.
  Built client/src/setup/netcode/authoritative-dispatch.js — cardRegistry-sourced hint builders
  (fail closed) + emitAuthoritativeCommand (fails OPEN to the legacy body when translation is
  null, so no move is ever silently dropped; recorded D12). 20 new tests, 1142/1142 + test:2p
  green, lint clean. No production call site wired, per the slice's own scope.
  flag: package.json's test script is an explicit file list — a new test file is invisible to
  `pnpm test` until added there. Cost one silently-passing full-suite run to notice.

S56 2026-09-09 feature(003 slice 1): gated moveCardBundle on the authoritative dispatch path.
  New dispatchAuthoritativeMoveCardBundle + readCardInstanceId in authoritative-dispatch.js;
  mouseClick.cardInstanceId captured in identifyCard, drop() passes {moving,target} instanceIds;
  setAuthoritativeDispatchContext seeded in seedNetcodeContext. 30/30 dispatch tests green.
  flag: legacy indices are unusable under authoritative rendering (findZoneCardIndex reads the
  empty legacy zone array) — every remaining 003 slice needs its own DOM-side identity capture.
  flag: relayed opponent actions still dual-execute — filed I21, blocks 002's 3.12.


S57 2026-09-09 feature(003 slice 2): gated all 14 zone-op family legacy functions
  (shuffleIntoDeck, moveToDeckTop, switchWithDeckTop, shufflePrizesToDeckBottom, shuffleZone,
  shuffleAll, shuffleBottom, discardAll, lostZoneAll, handAll, leaveAll, discardAndDraw,
  shuffleAndDraw, shuffleBottomAndDraw). New generic dispatchAuthoritativeZoneOp — no card
  identity capture needed, family addresses by zone/position not instanceId. 24 new unit
  tests. NOT run against pnpm test (forbidden this session) — verify next session first.

S58 2026-09-09 feature(003 slice 3): gated prize & board family — takePrizes,
  takePrizesByIndex (prizes-actions.js), discardBoard, handBoard, shuffleBoard,
  lostZoneBoard (board-actions.js). Confirmed via dual-run-bridge.js none attach a
  cardHint; reused slice 2's dispatchAuthoritativeZoneOp unmodified, no new primitive.
  Board ops' `message` param is passed through but has no server effect (translator
  returns bare {} payload, matching 002's own 3.4b deviation note). node -c only on both
  files. NOT run against pnpm test (forbidden this session) — two slices (2, 3) now
  unverified against the suite, verify next session first.

S59 2026-09-09 feature(003 slice 4): gated `takeTurn` (take-turn.js) — only server_command
  action in 002 §3.4c's setup/turn family, confirmed rest already server_lifecycle/
  client_local. Translator case returns bare {} payload, no cardHint — reused slice 2's
  dispatchAuthoritativeZoneOp unmodified. node -c only on touched file. NOT run against
  pnpm test (forbidden this session) — three slices (2, 3, 4) now unverified against the
  suite, verify next session first.

S60 2026-09-09 feature(003 slice 5): gated attack/retreat/stadium-effect via new generic
  `dispatchAuthoritativeAction` (verbatim commandArgs; zone-op gate now delegates to it) and
  useAbility via `dispatchAuthoritativeUseAbility` + mouseClick.cardInstanceId capture.
  VSTARGXFunction left ungated (dead local path — I23; translator mismatch — I22). 17 new
  unit tests. node -c only; pnpm test forbidden — slices 2-5 now unverified against the suite.
  flag: attack's gated path leans on server reducer fidelity (status coins, KO narration,
  turn-end banner all skipped locally) — 002 slice 3.12 is the only check for that gap.

S61 2026-09-09 fix(003 slice 6): `undo`'s local replay found silently no-op'ing under
  serverAuthoritative (gated re-entry into acceptAction/processAction swallowed by the
  isUndoInProgress guard) — fixed by skipping the local replay outright, reusing
  isAuthoritativeDispatchActive. Design 003 slices 0-6 now all done. Key file:
  client/src/actions/general/undo.js. node -c only; pnpm test forbidden.
  flag: slices 2-6 all unverified against pnpm test since S56 — run it before I21/3.12.

S62 2026-09-09 verify: ran pnpm test/pnpm test:2p for the first time since S56 — design 003
  slices 2-6 all confirmed against the real suite (1171/1171, 0 fail; test:2p ALL PASS, flag
  off). No product code changed this session.

S63 2026-09-09 fix(003): closed I21 — accept-action.js now skips the legacy mirror body for a
  relayed opponent action (server_command/manual_override disposition) under serverAuthoritative
  via new isMirrorSuppressedAction (authoritative-dispatch.js). 4 new tests; 1175/1175 pnpm test,
  test:2p green, lint clean. Started 002 slice 3.12, found I24 (zoneArrays never populated from
  views — blocks 3.11's heartbeat and any getZone-based hash tooling); stopped rather than build
  a flip-gate test on a broken hash source. Key files: accept-action.js, authoritative-dispatch.js.

S64 2026-09-09 feature(002 I24): closed I24 — gave 3.11's desync heartbeat and the e2e API a
  live zone source (apply-view.js's new lastAppliedView cache) instead of legacy zoneArrays,
  which are never populated under authoritative dispatch. Also excluded deck from the 3.11
  comparison (owner-secret under O4-A/I5, unfixable by the zoneArrays fix alone). 1180/1180
  pnpm test (9 new tests), pnpm test:2p green, server boots with SERVER_AUTHORITATIVE=1.
  3.12 unblocked, not started. Key files: apply-view.js, sync-check.js, e2e-api.js,
  server/game/sync-check.mjs, server.js.

S65 2026-09-09 feature(002 3.12): built the flip gate (flip-gate-test.mjs, pnpm test:flip) —
  full 2-browser game, flag on, deterministic over 3 runs. Fixed two blockers found by it:
  `pass` was never gated (design 003 slice-5 miss, still dual-executing) and nothing applied
  view.turn, so rulesState.turnPlayer froze and the server rejected the next command as "It's
  not your turn." (new reconcileTurnState in apply-view.js). Gate is GREEN on every netcode
  criterion (cross-client board agreement after all 6 attacks, zero cmdRejected, agreed winner)
  but FAILS on server card fidelity: loadDeck carries no hp/attacks, so knockouts can never fire
  — filed I26, flag NOT flipped. 1183/1183 pnpm test, pnpm test:2p ALL PASS (flag off).

S66 2026-09-09 fix(002 I25): closed I25 — turn HUD banner and status badges went stale under
  the flag because reconcileTurnState synced rulesState but dispatched nothing, and the old
  refresh trigger (rules-turn-began) is reserved for legacy adjudication the server now owns.
  Added a display-only `rules-turn-view-applied` CustomEvent (apply-view.js), wired as an extra
  listener alongside rules-turn-began on the HUD's refresh() and status-badge updateBadges()
  in rules-bridge.js — checkDeckOut/checkKnockouts (adjudication) intentionally left off it.
  1193/1193 pnpm test (1 new test; also fixed 2 tests broken by the new dispatch call needing
  doc.dispatchEvent on mock documents that lacked it). Lint clean on touched files (repo-wide
  lint still fails on pre-existing CRLF/no-undef noise, untouched by this change). Key files:
  apply-view.js, rules-bridge.js, apply-view.test.mjs, dual-run-sync.test.mjs.
  Key files: flip-gate-test.mjs, apply-view.js, chat-buttons.js, e2e-api.js.
  flag: I25 (turn banner stale under the flag — rules-turn-began carries legacy adjudication,
  so it can't just be re-dispatched).

S65b 2026-09-09 feature(002 I26): closed I26 — new `cardStats` command carries hp/attacks/
  types/weakness/resistance/retreatCost/stage to the server, addressed by syncInstance across
  every zone and applied in place (safe mid-game, since TCGdex enrichment is async). Client
  sends its own deck's stats from build-deck.js once ensureCardData settles. Two bugs the gate
  caught while wiring: a rejected enrichment skipped the send entirely, and reading deck.array
  at send time saw only the 7 undealt cards (setup had already dealt 13 of 20). 3.12 flip gate
  now ALL PASS, 4 consecutive runs — KO at exactly 6 attacks, win reason "no Pokémon in play".
  1192/1192 pnpm test, pnpm test:2p ALL PASS flag-off. Flag NOT flipped (D8: user's call).
  Key files: commands.mjs, reduce.mjs, card-stats.js, build-deck.js.

S67 2026-09-09 fix(002 I27): closed I27 — client coin flip and server turn order were decided
  by two independent RNG streams and only agreed by chance. `server.js`'s post-setup `dealOrder`
  broadcast now also carries `starter` ('self'/'opp' per recipient); `deal-order.js` caches it
  (`getDealOrderStarter()`, available before the coin-flip UI runs); `rules-bridge.js`'s
  `runTurnOrderCoinFlip` uses it instead of its local guess, and the peer-mirror handler now
  trusts the sender's broadcast `turnPlayer` (inverted) instead of recomputing blindly. 1197/1197
  pnpm test (4 new tests). Flag not flipped (D8). Key files: server.js, deal-order.js,
  rules-bridge.js, socket-event-listeners.js.

S68 2026-09-10 fix(002 I19): closed I19 — reveal/hide had no server-side driver, so
  view.mjs's revealed-branch redaction was correct but unreachable. Reclassified all 4 actions
  'server_command' in DISPOSITION_TABLE; added COMMAND_SCHEMAS + reduce.mjs cases that flip
  `card.revealed` on the SENDER'S OWN zone only (position-addressed, no cardHint needed, same
  pattern as takePrizesByIndex — D16). Added dual-run-bridge.js translator cases and gated the
  4 functions in reveal-and-hide.js with `dispatchAuthoritativeAction`. Opponent-triggered
  reveal (legacy honor-system path) deliberately left ungated, unchanged from its pre-existing
  broken-under-authoritative-rendering state (D16). 1208/1208 pnpm test (16 new tests, incl. a
  viewFor cross-player redaction check and an own-zone-only security check). pnpm test:2p ALL
  PASS (flag off, live server). Lint clean. Key files: commands.mjs, reduce.mjs,
  dual-run-bridge.js, reveal-and-hide.js, zone-op-commands.test.mjs, dual-run-bridge.test.mjs.

S68b 2026-09-10 chore(002 D17): flag flipped. render.yaml SERVER_AUTHORITATIVE '0' -> '1' per
  user's explicit go-ahead. 3.12 exit test passing, I25/I27/I19 all closed. server.js's own
  default stays OFF (env-var only) — local/dev/tests unaffected unless the var is set.
S69 2026-09-10 feat(deck-builder): added Energy tab to native deck builder's Browse Sets panel.
  set-browser.mjs: ENERGY_SET_ID synthetic entry aggregates /cards?category=Energy across
  Standard-legal sets (D18) + 8 gold secret rare Basic {Type} Energy cards from rotated sets +
  8 modern basics hardcoded with pkmncards.com art (TCGdex has no image data for "mee"). Icon:
  user-supplied colorless-energy PNG at client/src/assets/energy/colorless.png. 7/7 set-browser
  tests pass; verified live via pnpm start + browser (39 cards, images load, add-to-deck works).

S70 2026-09-10 patch(holo): fixed dead Secret/Gold Rare holo effect. resolveHoloEffect() has
  mapped Secret/Gold/Shiny Rare to data-rarity="rare holo vmax" all along, but every CSS rule
  for that value only fired alongside [data-trainer-gallery="true"] (rainbow-alt.css), an
  attribute nothing in the codebase ever sets — these cards got zero holo overlay. New
  client/src/css/holo/gold-secret-rare.css targets [data-rarity="rare holo vmax"] directly.
  First attempt replaced the repeating-gradient sunpillar beams with a single non-repeating
  gradient, theorizing the repeat pattern aliased into noise at small size — wrong: user's
  video reference shows exactly the repeating-beam + glitter-sparkle look (marked 1/2/3 on
  three shifting color beams), and that same repeating-gradient technique is already used by
  regular-holo.css/hyper-rare.css elsewhere with no complaints, so the technique wasn't the
  issue. Reverted to mirroring hyper-rare.css's proven structure verbatim (sunpillar beams via
  ::before, exclusion diagonal-shift via ::after, --iri7/8/9 glitter-dust sparkle layer),
  retargeted to "rare holo vmax". Registered via @import in index.css after rainbow-alt.css.
  User verifies visually on localhost themselves going forward (feedback memory, project
  memory dir, not this repo).
  flag: not verified live this session — confirm the hyper-rare-mirrored version actually
  matches the reference video at real render sizes next session before considering this closed.
  If it's still called grainy, the earlier "bright background + color-dodge blows out" theory
  (from the first WRONG diagnosis) was never actually tested against this real structure —
  check that angle before reaching for a third rewrite.

  User then reported "just a circle going left and right instead of having the pillar" — real
  pre-existing repo bug, not a design issue: `--rotate-delta` is referenced in
  calc((var(--rotate-x) - var(--rotate-delta)) * ...) by hyper-rare.css, regular-holo.css,
  ex-special-illustration-rare.css, and the new gold-secret-rare.css, but holo.mjs never sets
  it and no CSS file defined it — an undefined custom property with no fallback invalidates
  the whole calc(), silently dropping every beam/pillar layer that depends on it. Only
  card__glare (driven by --pointer-x/y, which IS set) rendered — the "circle." Fixed with one
  line in base.css's shared :root/.card var block: `--rotate-delta: 0deg;` (no lag tracked, so
  0 is correct — not a workaround). This also silently repairs the same dead-pillar bug on
  Holo Rare / Hyper Rare / Special Illustration Rare cards elsewhere in the app, not just the
  new secret-rare CSS. NOT verified live — user checks localhost.

  User then reported "no pillars still, and the grain is too strong". This time debugged with
  real DOM inspection (dynamic-imported holo.mjs in-page, built a controlled test card with a
  synthetic solid-color canvas image since live TCGdex/pkmncards URLs 404'd in the sandboxed
  browser) instead of guessing again. Found: (1) a hard-reload was needed for --rotate-delta to
  actually take effect (confirmed via fetch of the served base.css — fine, just a stale-page
  false alarm); (2) a SECOND repo-wide undefined-var bug, same species as --rotate-delta:
  `var(--card-opacity)` is used inside calc() by base.css (with a `, 1` fallback — fine) but
  WITHOUT a fallback by every rarity CSS file (hyper-rare.css, regular-holo.css, rainbow-alt.css,
  ex-full-art.css, illustration-rare.css, ex-regular.css, reverse-holo.css, gold-secret-rare.css)
  — invalid calc() pins those opacities to their initial value (1, i.e. permanent max intensity)
  instead of the intended pointer-driven fade. Fixed with `--card-opacity: 1;` in base.css
  (matches the value the old pre-port index.css.bak used). (3) Opened iri-9.webp directly:
  it's dense fine-grain TV-static noise, not discrete sparkle stars — tiled at hyper-rare.css's
  150px with contrast(2)/plus-lighter that reads as solid grain, not "sparkles" (checked the raw
  asset, not assumed). Retuned gold-secret-rare.css only (did not touch the other rarity files
  sharing the same asset/technique, out of scope): --glitter-size 150px->480px (thins the same
  asset's visible density without replacing it), glitter filter contrast 2->1.1, glitter:before/
  after opacity formulas each *.5, shine:before (the beam layer) bumped filter contrast/saturate
  and switched overlay->color-dodge for a more distinct band edge. Verified the retune in the
  same synthetic-card DOM test: visibly a real shifting teal/white band, no longer solid static.
  Real card art will read differently (more hue variation than a flat test color) — user
  verifies the actual look on localhost, this was sandboxed verification only, not a final call.

  User: "bands aren't vertical". Rotated shine:before to 90deg (repeating-linear-gradient
  angle: bands run perpendicular to the gradient direction, so 90deg = left-to-right color
  change = vertical bands) and added a 90deg base offset to shine:after's two angles (they'd
  been left at ~0deg/rotate-x-only, i.e. horizontal, and that layer sits on top at z-index 3 —
  masking the vertical ::before layer underneath). Self-verified in a synthetic-card sandbox
  test, judged it fixed — wrongly: the browser's <link> stylesheet was cached from before this
  edit (a real page reload is required for CSS file changes to take effect; dynamic-importing
  the .mjs with a cache-busting query only refreshes the JS module, not the already-loaded
  CSS), so both my "fixed" read and the user's follow-up check likely ran stale CSS. User
  supplied a direct annotated screenshot proving the bands were still horizontal — caught my
  own misread this time instead of asserting it was fixed. Confirmed via `fetch` of the served
  file that the 90deg edit was written to disk correctly (it was); the actual gap was the
  never-reloaded page. Re-verified for real after a forced navigate+reload, and separately
  confirmed a flat-color synthetic test card is unreliable for this kind of check (color-dodge
  blending against an unnaturally saturated flat color can wash a layer out entirely, giving
  false negatives) — switched to real card art: the exact pkmncards.com Basic Water Energy
  asset already used by set-browser.mjs, then to the actual reference card (PAL 279/193 Secret
  Rare) via images.pokemontcg.io once the user supplied it, confirming the real card's own
  printed art already carries the sparkle-star look — the CSS layer only needs to add the
  moving sheen on top, not draw sparkles from scratch. Bands read correctly oriented against
  real art in the sandbox now. NOT a substitute for the user's own check — pointer-tracked
  motion doesn't show in a static screenshot the way it does hovering live in the app.
  flag: going forward, ANY CSS-file edit requires a forced page reload (not just a JS module
  cache-bust) before testing again in this browser sandbox — cost real time twice in this
  session (base.css --rotate-delta fix once, gold-secret-rare.css 90deg fix once) by skipping
  it. Also: a flat solid-color synthetic test card is unreliable for holo/blend-mode
  verification — use real card art (a working local asset or a confirmed-loadable remote URL)
  instead.

  User: still just a horizontal line after that fix. Re-read the file: card__shine:after (the
  black/gray "exclusion cross-hatch" secondary layer, copied verbatim from hyper-rare.css) was
  angled to 90deg+rotate-x last round but its background-position was left driving off
  --background-y — a vertically-banded pattern (90deg) doesn't visibly move when you shift its
  position along Y (the axis it's uniform along), so that layer rendered as a static artifact,
  and sitting on top at z-index 3 it was masking/dominating whatever card__shine:before (the
  actual vertical sunpillar layer) was doing underneath. Rather than keep patching two
  interacting layers, deleted card__shine:after outright and kept a single card__shine:before
  vertical-bar layer, position now driven by --background-x (the axis it actually varies
  along, so motion is visible) and background-size narrowed from 240% to 33% width so ~3 full
  color cycles are visible across the card at once (240%/70% both rendered as one broad soft
  wash edge-to-edge, not the several distinct bands the reference shows). Verified against the
  real PAL 279/193 card art (images.pokemontcg.io) after a confirmed hard-reload (checked the
  served file's bytes before testing this time) — screenshots at two different pointer
  positions show a real teal/cyan vertical band pattern that visibly shifted between them, not
  a static horizontal artifact. Cleaned up all test DOM nodes each round.

S71 2026-09-10 feature(rules-ui): evolve/devolve/switch/heal/attach/damage-target pickers in
  client/src/setup/rules/trainer-execution.js (11 call sites) now use new openMatPick() —
  click the real card image on the mat, no modal — instead of openPickOnly/openCardPicker,
  for every step whose candidate is an in-play Pokémon (D19). Hand/deck/discard pickers
  untouched. 1208/1208 tests green (no DOM harness to unit-test this file; not browser-verified
  this session — see STATE Next).

S72 2026-09-10 debug(rules-ui): found + fixed why Grand Tree evolved the Stage 1 into its own
  bench slot and skipped the Stage 2 picker — chat-buttons.js's executeGrandTreeSpecialRule
  picker mixed openCardPicker's two mutually-exclusive move patterns (auto-move via
  zoneFrom/destination + a manual moveCardBundle in onPick, without pickOnly:true), so the
  auto-move ran first and the manual evolve-move then found a stale deck index and bailed
  before reaching the Stage 2 branch (D20). Same bug found + fixed in the generic
  stadium-evolve-search function Grand Tree was split from (2 more sites, 4 total).
  1208/1208 tests green. Not yet browser-verified — user checks localhost.

S73 2026-09-10 fix(bench,retreat): dragging active Pokémon onto bench triggered a raw
  moveCardBundle 'move' (moveCard.js autoMoveActiveBenchCard) — no retreat cost, no energy
  discard, no paralysis/asleep/stadium gating. `retreat()` in chat-buttons.js (the button
  path) already had all that logic but always auto-picked the first free bench Pokémon.
  Gave it an optional `targetBenchImage` param (resolves to server `benchInstanceId` for the
  authoritative dispatch — the server command and dual-run-bridge.js translator already
  supported an explicit id, just never called with one — and to the matching zone-array entry
  for the legacy swap). drag.js's `drop()` now branches before the generic move logic: active
  origin + bench destination + not an attached card calls `retreat(mouseClick.cardUser, true,
  event.target)` instead of moveCardBundle. 1208/1208 pnpm test green (server-side retreat-
  with-benchInstanceId already covered by commands.test.mjs/dual-run-bridge.test.mjs).
  Key files: chat-buttons.js, drag.js.
  flag: no automated drag/DOM test added for the client glue itself — chat-buttons.js and
  drag.js are both DOM-coupled with zero existing tests in this repo (same untestable class
  noted in S38/S52); user verifies live on localhost per standing convention.

S74 2026-09-10 fix: KO'd Pokémon's attached Energy wasn't auto-discarding, and manually
  discarding it threw a false "energy already attached this turn". Cause: relocateAttachedCards
  dumped detached cards into the generic `attachedCards` staging zone even when the host's real
  destination was discard/lostZone — energy got stranded there, and rules-bridge.js's
  checkEnergyAdds (the only place that ever sets flags[user].energyAttached) misread its arrival
  as a fresh attach. Fix: new pure resolveDetachedCardDestination() routes straight to
  discard/lostZone when that's the host's destination; attachedCards unchanged for hand/deck.
  Legacy client-side rules-mode only — server-authoritative handleKnockout (reduce.mjs) was
  already correct. 3 new tests, 1211/1211 pnpm test green. Key files:
  relocate-attached-cards.js, resolve-detached-card-destination.js (new),
  move-card-bundle.test.mjs.
  flag: the once-per-turn energy-attach flag is never set by a real attach (only by this
  misrouted-detach path) — noted in PROJECT.md landmines, not fixed here (out of scope).
S75 2026-09-10 feature(design): drafted .agent/designs/004-playwright-playtest-bot.md — 6-slice
  plan for a self-playing Playwright soak bot over the ?e2e=1 window.__ptcg bridge. Draft only,
  unapproved, no code. Key finding: headless applyCommand games are cheap but do NOT cover the
  legacy client rules path (attack-text clauses are browser-only; executor.mjs covers ~23 of 40+
  parsed trainer effect types).
  flag: unimplemented card effects silently no-op in shared/engine/effects/executor.mjs — any
  headless harness needs a coverage gate or it will report false green.
S76 2026-09-10 feature: design 004 approved by user via implement request; built slice 1
  (`observe()` on window.__ptcg, client/src/setup/general/e2e-api.js) — plain-JSON read model
  reusing card-state.mjs and energy-effects.mjs's resolveAttachedEnergyType for attached-energy
  typing. No DOM nodes/live card refs in the returned shape. Ran shared/engine/cards + card-state
  unit tests (13/13 pass); e2e-api.js has no unit harness (DOM/socket-bound) — cross-page
  acceptance from the design still needs a live two-page check (S77 or user).
  flag: slice 1's acceptance criterion (cross-page public-field parity mid-game) is unverified —
  no automated test exercises observe() yet.
S77 2026-09-10 feature: design 004 slice 2 — legal-move enumeration. New pure, Node-importable
  client/src/setup/general/e2e-options.mjs (enumerateOptions) delegating to canPerformAction /
  canEvolve / canPayAttackCost / canRetreat / filterUsableAbilities; wired as `__ptcg.options()`
  in e2e-api.js. 11 new tests in __tests__/e2e-options.test.mjs, all green.
  Fixed in passing: observe() counted attached Energy as benched Pokémon and read attachments off
  a never-set card.attachedCards — both render paths keep attachments flat in the zone array
  (attachedTo / image.relative), so e2e-api.js now has boardPokemon()/attachedCardsFor().
S78 2026-09-10 feature: design 004 slice 3 — `__ptcg.act(option)` write path added to e2e-api.js;
  reused existing moveCardBundle/useAbility/attack/retreat/pass actions, no gameplay files touched.
  flag: no live two-page run has exercised observe/options/act together yet.
S79 2026-09-10 feature: design 004 slice 4 — `__ptcg.picker()`/`pick()`. Risk gate fired: found a
  third overlay kind (openMatPick's DOM-outline mat targeting) beyond card-picker/coin overlays.
  Bridged it by reading the outline `openMatPick` already sets and clicking the DOM node, rather
  than exporting state from trainer-execution.js (a rules file, out of scope). Added
  getCardPickerSnapshot()/pickCardPickerIndices() to card-picker.js. No gameplay file touched.
  flag: still no live two-page run has exercised observe/options/act/picker together (open S76).
S80 2026-09-10 debug: ran the live two-page smoke run design 004 has needed since S76
  (`.agent/scratch/smoke-004-slice1-4.mjs`, legacy mode). observe()/options()/act()/picker()
  all confirmed correct through a real turn 1 (7 playBasic-family actions + retreat + pass,
  zero cmdRejected, picker() never false-positived). Found I28: the shared rules engine's
  local win check ends every legacy 2P game after player A's first pass, crediting a win over
  B for "no Pokémon in play" even though B hasn't had a turn yet — out of design 004's scope
  to fix (no gameplay-file changes there), filed for its own session.
  flag: I28 blocks any further live verification of multi-turn play (attack/evolve/energy) —
  fix it before trusting slice 5/6 against anything but turn-1 behavior.
S81 2026-09-10 patch: fixed I28 — gated the "no Pokémon in play" instant-loss check
  (chat-buttons.js `evaluateWinCondition`, rules-bridge.js `evaluateAndApplyWinConditions`)
  behind `rulesState.turnNumber >= 3` so it no longer fires against a side that simply hasn't
  had its own first turn yet. `pnpm test` 1220/1223 green (3 pre-existing network-test
  failures, unrelated — this sandbox blocks api.tcgdex.net). Live-verified with
  `.agent/scratch/smoke-004-slice1-4.mjs`: a fresh legacy 2P game now plays past turn 1, both
  sides place Pokémon and attack.
  flag: found I29 while re-running the smoke script — legacy 2P turn state can desync a few
  turns into a real game (both clients believe it's the other's turn), wedging it. Filed, not
  investigated; likely the same family as I24.
S81 2026-09-10 (continued): checked whether I29 needs the real mat Pass button — re-ran with
  `#passButton` clicked via Playwright instead of `__ptcg.act({kind:'pass'})`
  (`.agent/scratch/smoke-i29-realpass.mjs`). Same desync, same point. Not pass-specific: both
  runs break right after two attacks in a row, with the receiving side's `attack()` mirror
  never flipping its local turnPlayer back. Narrows I29 to the attack path, not pass — updated
  the ISSUES.md line accordingly, still not fixed.
S82 2026-09-10 debug: closed I29 as wontfix (harness bug, not engine). Instrumented
  attack()/endTurnWithBanner (chat-buttons.js) with temp console.log, ran
  `.agent/scratch/smoke-i29-realpass.mjs` live against localhost:4000 — found its own
  stall-detection re-poll always checked `a.page` regardless of which side was actually active,
  so it gave up after 3s watching the wrong (already-settled) page while B's mirrored attack was
  still completing over the socket round trip. Fixed the harness (poll `active.page`, 6s window);
  removed all engine instrumentation before close. Re-ran twice: a fresh legacy 2P game now plays
  15+ alternating attacks with zero cmdRejected through to a real deck-out win, both times.
  `pnpm test` still 1220/1223 (same 3 pre-existing network failures). No gameplay file changed —
  diff is `.agent/scratch/smoke-i29-realpass.mjs` only.
  flag: design 004 slice 5 (bot/bot.mjs) is now fully unblocked — no known live-game desync
  remains between it and a real multi-turn smoke run.
S83 2026-09-10 feature: shipped design 004 slice 5 — `bot/bot.mjs` (never-crash scaffold +
  `legalFallback`) and `bot/heuristic-scorer.mjs` (greedy priority scorer with the two guards).
  14 new unit tests over recorded observe()+options() fixtures, wired into `pnpm test`. No
  gameplay file touched. Next: slice 6 (`playtest-bot.mjs` runner) is unblocked.
S84 2026-09-10 feature: shipped design 004 slice 6 (final slice) — `playtest-bot.mjs` runner +
  `loadDeckList()` (e2e-api.js). Live-verified against a real server: fixed two runner-side bugs
  found along the way (legacy-mode turn-order race, a divergence-check race echoing S82's I29),
  then the bot immediately found a real pre-existing bug — legacy `retreat()` sends no target
  identity to the peer, so any retreat to a non-first bench slot desyncs both clients. Filed I30
  rather than fixed (chat-buttons.js is a gameplay file, out of design 004's scope). Design 004
  is now fully shipped; "50 games zero failures" stays blocked on I30, which is expected — that's
  the tool doing its job.
S85 2026-09-10 patch: closed I30 — legacy `retreat()` now sends the resolved bench index (not
  a DOM image) through `processAction`, via a new `parseRetreatArgs` (sync-action-args.mjs)
  mirroring the existing `parseAttackArgs` pattern. Regression tests added
  (sync-action-args.test.mjs). Live-verified: seed 10 (previously 1/5 pass) now 5/5, plus a
  fresh seed 20 at 6/6 — playtest-bot.mjs's slice 6 acceptance ("zero failures") now holds.
MERGE NOTE (S88): the two entries below ran as concurrent sessions on branches that had not
  met, and both numbered themselves S86/S87. Both are kept verbatim rather than renumbered —
  the journal is append-only history. The sweep's issue was renumbered I33 -> I35 in
  ISSUES.md; the pass-wedge I33 is the one the pushed commit messages reference.
S86 2026-09-10 maintain: sweep (overdue since S80). Archived design 004 to designs/archive/
  (shipped). Trimmed STATE.md to cap (was 46 lines). Fixed stale MAP.md line counts
  (chat-buttons.js/rules-bridge.js/trainer-execution.js) and test count (797 -> 1223). Fixed
  DECISIONS.md D3 (jsdom claim, already flagged wrong since S2) and dropped PROJECT.md's now-
  redundant pointer to that fix. Deleted 2 scratch files superseded by playtest-bot.mjs
  (smoke-004-slice1-4.mjs, smoke-i29-realpass.mjs). Filed I33 (executor.mjs's ~23/40+ trainer-
  effect coverage gap, flagged since S75, never tracked — renumbered from I31 on merge with a
  concurrent session that used I31/I32 for real bot findings, see S87 below). ISSUES.md Open:
  29 lines (cap 40). metrics: ~67469 loc (client+server+shared+bot, excl tests) · 8 deps ·
  1223 tests.
S86 2026-09-10 patch: design 004 bot — added the missing playTrainer tier (the spec omitted
  it; 264/291 actions were pass on a real 60-card deck), an inert-Trainer per-turn guard, a
  card-enrichment wait, and observations in every failure dump. +6 tests, 1242/1245 green,
  fixture 3/3 live. Filed I31: real decks now hit a legacy 2P divergence after a Trainer play.
  flag: the fixture deck exercises ~1/3 of the option vocabulary — treat any "all green" from
  it as covering Basics only until a Trainer/Energy fixture deck exists.
S86b 2026-09-10 feature(bot): added coverage-scorer.mjs (--scorer=coverage) — ranks by least-
  exercised mechanic instead of plausible play, so Stadiums/abilities get taken as soon as they
  are legal; per-run coverage histogram. +8 tests. Found I32 on its first run (retreat from a high
  bench index still breaks the peer after I30, 5/5 across two seeds). Coverage scorer RED on the
  fixture deck until I32 is fixed; heuristic still 3/3.
S87 2026-09-10 patch: I32 half-fixed — retreat on a FULL bench now does ONE targeted move
  instead of two untargeted ones, so move-card.js:279 bench gate no longer rejects the first leg
  and strands two Pokemon in active. Local board verified correct; the PEER mirror still diverges
  (I32 updated with the remaining half + hypothesis). heuristic 3/3, pnpm test 1250/1253.
  flag: subagent run abandoned mid-investigation; the I31 lead (board staging zone + discardBoard
  on attack) is recorded in I31 and was never followed up.
S87 2026-09-10 debug: I32 CLOSED (both halves). Local half: retreat did two untargeted moves,
  so a full bench made move-card.js:279 reject the first leg and strand two Pokemon in active.
  Mirror half: the peer re-ran canPerformAction on the replay path and silently returned when its
  per-turn flags disagreed, so it never applied the retreat at all. Fix: one targeted swap +
  isMirrorReplayCall (sync-action-args.mjs). coverage seed42 0/3 -> 3/3, retreats 3 -> 11 per game.
  Filed I33: pass() reports ok when the gate blocked it, wedging games at turn 10-14.
  flag: __ptcg.act() maps undefined to ok:true, so ANY client action that returns early on a
  blocked path looks successful to the bot — not just pass. Worth auditing act() as a whole.
S87b 2026-09-10 debug: I31 CLOSED — same root cause as I32's mirror half, in attack(). The
  peer re-adjudicated legality on the attack replay and returned early, skipping discardBoard(),
  so Trainers stranded in the peer's `board` zone forever. Same isMirrorReplayCall guard.
  Real-deck seed 7: divergence gone, 27 -> 507 actions, 16 -> 25 distinct mechanics.
  flag: THREE actions now shown to re-adjudicate replays (retreat, attack). Audit every
  acceptAction target in chat-buttons.js for the same canPerformAction-on-mirror pattern —
  pass() has one too and is the prime suspect for I33.
S87c 2026-09-10 debug: I33 CLOSED — three HARNESS defects, no engine bug: act() read a
  client action' undefined return as success (refusals are only ever a chat ⛔ line); the runner
  asked only client A whether the game had ended; and gameEndedInfo missed clients that reach
  phase:ended without the event. Also fixed dump chat capture reading #chatbox in a 2P game.
  coverage 9/10 on a 10-game soak, heuristic 5/5, pnpm test 1254/1257.
  Filed I34: both clients report turnPlayer:opp simultaneously with turnNumber drifted 13 vs 8 —
  a real divergence, same signature I29 was closed under as a harness race. That closure looks
  premature.
S88 2026-09-10 consolidate: merged every outstanding branch onto main — PR 94 (design 004 bot,
  all 6 slices + S86 maintenance sweep), the fix chain (I32 retreat / I31 attack replay / I33
  harness reporting), and the pass-button consolidation. Resolved a .gitignore conflict by
  keeping both rules, and two collisions from concurrent sessions: both claimed session number
  S87 (now S88) and both filed an issue as I33 (the sweep's renumbered to I35). Journal entries
  from both kept verbatim. pnpm test 1254/1257.
  flag: I24 appears in BOTH the Open and Closed sections of ISSUES.md — pre-existing on both
  sides of the merge, not introduced here. Needs a decision on which is right.
