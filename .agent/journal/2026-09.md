# Journal — 2026-09

S1 2026-09-07 feature (phases 1-2 only): drafted `.agent/designs/001-server-authoritative-netcode.md`
  — move simulation from two client mirrors to one server authority; picks: full server authority
  (no prediction, turn-based), `shared/` served at `/shared`, snapshot-per-update + advisory events,
  server-side PendingChoice, server-minted instanceId. Verified baseline: 797 tests pass.
  flag: harness never bootstrapped — PROJECT.md/MAP.md empty; design constraints came from reading
        code, not from PROJECT.md. Bootstrap owed before slice 1.
  flag: `chat-buttons.js` (4330 lines) and `rules-bridge.js` (2291) are the two monoliths that make
        the action layer unportable; they are the real cost centre of the migration.
