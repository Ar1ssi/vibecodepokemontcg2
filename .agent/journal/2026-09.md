# Journal 2026-09

S1 2026-09-07 bootstrap: adopted repository, initialized harness (CLAUDE.md, MAP.md, PROJECT.md, DECISIONS.md, ISSUES.md), verified 797 native tests pass.
  flag: pnpm lint fails on Windows CRLF line endings and no-undef global variables.

S2 2026-09-07 feature (phases 1-2 only, no code): wrote `.agent/designs/001-server-authoritative-netcode.md`
  — move simulation from two client mirrors to one server authority. 10 decisions, 20 edge cases,
  8 slices, passive shadow-mode rollout; expanded with glossary, hazards H1-H7, command-vocabulary
  dispositions, invariants, two worked examples, and per-slice procedures. Verified every citation
  against source; baseline 797 tests green. Also merged S2 findings into PROJECT.md/MAP.md/ISSUES.md
  (I6) on top of the S1 bootstrap. Design is DRAFT — approval gate not passed.
  flag: D3 claims rules tests use JSDOM; they run on plain stubs with no jsdom. PROJECT.md corrected,
        D3 left alone as a user-authored line — maintain.md should reconcile.
  flag: `chat-buttons.js` (4330 lines) and `rules-bridge.js` (2291) are the two monoliths that make
        the action layer unportable; they are the real cost centre of the migration.

S3 2026-09-07 feature (slice 1): created shared/engine/{rules,zones}, moved 32 modules + legacy-set-ids, rewrote client imports, guarded localStorage (H7), mounted /shared in server.js; 797 tests pass.
S4 2026-09-07 feature (slice 2): pure Card, GameState, mulberry32 PRNG, view redaction, state hashing, invariant guards in shared/engine/; 828 tests pass.
S5 2026-09-07 feature (slice 3): command schemas & disposition catalog in commands.mjs, pure 5-step applyCommand reducer in reduce.mjs, server GameRoom authority behind SERVER_AUTHORITATIVE; 850 tests pass.
S6 2026-09-07 feature (slice 4): passive shadow mode (ShadowSession), relayed RNG consumer (createRelayedRng), legacy action translation (translateLegacyAction), /debug/shadow-report route, mismatch logging to shadow-mismatches.log; 861 tests pass.
S7 2026-09-07 feature (slice 5): implemented turn loop, attack resolution, KO & prize handling, bench promotion, status checkup, and deterministic setupGame in shared/engine/; verified Edge Case 5 and scripted two-player game win condition; 876 tests pass.
S8 2026-09-07 feature (slice 6): PendingChoice protocol, resumable effect executors (shared/engine/effects/), Ultra Ball Worked Example B, Edge Cases 6, 10, 11, 12, 15; 895 tests pass.
S9 2026-09-07 feature (slice 7): client applyView renderer, cmd-emitter with in-flight affordances (.cmd-pending), dual-run bridge, resolveChoice socket handler, Edge Case 8 & Invariant 4/5 verified; 912 tests pass.
S10 2026-09-08 feature (slice 8): Phase 3 flip (SERVER_AUTHORITATIVE=true default), PROTOCOL_VERSION ('2.0.0') negotiation, Phase 4 deletion pass eliminating legacy reconciliation stack (catch-up-actions.js, resync-actions.js, sync-replay.mjs, request-board-snapshot.js, apply-board-snapshot.js, board-hash.js, rules-local-effects.mjs), Edge Cases 7, 14, 16, 17, 18 verified in slice8-edge-cases.test.mjs; 910 tests pass.
S11 2026-09-08 patch: resolved audit findings 1-3 (payload redaction leak in server.js, opponent choice deadlock in reduce.mjs, unique instanceId minting in shadow.mjs); 912 tests pass.
S12 2026-09-08 patch: resolved audit finding 4 (exchangeData/loadDeckData parameter offset in server.js, extractDeckData in shadow.mjs); 915 tests pass.
S13 2026-09-08 patch: resolved audit finding 5 (applyView choice resolver socket/room context options, setDefaultNetcodeContext fallback, async resolver); 917 tests pass.
S14 2026-09-08 patch: resolved audit finding 6 (clientSeq reset on reconnect, lastClientSeq in view broadcasts/requestView, seedClientSeq in cmd-emitter); 919 tests pass.
S15 2026-09-08 patch: resolved audit finding 7 (identity swap & seat hijacking on reconnect via state.players matching in joinGame/userReconnected, seat reservation in disconnectHandler); 927 tests pass.
S16 2026-09-08 patch: resolved audit finding 8 (parameter unpacking in dual-run-bridge.js for retreat, ability, damage counters, and special conditions); 944 tests pass.
S17 2026-09-08 patch: resolved audit finding 9 (bench knockout victim/attachment discard, no illegal auto-promote); 946 tests pass.
S18 2026-09-08 patch: resolved audit finding 10 (initiatorPlayerId attribution in PendingChoice resumeToken, trainer cleanup to initiator discard); 948 tests pass.
S19 2026-09-08 patch: resolved audit finding 11 (resolveChoice deduplication in GameRoom and socket handlers, active choice preservation); 952 tests pass.
S20 2026-09-08 patch: resolved audit finding 12 (gameEnded socket notification broadcast in server.js, getGameEndedPayload in GameRoom, reconcileGameEnded modal/event handling in apply-view.js); 954 tests pass.
S21 2026-09-08 patch: resolved audit finding 13 (Paralysis cured prematurely between turns; resolveCheckup now requires endingPlayerId match before clearing Paralyzed); 961 tests pass.
S22 2026-09-08 patch: resolved audit finding 14 (stadium overwrite & discard in trainer.mjs & reduce.mjs, duplicate name block); 969 tests pass.
S23 2026-09-08 patch: resolved audit finding 15 (bench limit in executor.mjs searchDeck, reduce.mjs playTrainer/stadium-effect validation); 976 tests pass.
S24 2026-09-08 patch: resolved audit finding 16 (Asleep attack legality check in reduce.mjs, eliminated infinite reroll exploit in attack reducer); 980 tests pass.
S25 2026-09-08 patch: implemented special rule for Grand Tree stadium activation in chat-buttons.js and stadium.mjs; 981 tests pass.
S26 2026-09-08 patch: card picker click-to-slot with left-to-right open slot priority (card-picker.js, card-picker-hitbox.mjs, index.css); 984 tests pass.
S27 2026-09-08 patch: card picker drag-vs-tap safety check via press duration and drift thresholds (card-picker.js, card-picker-hitbox.mjs); 986 tests pass.
S28 2026-09-08 patch: reworked pick card menu to only close via Done button; resolved merge conflicts with PR #84 (card-picker.js, card-picker-hitbox.mjs, discard-pile-viewer.js); 988 tests pass.
S29 2026-09-08 patch: parse Collect attack and singular draw ("draw a card"), draw card and end turn (damage-parser.mjs, attack-effects.mjs, chat-buttons.js, reduce.mjs); 990 tests pass.
S30 2026-09-08 patch: hide scrollbar on board zone across container stylesheets (self-containers.css, opp-containers.css, mat-layouts.test.mjs); 991 tests pass.
S31 2026-09-08 fix: resolved 2P multiplayer desync where decks and ready signals were dropped (removed serverAuthoritative early return in pushAction/requestAction; trimmed roomId/username in client/server; fixed peerSocketId handshake and immediate socket listener in rules-bridge); 991 tests green, two-player-sync-test green.






S32 2026-09-09 feature(design phase 1-2): verified an external Opus 5 netcode audit line-by-line against source.
  Corrected its root cause (applyView renders nothing in prod — zones are in iframes, window.__getZone unset), found 3 missed bugs
  (syncInstance != instanceId; reconnect recovery dead in both modes; stadium actively wiped), wrote .agent/designs/002-netcode-repair.md, filed I10-I14.
  flag: design 001 slice 8 recorded the authoritative flip as complete when three of its four preconditions were never met — treat slice-completion Deviations notes as claims, not evidence.
  User delegated the three open picks to the session: recorded as D8 (flag defaults off), D9 (bounded peer-log catch-up + loud fallback), D10 (server-issued syncInstance->instanceId map). Design status now approved; corrected its own 'not a git repository' constraint after the environment reported the repo.

S33 2026-09-09 feature (design 002 slices 0.1-0.2): built on new worktree `../vibecodepokemontcg2-netcode-repair`,
  branch `feature/netcode-repair`. 0.1: SERVER_AUTHORITATIVE now defaults off (server.js:21-23), render.yaml
  sets it explicitly, boot log states the active mode. 0.2: added resolveRenderTargets guard to apply-view.js —
  a blind renderer (no resolvable zone) now returns no_render_target without touching lastRenderedVersion or
  the DOM, closing the stadium-wipe repro from the design. Added non-injected §0.2 guard tests and a
  stadium-wipe repro test to apply-view.test.mjs; fixed dual-run-sync.test.mjs, whose applyView calls had no
  injected zone resolver and were unknowingly exercising the bug (applied:true, rendered nothing) — now
  injects minimal DOM stubs. 993 tests green; changed files lint clean. NEXTSTEPS.md ledger created.
  flag: design edge-case rows 1-2 ticked in 002-netcode-repair.md; Deviations section notes the
        dual-run-sync.test.mjs fix.

S34 2026-09-09 feature (design 002 slice 1.1): same worktree/branch. Replaced the dead
  `resyncActions` reconnect emit (N2 — no listener since slice 8) with a real requestPeerLog/
  peerLog pair: reconnecting client asks its peer for the selfActionData tail past its
  oppCounter, capped at 200 (PEER_LOG_MAX), replayed through the existing pushActionQueue
  chain; past-cap or no answer within 5s falls through to the O2-C "reload and rejoin"
  announcement. New DOM-free client/src/setup/netcode/peer-log-catchup.js (11 tests);
  pushAction handler refactored onto a shared applyPeerAction so live traffic and catch-up
  replay run identical code. server.js relays the two new events. 1004 tests green; changed
  files lint clean; test:2p green against a live server. Fixed a stale MAP.md section still
  listing slice-8-deleted reconciliation files as present.
  flag: design edge-case rows 6-7 ticked; row 7 (5s timeout) has no test harness for socket
        timers — verified by reading the guard, not by an automated test.
S35 2026-09-09 feature(netcode slice 1.2): deleted the moveCardBundle/isCounterOrStatusAction
  exemptions in socket-event-listeners.js's requestAction handler (was: apply anyway against
  the wrong counter). New DOM-free client/src/setup/netcode/request-action-queue.js buffers
  a gapped requestAction by counter and releases the contiguous run once expected catches up
  (5 tests); a gap open past STALE_ACTION_TIMEOUT_MS=2000ms clears the buffer and falls through
  to the 1.1 peer-log catch-up once, not per action. Updated a stale sync-action-args.test.mjs
  grep-assertion that expected the deleted exemption to still exist. Worked directly in the
  primary checkout per user instruction (no new worktree): branched briefly off main as
  feature/netcode-repair-1-2 (feature/netcode-repair was occupied by another worktree),
  merged back to main, deleted the temp branch. 1009 tests, test:2p, and changed-file lint
  all green.
  flag: design edge-case row 9 ("gap never closes -> catch-up triggered once") verified by
        reading the single-timer guard in armRequestActionStaleTimer, not by an automated
        test — same limitation noted for row 7 in S34, no test harness for socket timers yet.
  flag: ../vibecodepokemontcg2-netcode-repair worktree still exists on feature/netcode-repair
        and is now behind main by this slice — reconcile or remove before slice 1.3.

S36 2026-09-09 feature(netcode slice 1.3): deleted dead sync-check scaffolding — emitSyncCheck/
  triggerSyncCheck no-op stubs, their 30s heartbeat and two document-listener call sites, the
  dead syncCheckInterval/syncCheckDebounceTimer vars, and the server relay entries + SHADOW_MODE
  branch for syncCheck/resyncActions/catchUpActions/requestBoardSnapshot/applyBoardSnapshot —
  each grepped first and confirmed to have no live listener. Removed the stale, unused
  ../vibecodepokemontcg2-netcode-repair worktree (clean, behind main) per S35's flag. Worked
  directly in the primary checkout per user instruction (no new worktree): branched off main as
  feature/netcode-repair-1-3, merged back, deleted the temp branch. 1009 tests, test:2p, and
  changed-file lint all green.
  flag: design's dead-scaffolding list for requestSyncLogBundle/syncLogBundle was wrong — grep
        found live listeners in sync-logger-bridge.js backing the sync-log export tool; kept
        them and corrected the design's Deviations section rather than deleting a live feature.

S37 2026-09-09 feature(netcode slice 2.1): sweep grace + roomInfo decoupling (Finding 5).
  GameRoom (room.mjs) gained lastActivityAt, touched by the constructor, addPlayer, and
  handleCommand; server.js also touches it on pushAction ingest. cleanUpEmptyRooms' SERVER_
  AUTHORITATIVE branch now only deletes a GameRoom when its socket maps are empty AND it has
  been idle past new ROOM_GRACE_MS (30 min, env-overridable, 0 = old immediate-sweep behavior),
  and no longer deletes roomInfo from that branch — roomInfo keeps its own username-keyed
  lifecycle. 2 new unit tests in room.test.mjs (activity touch, grace-window sweep eligibility).
  Worked directly in the primary checkout per user instruction (no new worktree): branched off
  main as feature/netcode-repair-2.1, merged back, deleted the temp branch. 1013 tests,
  test:2p, and changed-file lint all green.
  flag: design edge-case row 5 ticked; cleanUpEmptyRooms itself lives inline in server.js
        (never exported/imported anywhere), so the sweep condition is verified via a
        standalone GameRoom-level test replicating that exact boolean, not a call into the
        real function — same class of gap as the socket-timer rows in S34/S35.
S38 2026-09-09 feature(netcode slice 2.2): room.mjs removeSocket clears clientSeqByPlayer
  (#12); cmd-emitter.js resolves PROTOCOL_VERSION via dual dynamic import and stamps the
  cmd envelope (#7/#8); joinGame mismatch now emits leaveRoom for server-side seat release
  (edge row 11); process-action.js routes emitCmd failures to appendMessage/logSync (edge
  row 12). 1014 tests & test:2p green. Merged feature/netcode-repair-2.2 to main.
  flag: rows 11/12 have no unit coverage — socket-event-listeners.js and process-action.js
        both pull in state.js, whose module scope calls browser-only io()/document APIs,
        so neither is importable outside a real browser; verified by reading the guard,
        same class as the socket-timer rows in S34/S35.
S39 2026-09-09 feature(design re-scope): design 002 Phase 3 re-scoped after Phases 0-2 shipped.
  Split into 3A server-correctness (3.1-3.5, no renderer, zero production exposure), an O4 end-state gate,
  3B renderer parity (3.6-3.10), 3C flip (3.11-3.12). Old slice 3.2 was not a slice: verified the
  authoritative renderer emits a listener-less <img> while legacy Card.buildImage attaches seven handlers,
  counters are sibling divs and Cover is a separate image. Added Options O4 (OPEN), edge rows 17-24,
  updated NEXTSTEPS ledger, filed I15. Docs only, no code touched.
  flag: O4 trades cheating-resistance (I5, only closed by full authoritative rendering) against ~5 slices
  of renderer work — a product call the user should make at the gate, not a technical one.
