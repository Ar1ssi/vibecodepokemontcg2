# Decisions — binding choices, one line each, newest first. Scan before designing anything;
# do not relitigate a decision without new information — build on it or surface the conflict.
# Cap 50 active lines; maintain.md moves superseded/expired ones to the Archive section.
# Format: `D<n> <YYYY-MM-DD> [scope] decision — why. (Supersedes D<m>.)`
- D90 2026-09-28 [rules] The ability parser populates the server executor's filter contract rather than the executor guessing it: `parseAbility`'s `discardCostAbility` now emits `energyOnly` (true only when the discard clause itself names Energy), `basicOnly`, and `energyTypes`, matching `discardCost` steps built by stadium-effects; `moveDamageAbility` joins `EXTRA_STEP_HANDLERS` (reusing `damageCounters`) so "place N damage counters on 1 of your opponent's Pokémon" raises a mat-pick PendingChoice. Chosen over inferring type filters in `executor.mjs` from `energyType`/`basic` because those fields scan the whole card text and misfire on non-Energy discard costs (63/259 corpus printings). (S229)
- D88 2026-09-20 [netcode] Active-KO promotion under server authority is settled at the command tail, not inline: `handleKnockout` marks the KO'd player `promotionPending`, and `settlePromotionChoices` (run just before `settlePrizeEntitlements`) auto-promotes a lone Benched Pokémon and raises a `source:'promote'` PendingChoice (`resumeToken.effectType:'promote'`, min=max=1) for 2+, which the existing mat-picker adapter renders (PR #178 parity). Chosen over the old inline auto-promote (silently took bench[0]) and over a stateless "empty Active ⇒ promote" scan (fired on unrelated test/edge states that merely leave the Active open). The tail deferral also keeps the promotion from being overwritten by an attack's own search/snipe PendingChoice. `promoteBenchToActive` is shared by the direct `promote` command and the resume path; prizes settle after the promotion resolves. (S226)
- D86 2026-09-20 [deck-builder] The coin-image downloader now LINKS automatically: it scrapes the Bulbapedia coin tables (plain `fetch`; Playwright navigation hung), and matches each coin to its scan by normalized `description` — exact then prefix — because `parseCoinTables` yields the same `Description:` text the catalog stores (737/737 historical matched, 720 unique 120px scans, 0 placeholders). Chosen over D85's manual manifest review once the match proved exact; `--no-link` keeps the manual path. (Supersedes D85's "never rewrites the catalog".) (S219, design 019)
- D85 2026-09-20 [deck-builder] Missing coin art is detected by path, not filesystem: `isPlaceholderCoin` = url contains `/coins/bulbapedia/`; the downloader writes real scans to `client/src/assets/coins/historical/`. Why: browsers can't stat files, and a mismatched scan is worse than a visible placeholder. (Superseded by D86 on auto-linking; the path heuristic stands.) (S219, design 019)
- D84 2026-09-20 [deck-builder] The coin catalog is normalized by a committed generator (`scripts/normalize-coin-catalog.mjs`), not hand edits or runtime fixups: drops byte-identical duplicates (942→939), cleans the scraper's `date\t` release pollution, reclassifies metal/cardboard materials, and makes every `id` unique by appending a compact release date then an occurrence index. The 202 Gen IX ids stay frozen because decks persist `coin.id`. Why: the S218 merge shipped 120 duplicate-id groups (302 entries) despite claiming dedup; `getCoinById`/selection key off `id`. (S219, design 019)
- D83 2026-09-20 [rules] Long-tail ability coverage completed (I65) at the parse/classify layer, not execution. `parseAbility` gains ~40 step types — locks (`playLockAbility`, `evolveLockAbility`, `retreatLockAbility`, `powerSuppressAbility`, `damageCounterLockAbility`), energy/damage moves (`energyOnKoAbility`, `moveOpponentEnergyAbility`, `energySwapAbility`; damage-to-self/opponent uses the existing move steps), self (`transformAbility`, `returnSelfToHandAbility`, `selfBenchPlacementAbility`, `selfAttachEnergyAbility`), deck (`deckPeekAbility`, `deckPlaceAbility`, `lostZoneFromDeckAbility`, `winGameAbility`, discard→top via `recursionAbility`), conditions (`recoverStatusAbility`, `transferStatusAbility`), misc (`coinFlipControlAbility`, `attackCostAbility`, `energyTypeChangeAbility`, `playExtraSupporterAbility`, `turnNotEndAbility`, `goFirstAbility`, `benchGuardAbility`, `attachRestrictionAbility`, `attachPermissionAbility`, `stadiumManipAbility`, `discardForDrawAbility`, `discardOpponentDeckAbility`, `searchDiscardAttachAbility`, `discardBenchAbility`, `resetInPlayAbility`, `useSupporterAbility`, `jokeAbility`). `classifyAbility` gains 18 families (copy-attack, energy-on-ko, status-recover, deck-peek, lost-zone, discard-cost, self-attach-energy, self-return, attach-restriction, attach-permission, energy-type, attack-cost, coin-control, extra-supporter, go-first, bench-guard, discard-bench, joke) and fixes existing predicates (`isEffectPrevent` plurals / 'has no abilities' / 'power stops working'; `isStatus` paralyzed; `isMoveDamage`/`isOpponentDisrupt`/`isRecursion` broadenings; empty text → `passive`). Ordering rule kept specific-before-broad, with `discard-cost` deliberately last (after `attach`) so "play from your hand … discard" triggers, cost-discount and damage-reduce keep their families; `attack-cost` sits after `cost-discount`; `status-recover` excludes heal/damage-counter wording. Continuous step types are in `PASSIVE_ABILITY_STEP_TYPES`; execution remains announce-only. Corpus: abilityGaps 199→0, unknown 62→0, unattributed 1→0. (S211)
- D82 2026-09-20 [rules] The last attack classifier one-offs are mapped rather than left `flat`: two narrow families `move-damage-counter` ("move N damage counters from … to …", word-boundary `\bmove` so "Remove N damage counters" stays `heal`) and `neutralize-opponent-energy` ("treat that card as a Special Energy card"), plus card-specific broadenings — `/when the song is finished/`→`conditional-damage` (Harmonize), named "…'s Retreat Cost is 0" / "… does N damage plus N more damage"→`next-turn-bonus`, and "if an attack does damage to … attacks your opponent's Active Pokémon for N damage"→`retaliate`. Chosen over a generic `novelty`/`unknown` bucket so each card keeps real semantics; the file already carries card-specific patterns (e.g. the Tauros and Beedrill ones). attackGaps 6→0. (S210)
- D81 2026-09-20 [rules] `classifyAttackEffect` grows six families for the corpus clusters — `first-turn-attack`, `effect-prevention`, `lost-zone`, `look-any-deck`, `devolve-self`, `supporter-effect` — and broadens existing families (self-damage, immunity, deferred-damage, multi-target, per-energy, move-energy, return-self, reveal-hand, next-turn-lock, next-turn-bonus, copy-attack, conditional-ko, recover-status, bench-damage, draw-attach, devolve-opponent, damage-prevention). Ordering rule kept: a compound card's *primary* mechanic wins (Fast Carrier's deck search beats the first-turn permission, so `first-turn-attack` is checked last, just before `flat`; a coin-flip lock stays `coin-flip`, not `multi-target`). The classifier stays announce-only (no execution). (S208)
- D80 2026-09-20 [rules] `parseAbility` gains distinct step types for the recurring corpus clusters rather than reusing neighbouring families: `typeChangeAbility`, `statusImmunityAbility`, `attackCopyAbility`, `extraAttackAbility`, `ignoreDefenderEffectsAbility`, `evolvePermissionAbility`, `whenDrawnAbility`, `handDeckSwapAbility`, `benchFromDiscardAbility`, `drawVariableAbility`, `returnSelfToDeckAbility`, `summonRestrictionAbility`. Continuous/triggered ones are added to `PASSIVE_ABILITY_STEP_TYPES` (skipped, never offered as usable); the activated-but-unexecuted ones stay announce-only, matching the existing `discardCostAbility`/`onPromotionAbility` convention. Chosen over collapsing them into `passiveAbility`/existing families so the audit metric and UI guidance keep the real semantics. (S208)
- D79 2026-09-20 [rules] The ability classifier gains a `type-change` family for own/in-play type changes ("is both {W} and {F} type", "type is the same as…", "provides N Energy of every type"); energy-provides wording ("…provide {E} Energy instead of their usual types") stays in `energy-multiplier`. Chosen over overloading `energy-multiplier` so a type change is not announced as "Energy counts as more". (S207)
- D78 2026-09-20 [rules] Stadium extra attacks (Shrine of Memories / Meteor Falls inheritance, Holon Lake / Rocket's Tricky Gym grants) share one merge order — printed, then inherited, then granted, de-duped by name — across the server (`attackViewFor` → `stadiumExtraAttacks`/`mergeAttacks`) and the client (`stadiumExtraAttacksFromZone` → `listAttacks`/`mergeAttacks`, and the inspector model), so an `attackIndex` picked in the DOM resolves to the same attack on the server; `stadiumExtraAttacksFromZone` normalizes both render paths (authoritative `attachedTo`, legacy `image.relative`). The three unlimited "as often as … likes" Stadiums (Ultimate Zone / Saffron City Gym / Celadon City Gym) are modeled as repeatable activations exempt from `stadiumUsedThisTurn` via `isRepeatableStadiumAction`. (S207)
- D77 2026-09-20 [board-ui] (was D61; renumbered on the PR #171/#172/#173 merge so once-per-game keeps D61.) Double-clicking the in-play Stadium opens the same card-inspector module as a board Pokémon, with a Use panel that runs the existing `stadiumEffect(user)` (the sidebox button's path). Usability — rules on, your turn, once-per-turn not used, once-per-turn condition met — is decided by a new pure `stadiumActivationStatus` in `stadium-effects.mjs`, and the model/renderer grew a `kind:'stadium'` branch; continuous/unknown Stadiums show the effect text with no click. Chosen over duplicating effect parsing/execution in the inspector, so the panel can never activate something the engine gates would reject. (design 018, S193)
- D60 2026-09-19 [netcode] The mat picker also serves MULTI-pick in-play-Pokémon choices: `mat-pick-request.mjs` returns a request for any `max` (not only `max === 1`) and `openMatPick` toggles cards + requires Confirm between `min..max`. Every effect that makes the player choose Pokémon now routes there under `SERVER_AUTHORITATIVE`: retreat with 2+ bench raises a `resumeToken.effectType:'retreat'` choice; Escape Rope parses to `switchOwn` then `switchOpponentOut` (each seat's own mat pick); attacks whose printed text chooses opponent Pokémon ("does N damage to M of your opponent's [Benched] Pokémon", "choose N … put M counters on each") raise an `effectType:'attack'` target choice, and "…in any way you like" places one counter per click until all land. Legacy `chat-buttons.js` retreat/snipe/counter clauses use `openMatPick` too. Supersedes D59's `max === 1` gate. Damage-to-all-Bench spread stays automatic. (design 017, S192)
- D59 2026-09-19 [netcode] The server-authoritative mat picker is a client-side routing decision, not an engine marker: `apply-view.js` sends a PendingChoice to the mat picker when every option resolves (via `cardRegistry`) to an in-play Pokémon root (`zone ∈ active|bench`, `card.attachedTo == null`, `.element` present) and the choice is single-pick (`max === 1`); otherwise it falls through to the card picker/modal. Chosen over a new `resumeToken.targetZone`/`ask({display})` engine field, which would touch every `ctx.ask` site and the serialized resume token for a purely presentational concern (D19 already scopes the mat picker to "the candidate IS an in-play Pokémon"). `openMatPick` was extracted from `trainer-execution.js` into `rules/mat-picker.js` so legacy and netcode share one implementation; `dismissMatPick()` gives silent teardown (choice cleared/passed/superseded) while a user decline still reports through `onCancel`. (design 016, S191)
- D57 2026-09-19 [rules-ui] The carousel never sets pointer capture over interactive slide content: `onPointerDown` skips `stage.setPointerCapture()` for targets matching `isSwipeBlockedTarget` (`card-picker.js`), which now includes the design-013 inspector chrome (`.ptcg-chrome`). While a pointer is captured the browser delivers the follow-up `click` to the capturing stage, not the hit-test target, so the inspector's delegated listener (a stage descendant) never fired — a silent dead click with no error, no log and no emit. Consequence for future work: any interactive content mounted on a carousel slide must be added to that selector, and carousel interaction must be verified with real input (`locator.click`) — `el.click()` dispatches no pointerdown and masks the bug. (S189)
- D56 2026-09-19 [board-ui] The holofoil drift ignores `prefers-reduced-motion` entirely — the amplitude is unconditional. Zeroing it is not a milder version of this effect, it is the absence of it: on a machine that reports `reduce` (Windows "Show animations" off) every card froze on the neutral pose and read as broken, not calm, and the foil is the product's look. There is deliberately no holo opt-out for motion-sensitive users; the preference still governs everything else. (user decision, S188)
- D55 2026-09-19 [board-ui] Holofoil drifts on EVERY surface while the cursor is not holding the card: interactive mode (previews, picker slides, full-view panels) now runs the same idle drift board/hand cards already had, suspended only while the cursor is over the card and the tilt spring is off-centre. Reverses 8f5e786's hover-only previews ("match reference hover 1:1"), which left any card the user never hovers holding the neutral pose forever — reported as the foil "pausing on its first frame" (user decision, S186).
- D53 2026-09-19 [rules-ui] Card inspection AND attack/ability selection both live on **double-click**, in one enlarged inspector; single-click returns to select-to-move. Supersedes 008's D1 (single-click = attack preview) and D6 (bench single-click = ability preview) — `click` and `dblclick` bind to the same nodes (`card-listener-table.js:18-19`) and a double-click fires two clicks first, so the two features popped, re-popped and tore down each other. It also repairs 008's R3, since D1 had made the player's own active unselectable for moves. Chrome mounts in `card-picker.js` via an optional `decorate` hook rather than 008's pop host, because only the carousel can carry attached-Energy slides — the user's chosen interaction (design 013, user decision S174).
- D51 2026-09-18 [netcode] The opening turn order is decided by a server-owned coin call, not by setupGame's RNG: GameRoom picks the caller from the room seed, emits `turnOrderCall`, takes only the caller's heads/tails CALL from the client, flips with its own rng and feeds the winner to `setup` as `firstPlayerId`. The handshake lives on GameRoom (never in GameState) so it stays out of hashState/commandLog/undo, mirroring `readyPlayerIds`. A caller who is silent for 15s or disconnects is called for by the server, so a dead seat cannot hang the match. (Supersedes I27's accepted cosmetic-only flip.) (design 013, user decision, S173)
- D52 2026-09-18 [netcode] The client's rules-mode opening sequence is guarded by an explicit `openingStarted` latch, never by `rulesState.phase === 'setup'`: under server authority the post-deal view copies the server's phase ('main') onto rulesState before `both-players-ready` fires, which silently killed the whole opening ceremony (coin call, startGame, beginTurn, opening draw, mulligan). The game starts when BOTH the server's flip animation and the local deal are done, whichever lands last. (design 013, S173)
- D50 2026-09-18 [rules] Grand Tree's search-evolve, including its chained Stage 2 step, is implemented in the SERVER engine, not by exempting the card from the `stadium-effect` authoritative gate — under `SERVER_AUTHORITATIVE` the client's `executeGrandTreeSpecialRule` (and every fix in it: D20, D26, D30) is unreachable, and keeping the peer's view correct means the evolve must be a server command like every other move. `searchEvolve` (Salvatore's handler) gained an opt-in `step.chainStage2` phase rather than a second handler, and `executeStadium` maps `opt.kind === 'search-evolve'` to it, replacing the by-name `searchDeck`-to-hand fallback that produced the reported "Stage 1 lands on the Bench, window closes" behavior. One `deckShuffled` per activation, at the end of the chain. (design 013, S173)
- D47 2026-09-18 [netcode] The manual board tools (damage counters, Special Conditions, rotation, card type, ability marker) are planned from the authoritative card registry at the UI entry (`manual-card-commands.mjs` + `manual-card-dispatch.js`) and always sent as the acting player, never routed through the card's owner via requestAction — the owner is off-turn, so rules mode rejected every tool aimed at the opponent's Pokémon, and the legacy index-addressed bodies cannot name a server-drawn card at all. (design 012, S171)
- D48 2026-09-18 [netcode] A card played face down lives on the board with `card.faceDown`, redacted for EVERYONE (owner included) until `revealShortcut` sets `revealed`; the flag is swept off any card that is not on a board zone at the end of every command, so no individual move path has to clear it. `playRandomCardFaceDown` picks the card with the state's seeded RNG server-side, so undo replay picks the same card. (design 012, S171)
- D49 2026-09-18 [netcode] "Look at the top/bottom N cards of a deck" is answered by a `peekDeck` socket request to the asking socket only — no state change, no commandLog entry — and picked cards leave the deck as ordinary `moveCard` commands; the deck is never sent in the view. A player may look at the opponent's deck only with rules off. Restart reuses the player-requested Reset (D35) instead of a new command. (design 012, user decision, S171)
- D44 2026-09-17 [harness] Plan-driven work runs through `.agent/workflows/exec-plan.md`: one section at a time, tests+lint green for that section before the next (verification is never offered as a question — see CLAUDE.md § Code standard), ONE commit for the whole plan, structural sections put to the user before editing, status written back into the plan file. QWEN.md is an untracked LF mirror of CLAUDE.md — edit the two together or the two runtimes silently get different rules. (S159, user decisions)
- D43 2026-09-17 [board-ui] Highlight palette contract (TCG Live parity plan): the board's self-ring stays BLUE and the far side stays RED — TCG Live's gold is deliberately NOT adopted for "your actionable card", because gold already means "this attack is payable" in the preview and on the sidebox, and blue/red is the only at-a-glance separator between the two mats. Ability affordance stays blue (same accent as the ability zone); "cannot act" is gray + a red ✕, never a hidden row. (S158, user decision)
- D42 2026-09-16 [board-ui] #bench uses a 5-column CSS grid (repeat(5, 1fr) with gap: var(--bench-gap) and justify-items: center) across both container stylesheets instead of flexbox center-clustering, so bench Pokémon align exactly into the 5 printed/SVG bench zone slots regardless of how many cards are benched.
- D36 2026-09-16 [playmats] Added 8 user-supplied Pokémon illustrations (Mega Gardevoir, Mega Lucario, Mega Charizard X, Mega Gengar, Mega Darkrai, Mega Zeraora, Reshiram, Zekrom) as selectable playmats in MATS_CATALOG with both one-player and two-player full-size variants. Images upscaled 2x with low-noise filtering and cropped to authentic one-sided playmat aspect ratio (1.9394:1) with the Pokémon centered.
- D35 2026-09-16 [netcode] Under SERVER_AUTHORITATIVE the opening deal runs only when both seated players pressed Set Up (pushAction 'readyUp' → GameRoom.markReady) with decks loaded, never on deck load alone; and a player's Reset (pushAction 'reset', clean=false) resets the whole server game for both seats (resetGame + 'gameReset' + fresh views). Why: dealing on load auto-started games on join, and the server owns turn/phase, so a one-sided reset cannot be represented. Deck-load resets (clean=true) never reset the server game.
- D34 2026-09-15 [ui] Holo foil is lit by a fixed virtual light: `--pointer-*`/`--background-*` are computed from the card's angle (`computeLightVars` in holo.mjs), never from the cursor; board/hand cards drift a virtual angle slowly on two axes (reduced motion → static) without visible tilt, and previews/picker tilt with the mouse. The card image doubles as a luminance mask (`--card-ink-mask`) so printed ink stays black — only for CORS-verified hosts, because a mask image that fails to load renders transparent and would hide the foil entirely. Why: user's TCG Live comparison, design 010. (Supersedes D31's cursor-driven glare spring; rotate spring physics unchanged.)
- D33 2026-09-15 [rules] Gen 6 Mega Evolution/Primal Reversion turn-end forces the turn to end by dispatching a `rules-mega-evolution-forces-turn-end` event that clicks the existing Pass button (`passButton`/`p2PassButton`), instead of calling `endTurn()` directly from move-card.js — reuses hookTurnButton's vetted pipeline (status boundary, end-of-turn abilities, banner, 2P sync) with zero duplicated netcode logic (same pattern already used by keybinds.js's Alt+T shortcut). Legacy-vs-modern Mega detection is by name prefix only ("M "/"Primal " = legacy vs full-word "Mega " = modern, per isModernMegaCard reusing ko-flow.mjs's isMegaCard logic) since no release-date field is available on client card data.
- D32 2026-09-14 [board-ui] Playmat artwork is aspect-ratio locked (~1.91:1 single-player, ~1.0:1 two-player sheet) and centered with an ambient blurred bleed backdrop; interactive card zones in playmat iframes scale horizontally relative to the centered mat width via CSS custom properties (--mat-aspect, --mat-width, --mat-offset-x), eliminating horizontal stretching distortion.
- D31 2026-09-13 [ui] Aligned holo spring physics and math 1:1 with simeydotme/pokemon-cards-151: 3 decoupled springs (springRotate, springGlare, springBackground), Euclidean distance for --pointer-from-center, glare2 DOM support, and stable card-level pointer tracking.
- D30 2026-09-11 [rules] Grand Tree's chained Stage 1→Stage 2 evolve (same activation) is a printed exception, confirmed against the real card text (TCGdex sv07-136 effect): "If that Pokémon was evolved in this way, [you] may search... evolve it [again]" — it explicitly bypasses both the just-played and already-evolved-this-turn gates for that one chained step, but NOT the Basic's own "put into play this turn" gate (separately reminder-texted on the same card and still enforced). Added `bypassJustEvolvedGate` as an explicit option threaded canEvolve -> moveCard -> moveCardBundle (mirrors the existing `isRareCandy` hint-passthrough pattern), rather than reusing the mirror-only `forceEvolution` flag (which would have also skipped stage-order/evolvesFrom validation). Local-only — never sent to a peer, since a relayed evolution already sets `forceEvolution` unconditionally. Corrects my own initial read of this as a bug (I38, closed — see S95 journal). (Supersedes the S95 "needs a product decision" framing — was actually a missing exemption, not a design ambiguity.)
- D29 2026-09-11 [testing] `stampE2eCard` (fixture-Basic HP/stage/attack stub) must only stamp cards from `e2eFixtureDeck()` (`set === 'e2e'`), not every card built while `?e2e=1` is active — why: `build-deck.js` was calling it unconditionally, so any real decklist loaded via `loadDeckList`/`loadDeckData` under e2e mode got every Pokémon's stage force-set to 'Basic' before the real TCGdex fetch ran, and `ensureCardData`'s enrichment guard (`card[k] == null`) then refused to overwrite that placeholder with the true Stage 1/Stage 2 value — silently breaking every stage-dependent effect (evolution search, Rare Candy, Grand Tree) for e2e-driven testing of a real deck. Found while live-verifying Grand Tree (S95).
- D28 2026-09-11 [netcode] Legacy search effects must finish (await) every card move before shuffling the deck, and a mirror that receives a permutation of the wrong length keeps every card (skips bad indices, appends unreferenced cards) and logs `shuffleZone.indices_mismatch` — never drops or leaves holes — why: relaying in local execution order is the only way permutation lengths match; the tolerance is a safety net, not a resync (legacy has none, I12). (design 007) (Renumbered from D26 on merge — collided with the concurrent debug-mode/Grand Tree D26/D27 below.)
- D27 2026-09-11 [testing] "Debug mode" for testers/bots to summon arbitrary cards and break all
  rules turns out to already be ~fully built, gated behind the existing `?e2e=1` bridge (Archive
  D21): `window.__ptcg.loadDeckList(rows)` already loads any card list of any size (no 20-card
  minimum exists client or server side to bypass), and `rules-state.mjs`'s `canPerformAction`
  already short-circuits to `{allowed:true}` for everything when `rulesState.enabled` is false.
  The one real gap: `forceRulesEnabledForMultiplayer` (rules-bridge.js) snaps rules back on at
  every 2P room join, which would fight a bot that disables them for a bot-vs-bot debug game —
  closed by exempting e2e mode via a new pure `multiplayerLocksRulesEnabled(isTwoPlayer, e2eMode)`
  helper (e2e-mode.mjs), applied at both the settings-checkbox guard and the force-on call. Added
  `window.__ptcg.debugMode(enabled)` as the discoverable one-call toggle (sets `rulesState.enabled`
  + persists) rather than requiring bots to poke the already-exposed `window.__ptcg.rulesState`
  object directly. No new server surface, no change to E2E_ENABLED's arming — same security
  posture as Archive D21 (tester-only, dev/PTCG_E2E-gated). (Renumbered from D26 on merge —
  collided with the concurrent Grand Tree D26 below.)
- D26 2026-09-10 [rules] Grand Tree's Stage-2-chain step re-resolves the Stage 1 host's live zone/index via `zoneOfInPlay(user, picked)` at pick time instead of reusing the pre-evolve `hostZone`/`hostIdx` closure vars — `evolveCard.js` keeps the base card in the zone array (marked `attached: true`) and inserts the evolution as a separate array entry, so a captured index/array-of-one snapshot from before the first evolve is not guaranteed to still describe where the Stage 1 card now lives. Both Grand Tree evolve-guard failure paths (deckIdx/hostIdx invalid) now `appendMessage` a reason instead of silently calling `finishGrandTree()` — the prior silent skip is indistinguishable from the user's report of a stage-2 card "disappearing". Root cause on the live PR96 (server-authoritative) repro is not 100% pinned from static reading alone; this is the concrete fragility found and the fix that removes it, plus gives a visible signal if it recurs. (Renumbered from D21 on merge — collided with the 3D-energy-tokens D21 from a concurrent session.)
- D25 2026-09-10 [deck-builder] Each generation pill also gets a synthetic Energy tab
  (`__energy_gen<N>__`), same colorless-energy logo and aggregation idea as the Standard view's
  `ENERGY_SET_ID` tab (D18), but without D18's `MODERN_BASIC_ENERGY_TYPES`/`EXTRA_ENERGY_CARD_REFS`
  extras — those exist solely to backfill sets that rotated OUT of `LEGAL_SET_REGISTRY`; a
  generation's own set list already includes every set it ever had (rotated or not), so the plain
  per-set Energy-category sweep already surfaces rarer variants (gold secrets, alt arts) with no
  hardcoded list needed. (design 006 — renumbered from 005, see that doc's header.)
- D24 2026-09-10 [deck-builder] Generation pills (design 006, originally drafted as 005 — renumbered
  on merge, see D22/D21 below which claimed 005 first) source their sets from TCGdex's own
  `series` grouping (`GET /v2/en/series/{id}` → `{sets: [...]}`), not a hand-maintained registry —
  verified live that TCGdex's series boundaries already match pkmncards.com/sets/'s era headers.
  This makes "ignore POP/Other/Misc" free (those are separate series, `pop`/`mc`/`misc`, never
  reachable from a generation pill) instead of needing per-set filtering. Generation→series map,
  user's explicit call: 9=sv (Mega Evolution's `me` deliberately excluded), 8=swsh, 7=sm, 6=xy,
  5=bw, 4=dp+pl (HeartGold&SoulSilver's `hgss` and Call of Legends' `col` deliberately excluded),
  3=ecard+ex, 2=neo, 1=base+gym.
- D22 2026-09-10 [board-ui] Energy-token front art is 11 user-provided PNGs (colored-circle/black-glyph symbols, cropped+alpha-masked from a reference sheet the user supplied) at `client/src/assets/energy/tokens/`, replacing the initial `151MT_*_Coin.jpg` photo-coin choice from D21 — user's explicit call after seeing both ("make it EXACTLY like the image"). `.energy-token-3d` had to be raised to `.play-container img.energy-token-3d` (specificity 0,0,2,1) because `.play-container img` (self/opp-containers.css:646-664) sets its own border-radius/box-shadow at the same specificity (0,0,1,1) but later in the cascade — confirmed by comparing computed styles in the live iframe before/after (design 005).
- D21 2026-09-10 [board-ui] Attached-Energy 3D tokens swap `movingCard.image.src` in place (stashed in `dataset.energyCardSrc`, restored by `resetImage`) rather than a separate overlay element — nothing in game logic reads `.image.src` for identity (checked zones/move-card/sync), only cosmetic zoom/hi-res code does, which degrades harmlessly to showing the token there too. Reuses the existing `151MT_*_Coin.jpg` per-type assets and `coin-back-tm.png` already shipped for burn/poison status tokens (design 005).
- D20 2026-09-10 [rules-ui] `openAbilityChoicePicker`/`openCardPicker` has two valid usage patterns: (a) no `pickOnly`, no manual move in `onPick` — rely on the picker's own `zoneFrom`→`destination` auto-move (card-picker.js:652-661); (b) `pickOnly: true` with a manual `moveCardBundle` in `onPick`. Mixing them (destination set, no `pickOnly`, but `onPick` also moves manually) double-moves the card — the auto-move fires first, so the manual move's own zone-index lookup comes up empty and the handler bails. Fixed 4 sites in chat-buttons.js that mixed the patterns (Grand Tree's two pickers, and the generic stadium-evolve-search's two pickers it was split from) by adding the missing `pickOnly: true`.
- D19 2026-09-10 [rules-ui] Trainer-effect pickers whose candidate IS an in-play Pokémon (evolveStage2, devolve, switchOpponent/Own/Out, heal target, damageCounters target, attach-energy target, discardToolAndSpecialEnergy, swapWithDiscard host) now resolve via `openMatPick()` in trainer-execution.js — a click-on-the-real-mat-image picker (outline highlight + capture-phase document click/Escape to gate normal drag/click handling) — instead of the modal `openCardPicker` carousel. Hand/deck/discard/attached-item candidates (the Stage-2-from-hand step, search/discard pickers) are unchanged — they aren't rendered on the mat. User's explicit scope call: convert every in-play-Pokémon picker, not just Rare Candy's.
- D18 2026-09-10 [deck-builder] Energy tab in Browse Sets is a synthetic entry (`ENERGY_SET_ID = '__energy__'`), not a real TCGdex set: `fetchLegalEnergyCards` cross-references the global `/cards?category=Energy` summary list (id/localId/name only, no image) against each Standard-legal set's already-cached full record (which has `image` but no category) to build normalized cards, since neither TCGdex endpoint alone carries both fields. Placed last via `releaseDate: ''` rather than resorted, matching the user's requested tab position. The modern basic-energy reprint (Mega Evolution Energy / "mee" set) has NO `image` field anywhere in TCGdex (set record, card detail, or CDN path all 404) — `getModernBasicEnergyCards()` hardcodes its 8 cards with art hotlinked from `pkmncards.com/wp-content/uploads/mee_en_{localId}_std.jpg` instead (user's explicit call after TCGdex was confirmed to have no usable asset; verified cross-origin loadable from the app's own origin). The 8 gold secret rare "Basic {Type} Energy" cards are pulled individually by exact id from their original (now non-Standard) sets via `EXTRA_ENERGY_CARD_REFS`, since a blanket per-set sweep would also pull unrelated rotated-out special energies from those same sets.
- D16 2026-09-10 [netcode] `revealCards`/`hideCards`/`revealShortcut`/`hideShortcut` (I19) only ever flip `card.revealed` on the sender's own zone — no cross-player targeting was added, unlike legacy's honor-system `mouseClick.cardUser === 'opp'` path (opponent revealing your cards). `applyCommand` already only ever touches `draft.players[playerId]` (the sender), so this is free by construction, not an extra check; the opponent-targeted case is left ungated and keeps its old (already broken under authoritative rendering) direct-socket-emit behavior rather than being wired to a command that would need a new permission model. Matches D14's "server is sole arbiter of what a client may know" posture — letting player A force-reveal player B's secret zone contents would be a real capability, not a display toggle, and I19 didn't ask for one.
- D15 2026-09-09 [netcode] Printed card data (hp, attacks, types, weakness, resistance, retreatCost, stage) reaches the server as its own `cardStats` command, not as part of `loadDeck`. Deck rows carry identity only and the client resolves the rest from TCGdex asynchronously, so it cannot ride along with the deck load; `cardStats` addresses cards by `syncInstance` across every zone and updates in place, never rebuilding one, which is what makes it safe to arrive mid-game. The client is trusted for these values for the same reason it is already trusted for the deck list itself — D10's identity rule is untouched, since the server still mints every instanceId (I26).
- D14 2026-09-09 [netcode] The server is the sole turn authority under the flag: `apply-view.js`'s `reconcileTurnState` writes `rulesState.turnPlayer/turnNumber/phase` from every applied view, and no client-side path advances the turn. It deliberately does not dispatch `rules-turn-began` — `rules-bridge.js` hangs legacy knockout/deck-out adjudication off that event, which the server now owns (banner staleness tracked as I25). Cross-client parity is asserted over **public zones only** (`active`/`bench`/`discard`/`lostZone`/`board`/`stadium`), with owner-secret zones compared by count: per-recipient redaction (O4-A/I5) makes hand/prizes/deck contents structurally incomparable between two clients, so hashing them across clients can only ever false-positive (design 002 3.12, S65).
- D13 2026-09-09 [netcode] Live client-side zone reads under server-authoritative rendering (the 3.11 desync heartbeat, e2e API) go through `apply-view.js`'s own last-applied-view cache, not legacy `zoneArrays` (never populated post design-003, I15) and not `cardRegistry` (insertion-ordered, not view-ordered — would false-positive on hash comparisons). `deck` is excluded from the 3.11 comparison specifically: it's redacted to `{count}` even for its owner (O4-A/I5), so it can never be hashed for real; `hashState`/`hashBoardSnapshot` keep full deck fidelity for replay-harness/dual-run use (I24).
- D11 2026-09-09 [netcode] O4 gate ruled A — full authoritative rendering. Client renders only from server views; legacy DOM mutation deleted once parity lands. Closes I6 (arbiter) and I5 (deck secrecy). Commits to full Phase 3B (renderer parity, ~5 slices) — user's explicit call at the O4 gate (design 002 O4).
- D12 2026-09-09 [netcode] A `server_command` action gates its own legacy local-mutation body on `systemState.serverAuthoritative` at its entry point and builds hints from `apply-view.js`'s `cardRegistry`, never from legacy `zoneArrays` (design 003, O1-B/O2-B). When the command cannot be translated the gate fails OPEN — the action falls through to its legacy body — so an untranslatable action degrades to today's dual-run instead of silently vanishing.
- D10 2026-09-09 [netcode] Card identity across the dual-run boundary is translated, never punned: the server keeps minting instanceId and returns a per-player syncInstance→instanceId map at deck init; an unresolvable hint drops the command rather than guessing. Client syncInstance is 0-based per player and is NOT the server's id (design 002 O3).
- D9 2026-09-09 [netcode] Reconnect recovery is a bounded peer action-log catch-up (cap 200, 5s timeout) with a mandatory loud "reload and rejoin" failure branch — no snapshot machinery. A recovery path without a failure branch is what grew into the stack slice 8 deleted (design 002 O2).
- D17 2026-09-10 [netcode] Flag flipped: `render.yaml` `SERVER_AUTHORITATIVE` set to `'1'`. All 3.12 exit-test preconditions were met (flip gate ALL PASS, I25/I27/I19 closed) and the user gave explicit go-ahead in chat. `server.js`'s own default (no env var set) stays OFF — only the prod deploy config changed, so local/dev runs and every existing test still exercise the flag-off legacy path unless `SERVER_AUTHORITATIVE=1` is set explicitly. (Supersedes D8's "not flipped yet".)
- D8 2026-09-09 [netcode] SERVER_AUTHORITATIVE defaults OFF and is set explicitly in render.yaml; the flip is a result gated on design 002 slice 3.6's two-browser exit test, never a default. Design 001 slice 8 flipped it with three of four preconditions unmet (design 002 O1). (Supersedes the slice-8 default in D6's rollout.)
- D6 2026-09-07 [netcode] Named-payload commands validate via 5-step pipeline in pure applyCommand reducer; undo deferred as deterministic commandLog replay (design 001).
- D5 2026-09-07 [netcode] Pure Card and GameState data models use instanceId and absolute player IDs with Mulberry32 PRNG (design 001).
- D4 2026-09-07 [netcode] Shared engine lives in shared/ served at /shared without bundler or new dependencies (design 001).
- D1 2026-09-07 [stack] Node.js ES modules with native node --test runner used across workspaces. (observed)
- D2 2026-09-07 [architecture] Express + Socket.IO server with SQLite3 for 2P real-time multiplayer state sync. (observed)
- D3 2026-09-07 [rules] Rules engine uses modular JS; pure card logic tests run on plain stub
  objects via native `node --test`, no jsdom (jsdom is a devDependency used only by
  `integration-test.mjs`) — corrected S2, see PROJECT.md. (observed)


## Archive (dead decisions — kept greppable, never loaded into working context)
- D21 2026-09-10 (S88): the `?e2e=1` test bridge is gated server-side. It exposes a scripting
  API over the local player's own board (act/attack/passTurn/loadDeckList), and the query flag
  is typeable by any visitor, so the client half must not be sufficient to arm it. Chose
  `PTCG_E2E=1 || NODE_ENV !== 'production'` over pure explicit opt-in so local dev and the
  Playwright harness keep working with no extra flags while the deploy is closed by default;
  the cost is that a deployment which forgets NODE_ENV=production would arm it, which the boot
  log now states explicitly. Not anti-cheat hardening (a non-goal): everything the bridge does
  is reachable from devtools anyway. This only stops it being five characters in the URL.

- D-holo-gen 2026-09-16: Holo effect picks from rarity text plus card name (rule-box holo rares become double rare) and data-card-era (art window, classic cosmos); shiny cards get a silver effect, not gold.
- D-holo-ref 2026-09-16: Holo patterns follow physical cards (Sleeve No Card Behind holo-pattern video) except where Pokémon TCG Live differs; TCG Live wins (user decision).
- D36 2026-09-16 [netcode] Under SERVER_AUTHORITATIVE 2P the legacy renderer never writes the DOM of a zone apply-view draws (hand/prizes/active/bench/discard/lostZone/board — `server-rendered-zones.mjs`); legacy move/sort/shuffle/reveal still update the zone arrays rules code reads, but their images stay detached. The deck and pile covers remain legacy-rendered because the server sends only a deck count (I48, S142; follows D11).
- D37 2026-09-16 [netcode] A rules-mode `moveCard` hand -> 'board' of an Item/Supporter with effect text is promoted to `playTrainer` inside `applyCommand`, from the server's own card data, instead of teaching the client to send `playTrainer`. The client has no reliable card type at the drop site, and a server-side decision means a client cannot place a Trainer while skipping its effect. Tools and Stadiums keep their moveCard/attachCard paths. Trainer effect text, trainerType and subtypes ride the existing `cardStats` command (S144).
- D38 2026-09-16 [rules] Card-printed Trainer play restrictions (turn-1 Supporter, same-name Stadium, discard cost, bench full, 'only if' prize conditions, not-first-turn) live in one module, rules/trainer-play-conditions.mjs, used by both server legality and the playtest bot's option list, so the bot never offers what the server rejects (S144).
- D39 2026-09-16 [engine] A step needing several choices keeps its progress in resumeToken.context keyed by `<stepIndex>:<stepType>`; resume passes context back. Yes/no questions use fixed negative pseudo-card ids (BARGAIN_YES/NO) because PendingChoice selection is validated against option instanceIds (S144).
- D40 2026-09-16 [rules] Evolutions stay attached under the Basic (no state migration). Rules that read what a Pokémon is (HP, attacks, Weakness, Retreat Cost, prizes) read `evolvedView(zone, root)`, a read-only copy with the top card's printed fields; damage and conditions stay on the root. The bot's authoritative observation uses the same view (S145).
- D42 2026-09-16 [board-ui] #bench uses a 5-column CSS grid (repeat(5, 1fr) with gap: var(--bench-gap) and justify-items: center) across both container stylesheets instead of flexbox center-clustering, so bench Pokémon align exactly into the 5 printed/SVG bench zone slots regardless of how many cards are benched.
- D41 2026-09-16 [rules] Rare Candy traces Stage 2 → Stage 1 → Basic through the player's own cards only, and allows the play when the line cannot be traced. The bot knows only its own deck list, so both sides reach the same answer (S145).
- D-mp-picker 2026-09-16 [netcode] Server pendingChoices with card art open card-picker.js (pickOnly; server moves cards), not a new selectable carousel; browser-only helpers (holo, picker) reach apply-view.js via setDefaultNetcodeContext injection so it stays node-testable. Grid modal is the fallback for art-less options (user decision).
- D43 2026-09-17 [rules] Prize cards are a server-granted entitlement (`player.flags.prizesOwed`, added by `handleKnockout`) and the only authority to move one; `takePrizes` redeems it, never a client-chosen count. The reducer collects the balance itself when the granting command ends (`collectPrizeEntitlement`, also called pre-reset in `advanceTurn`), because no runtime actor sends a redemption: the client's picker is dead under SERVER_AUTHORITATIVE and the bot never emits prize commands, so a deferred award would strand prizes and soft-lock. Settled keys are deleted so the state stays byte-identical to pre-change (S166, audit B-1).
- D44 2026-09-17 [netcode] Socket-originated commands (`cmd`, `resolveChoice`) must carry an integer clientSeq >= 0 and enter through `GameRoom.handleClientCommand`; server-internal calls (setup, loadDeck, shadow) keep using `handleCommand` without one. A replayed seq still answers success+dedupe with the current view — idempotent retry, never re-executed (S168, audit A-9).
- D45 2026-09-17 [rules] Server cards hold the rotation condition (Asleep/Confused/Paralyzed) in `specialCondition` and Poison/Burn as `poisoned: true` / `burned: true` keys present only while set; all reads/writes go through `rules/special-conditions.mjs`. Chosen over a `specialConditions[]` array (user, S169): smaller blast radius and unmarked states stay byte-identical. Client draws one marker per condition in `img.specialCondition` / `poisonMarker` / `burnMarker` (design 011, audit A-1).
- D46 2026-09-17 [rules] Supersedes D43's self-collect: a Knockout's prize entitlement is settled by a server `pendingChoice` (`resumeToken.effectType: 'prizes'`, options = prize instanceIds only, min=max=owed) that pauses the game until the owed player picks (user choice: pause, like TCG Live). `settlePrizeEntitlements` runs at command end; a second owed player gets the next choice; the game-over and all-remaining-prizes cases still collect directly. `advanceTurn` carries `prizesOwed` across its flags reset. Client shows it as the legacy fly-up prize fan via an injected `prizePicker` (S170, closes I4).
- D54 2026-09-19 [rules] Ability activation is position-aware in the ENGINE, not just the UI: `requiresActiveSpot(card)` (`rules/ability-executors.mjs`) matches only the restrictive conditional ("if this Pokémon is in the Active Spot" / "if this Pokémon is active"), and `listAbilities(card, { …, zone })` reports `usable: false` with a reason when the card is off-spot; `validateLegality`'s `useAbility` case refuses the same dispatch with a player-readable reason, beside the existing once-per-turn refusal. Chosen over matching a bare mention of the Active Spot, which would also match the on-move trigger ("when this Pokémon moves from your Bench to the Active Spot", legal from the Bench) and the "As long as this Pokémon is in the Active Spot" passive — a false positive silently breaks a legal ability, which costs more than the over-permissive behaviour it replaces. `zone` defaults to `'active'` so no existing caller changes behaviour (user choice: engine + server + panel; S182, design 015).
- D76 2026-09-19 [rules] One card-classification contract lives in `shared/engine/rules/card-classify.mjs` (`isRuleBoxPokemon`, `prizesForKO`, and the ex/GX/V/VMAX/VSTAR/Tera/Mega/Tag-Team/V-Union/Prism/Radiant/ACE-SPEC/LEGEND/Basic-Energy predicates). The old `prizesForKO > 1` rule-box heuristic and the `stadium-effects` subtype list are deleted; `ko-flow`, `tool-combat`, `search-match`, `evolution` and `reduce` all delegate. **Superseded in part by D89** (Mega prize count). VMAX/TAG TEAM/V-UNION are 3-prize cards (rulebook 30c Phase 0, S193). (renumbered from a `D-cardclassify` placeholder on the PR #171/#172/#173 merge).
- D89 2026-09-28 [rules] `prizesForKO` splits Mega by generation (user report: Mega Greninja ex gave 2): modern 2025+ **Mega Evolution Pokémon ex** (`isModernMegaCard`, e.g. "Mega Greninja ex") give **3** prizes, Gen 6 XY **Mega Evolution Pokémon-EX** (`isLegacyMegaCard`, "M …-EX"/"Primal …-EX") stay **2** (rulebook App. 1 vs App. 14/15). `isModernMegaCard` also now returns false when the printed name matches the legacy `^(M|Primal) <Name>-EX` form, before the rarity/subtype fallbacks — TCGdex reports legacy Megas as `stage: "MEGA"`, which an importer can surface as a "MEGA" subtype token and would otherwise mis-flag them as modern. (S228).

- D61 2026-09-19 [rules] GX-attack and VSTAR Power limits are independent, per-player, per-game state in `player.oncePerGame {vstarUsed,gxUsed}` (`state.mjs` create/clone, `setupGame`, `room.mjs` seeds it), NOT on `player.flags` — `advanceTurn` rebuilds `flags` wholesale and used to wipe them. A GX attack is any attack whose printed name ends in `GX` (`isGxAttack`, damage-parser); the `attack` command refuses a second one ("Only one GX attack can be used per game.") and sets `gxUsed` when it resolves — not on a Confusion fizzle, and on the target-choice resume. `useVStarGX` carries an optional `kind: 'vstar'|'gx'` (inferred from `isVstarCard`/`isGxCard` when omitted; `instanceId` optional — the flag is per-player, not per-card), touches only the matching allowance, and falls back to spending both for unclassifiable legacy senders; `dual-run-bridge.js` maps the legacy `VSTAR-GX.js` `[type]` to `kind` (closes I22). `view.mjs` projects `oncePerGame` into the exposed `flags`, and `advanceTurn` mirrors it, so the client's special-move buttons stay greyed. A GX knockout is a normal 2-prize KO (D76), not a match loss; VMAX is unchanged (no once-per-game rule) (S193/S194, design 018, rulebook gaps #2/#3, 30c 1.2).
- D62 2026-09-19 [rules] Simultaneous wins enter a `tiebreak` turn phase with `draft.tiebreak = { players, ways }` and no `winner`, instead of a single-winner `ended` (`reduce.mjs` `setGameEnded`/`handleKnockout`). `settlePrizeEntitlements` skips the tiebreak so the original match's prizes are not collected; every start/end-of-turn hook is gated by `isGameConcluded` (ended OR tiebreak). Tiebreak resolution shipped is the scope-limited sudden-death slice: `setupGame({ firstPrizeWins: true })` flags a game where the first `takePrizes`/`takePrizesByIndex` ends it for that player, and a prize may be taken in `tiebreak` without a Knockout entitlement. The automatic fresh six-prize mat is NOT wired — deck reconstruction + client UX tracked as follow-up (rulebook 30c 1.3, S194).
- D63 2026-09-19 [rules] Phase 2 of rulebook 30c: (a) `isBasicPokemon` treats `vunion`/`restored`/`break` as non-Basic, so V-UNION/Restored/BREAK can't open, satisfy a mulligan, or be played from hand; (b) mulligan bonus draws are the NET per-pair difference (`mulliganBonusDraws`), not one-per-mulligan inside the loop, so lockstep mulligans award nothing; (c) the server's hand→play gate (`moveCard` to active/bench) now enforces Basic-only using `isBasicPokemon`, while unknown-stage cards still default to Basic so async-unenriched cards stay playable; (d) `normalizeStage('BREAK')` returns a distinct `BREAK` stage (an Evolution, ranked above Stage 2) and `evolvedView` merges the previous evolution's attacks/Ability/Weakness/Resistance/Retreat Cost into the BREAK view, the BREAK's own entries winning on a name collision (S195).
- D64 2026-09-19 [deck] Phase 3 of rulebook 30c: `deck-validation.mjs` groups every printing under `officialCardName` (strips `LV.<n>`/`LV.X` and the `Team Plasma ` prefix; KEEPS owner/form and trailing symbols) for the 4-copy limit, requires ≥1 Basic Pokémon, exempts only Basic Energy, and caps ACE SPEC ≤1 / Radiant ≤1 / Prism Star ≤1-per-name. This copy-limit normaliser is deliberately distinct from `evolution.mjs`'s `cleanPokemonName` (Phase 4.1 will document the split). Because deck-builder cards carry no `subtypes`, `card-classify.mjs` gained TCGdex fallbacks (`energyType` "Normal"/"Special", ACE-SPEC/Radiant `rarity`) and `card-search.mjs` preserves `energyType`; the shared helpers are imported with a relative path that resolves in both Node and the browser (S196).
- D65 2026-09-19 [rules] Prism Star cards route to the Lost Zone instead of the discard pile (App. 17) via `discardCardToPlayerZone` (`state.mjs`), applied to every discard site in `reduce.mjs` (KO, retreat cost, attack-energy discard, `discardAll`/`discardBoard`/`discardAndDraw`) and `effects/executor.mjs` (discard costs / hand discard), plus the legacy KO sites in `rules-bridge.js` and `chat-buttons.js`. Mass discards emit a separate `zoneMoved` event for each destination (S196).
- D66 2026-09-19 [rules] Two name normalisers are deliberately kept distinct (rulebook 30c Phase 4): evolution's `cleanPokemonName` strips `LV.<n>`/`LV.X`, collapses `VMAX`/`VSTAR` to `V` (kept so a VMAX only matches a V — App. 9/13), strips `ex`/`GX`, and PRESERVES owner/form/Team Plasma; deck-validation's `officialCardName` strips `LV` + the `Team Plasma ` prefix but KEEPS `ex`/`V`/`GX`/form suffixes/symbols (they are part of the name for the 4-copy rule, p.21). Only level-stripping is shared; the split is documented in both module headers. The plan also listed "Team Plasma stripping" as shared, but its own bullet + gap #17 require preserving it in `cleanPokemonName`, so that was kept (S197).
- D67 2026-09-19 [rules] The `cardHasRuleBox` aliases (`ko-flow.mjs`, `tool-combat.mjs`, both `= isRuleBoxPokemon`) are deleted; every call site uses `isRuleBoxPokemon` from `card-classify.mjs` directly. `search-match.mjs`'s "with/without a Rule Box" search now correctly includes Radiant and V-UNION (the old `prizesForKO > 1` heuristic excluded them) — new `rules/__tests__/search-match.test.mjs` (rulebook 30c 4.2, S197).
- D68 2026-09-19 [rules] Team Flare Hyper Gear (App. 24) is the one Tool that attaches to the opponent's Pokémon-EX. Detected by `isTeamFlareHyperGearCard` (`card-classify.mjs`, name/subtype marker). `validateReferences`+`validateLegality` allow the cross-player target AND require it to be a Pokémon-EX; the `attachCard` apply path was fixed to push into the HOST zone (`draft.players[hostRef.playerId]`) rather than the actor's, so the Tool actually lands on the opponent. A normal Tool is still own-side only (rulebook 30c 5.1, S198).
- D69 2026-09-19 [rules] Ancient Traits (App. 23) are tagged `trait: 'alpha'|'omega'` by `parseAbility` (α attach-trigger / Ω Trainer-prevention branches) and the "have no Abilities" gate `stadiumAbilityBlocked` skips any card whose ability parses to trait-only steps (`isAncientTraitAbility`). The plan named different suppressor files, but this is the live suppression point (rulebook 30c 5.2, S198).
- D70 2026-09-19 [rules] `addSpecialCondition` is Active-only (`validateReferences` rejects non-Active targets) per p.15 — closes the manual-tool hole; parsed effects already target the Active. Pokémon LEGEND are non-Basic (`isBasicPokemon`) so they cannot open/satisfy the Basic mulligan; LEGEND was already a 2-prize rule-box card since Phase 0 (rulebook 30c 5.3/5.4, S198).
- D72 2026-09-20 [rules] Ancient Traits (App. 23) are identified by the PRINTED α/Ω marker only: `parseAbility` no longer infers `trait: 'alpha'|'omega'` from trigger wording, so real Abilities worded like "when you attach an Energy…" or "whenever your opponent plays a Trainer card…" stay real Abilities and are suppressed by a "have no Abilities" Stadium (`stadiumAbilityBlocked`). `ancientTraitIn` also fixed to match the LOWERCASE ω (U+03C9) — it lowercased before checking the uppercase Ω (U+03A9), so the Ω marker never matched on its own. Data probe: TCGdex tags Ancient Trait entries `type:"Ancient Trait"` with the marker in `ability.name` (e.g. xy5-9 Sceptile "Ω Barrier"), but `tcgAbilityFromDetail` maps only `type === 'Ability'`, so those entries never become `card.ability` (correct: traits are not Abilities, and `stadiumAbilityBlocked` has no production caller yet); the marker path is exercised when an ability carrying the marker is present. Refines D69 (review-fix-plan Phase 3, S202).
- D71 2026-09-20 [rules] Renames/refines D62's simultaneous detection: `handleKnockout` now counts win **ways** per player (attacker: last Prize + victim board emptied; victim: attacker's own board emptied on a self-KO) and decides from the counts, so "last Prize + empty board on one KO" is a two-way attacker win (prizes collected) instead of a bogus tiebreak that wedged the turn (rulebook 30c 1.3a / p.21). A `tiebreak` is now reachable only when both boards empty with equal ways. `setGameEnded`'s simultaneous branch also sets `firstPrizeWins`, so the interim "first Prize wins" resolution survives a `pass` hand-over; `pass`/`takeTurn` hand the turn over during `tiebreak`, while `attack`/`moveCard`/`attachCard`/`playTrainer`/`useAbility` are refused with `tiebreak_pending`; a new `isGameFrozen` (phase `'ended'` only) separates "game over" legality from `isGameConcluded` (ended OR tiebreak) start/end-of-turn hook gating. The fresh six-prize sudden-death mat remains deferred (review-fix-plan Phase 1, S200).
- D73 2026-09-20 [rules] `isExCard`/`isGxCard` require a separator before the name suffix (`/(?:^|[\s-])ex$/i`, same for gx) instead of a bare `endsWith`: plain names ending in the letters "ex" — Toxapex is the canonical one — were classified as rule-box ex (2 prizes, excluded from "without a Rule Box" searches, rule-box-gated accelerators skip it). Regression names `Cetitan ex` / `M Venusaur-EX` / `Pikachu ex` unchanged; audit found no separator-less fixture (review-fix-plan Phase 4, S203).
- D74 2026-09-20 [rules] App. 24 Team Flare Hyper Gear accepts a modern Pokémon ex as its "Pokémon-EX" target: `isExCard` deliberately unifies modern ex and legacy EX engine-wide (prize counts, Briar-style effects), so the letter of App. 24 is relaxed in Unlimited by design — documented at `isTeamFlareHyperGearCard`, not narrowed (Phase 6.1). A genuine simultaneous tie likewise keeps Phase 1's click-resolution as the shipped behavior; a fresh six-prize sudden-death mat stays deferred to I61 (Phase 6.2). (review-fix-plan Phase 6, S204)
- D75 2026-09-20 [harness] `pnpm test` now runs `node --test "shared/**/*.test.mjs" "client/**/*.test.mjs" "server/**/*.test.mjs" "bot/**/*.test.mjs"` instead of the ~151-file explicit list, which overflowed the Windows command line ("The command line is too long"). The globs also pick up `evolution.test.mjs`, which the explicit list had omitted — 2126/2126 green. (review-fix-plan Phase 6.3, S204)
- D86 2026-09-20 [rules] Special-energy card effects get a dedicated parser (`shared/engine/rules/special-energy-parse.mjs`) one level below `energy-effects.mjs`'s coarse family classification: `parseSpecialEnergyEffects(card)` emits structured steps (provide with conditions, attach restriction, HP/damage/reduction modifiers, status/effect shields, on-attach/on-KO/on-discard triggers). Announce-only like the other rules parsers; execution lives in the pure `getSpecialEnergy*` helpers, wired for combat (`computeAttackDamage`: bonus/penalty/reduction) and HP (`effectiveHp`). Chose a text parser over per-card name rules so new special energies generalize. (S220)
- D87 2026-09-20 [rules] `ancientTraitIn` (`shared/engine/rules/abilities.mjs`) identifies an Ancient Trait by any of the four printed markers — Δ (`delta`), θ (`theta`), Ω (`omega`), α (`alpha`) — or the one spelled "Delta …" name (M Rayquaza-EX "Delta Wild"), not just α/Ω; the marker (or name) no longer has to be inferred from wording (D72). Tagging moved to ONE site: `parseAbility` runs `ancientTraitIn(text)` after the passive fallback and copies `trait` onto every emitted step (the two per-branch tags are deleted), so a trait whose body matched no branch still carries a tagged fallback and `isAncientTraitAbility`'s "every step tagged" invariant holds. Ancient-Trait coverage gets its own corpus + audit — `out/pkmn-ancient-trait-cards.json` (pkmncards `has:ancient-trait`, 59 printings) and `scripts/audit-all-ancient-traits.mjs`, which reports `unrecognized 0` and exits non-zero otherwise; the pkmncards splitter both audits share lives in `scripts/lib/split-card-text.mjs`. Announce-only: trait effects are still not executed. (S224)
- D91 2026-09-21 [netcode] Under server authority the battle log is derived client-side from the same advisory view `events` stream (design 013's precedent), not from server-rendered strings: a new pure `client/src/setup/netcode/server-battle-log.mjs` maps ~20 ordinary-play event types to perspective-phrased lines and composes `attack-announcements.mjs`; both players read one source and phrase by their own absolute `playerId`. The server gains an additive `trainerPlayed {playerId,instanceId,name,stadium}` event (Tools excluded � their `cardAttached` narrates them) and `cardAttached` is now emitted only for non-Pok�mon attaches so an evolution is not double-announced. (design 021, I71, S231)
- D92 2026-09-21 [render] Coin material realism is one shared CSS system, not per-surface rules: `client/src/css/coin/` (base + one sheet per material + `finish.css`) owns the coin geometry, the flip, and four fixed-light layers — `__env` studio reflection, `__spec` anisotropic sweep masked by the coin's own art as a luminance relief mask, `__holo` finish rainbow, `__grain` brushed texture. `index.css` (picker) and `mat-coin.css` (playmat iframes) only `@import url('./coin/coin.css')` plus sizing/position; the old duplicated `::after` sweeps and material rims were deleted. `coin-effects.mjs::resolveCoinEffect` maps `material` to a known value and derives `finish` (standard/holofoil/mirror) from the Bulbapedia `description` already in the catalog rather than inventing a field; `computeCoinLight` places the highlight from the coin's angle (never the cursor), matching holo's fixed-light decision (design 010). Chosen over per-surface rules (drift left metal/cardboard unstyled on the mat) and over a class-name-only effect. Techniques 4 (pre-baked normal maps) and 5 (WebGL env map) were offered and deferred by the user: CSS metal is stylized, not a true relight. (S234)
- D93 2026-09-21 [board-ui] The in-play Stadium is rendered in the SAME tilted plane as the board, not flat above it: `#stadium` is parent-owned (sibling of `#battleMat`), so it cannot inherit the mat's transform. `apply-table-tilt.js` reuses `tilt.mat.transform` with the mat's pivot (bottom-centre of the mat box) re-expressed in the Stadium's own frame via pure `stadiumTilt` (`table-tilt.mjs`); both boxes are read untransformed, so re-applying on resize/flip/mat-change never feeds the projected rect back in. The Stadium's box is sized by the mat layout to an in-play card (`active-height * 0.84`, the iframe/parent mat-height ratio) and takes `aspect-ratio: var(--card-aspect)` so the image fills it without distortion; `--stadium-width` was removed. Chosen over a flat enlargement (a flat element can never match a perspective-projected plane) and over moving the Stadium into an iframe (it is neutral and straddles both halves). (S235)
- D94 2026-09-21 [render] Mat cosmetic effects (design 022) share ONE dispatch: `EVENT_FX` in advisory-animations.mjs maps advisory event types to `{kind:'fx', effect}`, routed by `netcode/mat-fx/dispatcher.mjs` (kill switch `body.fx-off` / localStorage `ptcg-fx-off`, reduced-motion skips transient effects) to the registry in `mat-fx/index.js`; overlays are detached body children via `image-logic/mat-fx.mjs` (`spawnOverlay`/`runPose`). Board ambience (glow/hover/tilt) is CSS in `css/mat-ambient.css` (zero-specificity `:where`), mat tilt is `deck-constructor/mat-tilt.mjs`, not holo.mjs. Add an effect = one EVENT_FX row + one registry entry; never a second handler.
- D95 2026-09-22 [deck-builder] The deck builder's PTCG Live restyle is ONE stylesheet, `client/src/css/deck-builder-live.css`, with every rule scoped under `.db-live` (set on `#nativeDeckBuilderWorkspace` by `core/builder-theme.mjs`). The scoping class is load-bearing: a plain `@import` lands before index.css's own deck-builder rules and would lose the cascade at equal specificity, so `.db-live` is what wins — chosen over reordering index.css (fragile) and over `!important` (unpickable later). Themes differ ONLY by the token block at the top: `.db-light` re-tokenizes to the pre-Live grey palette, so no layout rule is ever duplicated per theme. Dark is the default because that is what Live looks like, and the builder owns its theme rather than riding the legacy per-element `dark-mode-1` body class, which only covers the game surface and would couple a full-screen overlay to the mat's setting. (S252)
- D96 2026-09-22 [deck-builder] Deck-builder presentation splits pure model from DOM: `core/deck-counter.mjs` (the "x / 60" readout) and `core/card-filters.mjs` (filter state + predicate) hold every rule and are unit-tested headlessly, while `native-deck-builder-renderers.js` only draws and reports clicks. Filters are OR within a group and AND across groups (Live's behaviour) and run client-side over the already-fetched result set, so a pill never costs an API call. The energy pills reuse `actions/move-card-bundle/energy-token-assets.mjs`'s type vocabulary and art rather than a second type list, which is why "Dark" matches the Darkness pill for free. `validateDeck` gained `requiredCards` so the counter reads the format's legal size instead of hard-coding 60. (S252)
- D97 2026-09-22 [deck-builder] Deck Pokémon sprites (design 024) are VENDORED, not hotlinked: all 905 gen-8 base-form species from msikma/pokesprite, regular + shiny, live at `client/src/assets/pokemon/gen8/{regular,shiny}/<slug>.png`, refreshed by `scripts/generate-pokemon-sprites.mjs` (the only thing that talks to the network; it also emits `core/pokemon-sprite-catalog.generated.mjs`, already prettier-formatted so it is never lint noise). Chosen over hotlinking jsDelivr at runtime (the builder would gain a third-party dependency and render nothing offline) and over a curated subset (two code paths plus a curation list); the cost is ~1.5 MB and 1810 files, which buys a URL builder with no fallback branch. Base forms only — mega/gmax/regional would multiply the catalog and force a two-level picker for a decoration. A deck stores `sprites: [{slug, shiny}]` (max 3) rather than parallel slug/shiny arrays, so a slot's variant cannot drift from its species, and `parseLibrary` normalizes the field on every load, which is what makes a pre-S253 library and a hand-corrupted one both safe. The picker opens from the strip beside the deck name rather than becoming a fourth Customize tab: the control sits where its effect shows, and Customize stays about board cosmetics. (S253)
- D98 2026-09-22 [deck-builder] Per-card deck-list sprites (design 025) come from one pure resolver, `core/card-sprites.mjs` `cardSpriteFor(card)`: Pokémon names are stripped of rule-box suffixes, owner ("Iono's") and decorative ("Radiant") prefixes, then mapped to a form slug (Mega [X/Y], Primal, VMAX→Gigantamax, Alolan/Galarian/Hisuian/Paldean) that falls back to the base species when pokesprite has no art; unknown leading words are dropped one at a time so "Origin Forme Dialga" still lands. Item and Tool Trainers match pokesprite item names, with a small `CARD_ITEM_OVERRIDES` table for gaps (Choice Belt → Choice Band). Item art is vendored like D97 by `scripts/generate-item-sprites.mjs` (only folders that can match a Trainer, ~500 PNGs, ~0.8 MB), not hotlinked. Supporters, Stadiums and Energy get no sprite but keep an empty slot so names align. (S254)
- D99 2026-09-22 [deck-builder] PC Box restyle (design 025, user-approved Superdesign drafts): decks show exactly TWO sprites — `MAX_DECK_SPRITES` 3→2 (a third saved slot is dropped on next save; revert = set back to 3) and empty slots auto-fill at render time from the deck's most-copied Pokémon, never persisted. Each deck stores a `wallpaperId` (16 Gen V Box wallpapers, 1x PNGs scaled pixelated). The look is a separate layer, `css/deck-builder-pc-box.css`, scoped `.db-live:not(.db-light)` so it out-ranks D95's Live rules without !important and the light theme opts out. The Box wallpapers (Nintendo) and a Latin-subset woff2 of FOT-Rodin ProN Condensed (Fontworks, 9.8 MB TTF → 19 KB via fonttools, a dev-time tool, not a dependency) are committed at the user's explicit request. (S254)
- D100 2026-09-22 [deck-builder] Gen 9 sprites are FAN ART, supplied by the user as one 142-sprite sheet in the pokesprite gen-8 box style (pokesprite stops at Hisui). It holds all 120 species (906–1025) plus 22 Paldea-era forms, among them Paldean Tauros ×3, Paldean Wooper, Bloodmoon Ursaluna, the Ogerpon masks and Terapagos Terastal/Stellar. Each sprite was sliced once into pokesprite's 68×56 canvas, bottom-aligned, and vendored at `assets/pokemon/gen9/regular/`. The catalog is the hand-kept `core/pokemon-sprite-catalog-gen9.mjs`, not generated. `deck-sprites.mjs` merges it into the gen-8 catalog with a numeric, stable idx sort, so a Paldean form sits under its gen-8 species. There is no shiny art, so `hasShinySprite` is false for these slugs: shiny is never stored, the URL falls back to regular, and the picker hides the ✦ toggle. Card names map through `card-sprites.mjs`: Bloodmoon/mask prefixes, the Terapagos ex → Terastal and Palafin ex → Hero forms, and Paldean Tauros breed from the card's `types`. Koraidon Limited Build and Miraidon Low-Power Mode are identified from the art and are the least certain labels. (S255)
- D101 2026-09-22 [deck-builder] The Legends: Z-A and Mega Dimension Megas are 48 more fan-art sprites from a second user sheet. They go in the same hand-kept `pokemon-sprite-catalog-gen9.mjs` and the same `gen9/regular/` folder, again with no shiny (D100), rather than a third art set. Each slug is `<species>-mega[-x|-y|-z]`. Magearna's Original Color becomes `magearna-original-mega`, and the Tatsugiri forms become `tatsugiri[-droopy|-stretchy]-mega`. The card parser's Mega pattern now accepts a Z suffix, so "Mega Lucario Z ex" finds `lucario-mega-z` and "Mega Lucario ex" still finds the XY Mega. This sheet was sliced per 32 px cell, not by outline, because neighbouring sprites touch. Least certain labels: Mega Floette, the Magearna pair (normal vs Original Color) and which Tatsugiri form is which. (S256)
- D102 2026-09-22 [deck-builder] TCGdex JSON responses (set, series, card records, category indexes, name searches) persist in IndexedDB via `core/tcgdex-cache.mjs`, which replaces the local `fetchJson` in `set-browser.mjs` and `card-search.mjs`. The TTL is 7 days for single `/sets/{id}` and `/cards/{id}` records and 1 day for everything else, so new releases still show up within a day. A stale entry is served when the network fails, and failed requests are never stored. IndexedDB was picked over localStorage because the category indexes and set card lists together exceed localStorage's ~5 MB limit. With no IndexedDB available (node tests, blocked storage) the cache is a plain pass-through. Card images are out of scope: caching them needs a service worker, and the CORS/opaque-response quota cost must be checked first. No dependency added. (S257)
- D103 2026-09-22 [mat-fx] Mat effects run on WAAPI keyframes sampled from the pure pose functions (`sampleKeyframes` + `animateFrames` + `removeWhen` in image-logic/mat-fx.mjs), not on rAF style writes: compositor-thread motion, and the pose functions stay the unit-tested source of truth. Hit/KO effects wait on an impact queue because the engine emits `attackExecuted` after the damage events (design 026).
- D104 2026-09-22 [mat-fx] Mega and Tera Pokémon get signature entry animations (Tera: type-coloured crystal that shatters; Mega: keystone sphere with blue/orange swooshes, card-local) instead of the generic evolve burst. They play on `pokemonEvolved` and on a `cardMoved` into play — from hand, deck or discard onto Active/Bench, the engine's own `enteredPlayTurn` rule — which the client maps to a new `enter` effect. No engine or protocol change; the Mega/Tera test runs in the effect from the registry card, so plans stay card-free. Other rule-box families keep the default burst for now (design 027).
- D105 2026-09-22 [mat-fx] `motionReduced()` ignores the OS `prefers-reduced-motion` query and is opt-in only (`localStorage['ptcg-reduce-motion'] === '1'`). Windows reports reduced motion whenever "Animation effects" is off system-wide, and with an empty `STATIC_FALLBACKS` that silently skipped every battle effect (only draw/shuffle/KO ghost played). Covers mat FX, the KO burst and the card holo. The coin-flip ceremony and CSS `@media (prefers-reduced-motion)` rules still follow the OS. (S260)
- D106 2026-09-23 [netcode] applyView moves a card node only when it is out of view order (`placeInViewOrder`), and never re-sets an unchanged `src`: re-inserting detaches the node and restarts its foil/idle animations, which made every card blink on every view. Finding #10's ordering still holds. The board's End Turn button clicks `#p2PassButton` (same route as Alt+T). Why: S264 #11/#9.
