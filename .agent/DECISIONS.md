# Decisions — binding choices, one line each, newest first. Do not relitigate without new info.
# Read by GREP, not wholesale: `grep -n "\[rules\]" .agent/DECISIONS.md` (scope tag) or `grep -n "D119"`.
# Format: `D<n> <YYYY-MM-DD> [scope] decision — why. (Supersedes D<m>.)` — ONE line, ≤220 chars.
#   Detail belongs in the design doc the line cites, not here.
# Caps: ≤90 active lines / ≤20 KB. Superseded lines are deleted (git + archive keep them).
# Full pre-2026-09-24 wording of every line: .agent/archive/DECISIONS-full-2026-09-24.md (grep it).
# ID collisions from parallel sessions: D43, D44, D86, D117–D120 each exist twice — cite as `D119[rules]`.
#   Next id = highest D number in this file and the archive + 1 (next: D125).

## Active
- D124 2026-09-24 [drag] Dragged card = body-level avatar that rolls into horizontal motion (design 038); native DnD still drops, ghost blanked via setDragImage, pointer from dragover in all frames; no resize (user). (S287)
- D123 2026-09-24 [harness] Big harness files are grep-only (CLAUDE.md § Reading budget); verbose originals live in .agent/archive/; a PostToolUse hook (scripts/hooks/post-edit-check.mjs) lints + runs the edited file's own test — full suite once before commit. (S286)
- D122 2026-09-24 [mat-fx] Tera crystal takes the Pokémon's type colour via `teraPaletteFor(card.types[0])` (tera-crystal.mjs, own TERA_TYPE_RGB); orb/flash/jewel/rainbow universal; skin follows. (Supersedes D117[mat-fx] mint, D120[mat-fx] icy.) (S281)
- D120[rules] 2026-09-24 Conditional-KO wordings reuse `atkKnockOut`/`atkKnockOutChoose` steps; KO wins settle once via `settleKnockOutWins` (design 036 A4). (S284)
- D119[rules] 2026-09-24 Extra-Prize-on-KO read by `parsePrizeOnKo`; immediate wording pays in reduce's attack tail, "next turn" wording is a `prizeBonus` marker; grants floored at remaining Prizes (design 036 A3). (S284)
- D118[rules] 2026-09-24 Whole-attack conditions (`rules/attack-conditions.mjs`, gate once per attack) and printed status clauses (`rules/attack-status.mjs`) get one pure layer each; `card.movedToActiveTurn` stamped at every promote/switch (design 036). (S282)
- D117[rules] 2026-09-24 Tool modifiers gate through one pure layer, `rules/tool-conditions.mjs` (null condition = unconditional); holder conditions read the top evolution view (design 035). (S281)
- D121 2026-09-23 [mat-fx] Tera Pokémon keep a crystal skin in play, reconciled by instanceId (`createSkinReconciler`) on `board-view-applied` and `holo-wrapper-changed`; needs an authoritative view (design 037). (S280)
- D119[mat-fx] 2026-09-23 Mega vortex = 3D brush strokes on the orb canvas (mega-vortex.mjs); all canvas FX share `playCanvasStage` (entry.js) so D118's WAAPI clock holds (design 036-mega). (S280)
- D118[mat-fx] 2026-09-23 Mega keystone orb is a canvas 3D faceted shell (mega-orb.mjs); rAF redraw reads a WAAPI animation's `currentTime` (keeps D103's single clock); geometry/draw DOM-free and unit-tested. (S279)
- D116 2026-09-23 [rules] Team-wide "no Retreat Cost" passives are read from the Bench by one pure helper `teamNoRetreatCostForActive` (ability-executors.mjs); callers pass optional `benchCards`. (S277)
- D115 2026-09-23 [board-ui] #stadium's transform belongs to the table tilt; owner-facing flip is class `.stadium-opp-facing` rotating the img (`setStadiumFacing`), never an inline transform on #stadium. (S273)
- D114 2026-09-23 [rules] An attack's printed hand-card discard (Voltage Shoot) makes the attack illegal when the hand cannot pay it (design 033). (S271)
- D113 2026-09-23 [rules] Encore-style locks are an `attackLock` marker `{mode:'only'|'except', attackName}` on the opponent's Active, checked in the attack legality gate (design 033). (S271)
- D112 2026-09-23 [rules] "If you do / in this way" after a non-attach step is one BLOCK step with `then: <marker step>`, run only when its own discard happened; executor unchanged (design 033). (S271)
- D111 2026-09-23 [rules] Coin-gated attack sentences run as design-030 steps; "flip until tails" in `flipAttackCoins` (cap 20); opponent-turn coin effects are markers (design 032). (S271)
- D110 2026-09-23 [rules] Copy attacks pick the copied attack in the attack command, before coins, and resolve through the normal damage+step path; resume tokens carry `copiedAttack` (design 031). (S269)
- D109 2026-09-23 [rules] Timed attack effects live in one `card.attackMarkers` array read at damage time; cleared on leaving play, retreat, evolution (design 031). (S269)
- D108 2026-09-23 [tooling] Attack/ability execution claims are gated by `pnpm audit:oracle` (~2 min) against committed scripts/oracle-baseline.json; 0-row families only via ORACLE_BLIND_FAMILIES with a reason (design 032). (S269)
- D107 2026-09-23 [rules] Attack effects run as parsed executor steps (design 030); text helpers stand down when steps own the clause. (S267)
- D106 2026-09-23 [netcode] applyView moves a card node only when out of view order (`placeInViewOrder`) and never re-sets an unchanged `src` — re-inserting restarts foil/idle animations (blink). (S264)
- D105 2026-09-22 [mat-fx] `motionReduced()` ignores OS prefers-reduced-motion; opt-in only via `localStorage['ptcg-reduce-motion']==='1'` (Windows reports reduce whenever animations are off). (S260)
- D104 2026-09-22 [mat-fx] Mega/Tera get signature entry animations on `pokemonEvolved` and `cardMoved` into play (client `enter` effect); no engine/protocol change (design 027; look superseded by D118–D122[mat-fx]).
- D103 2026-09-22 [mat-fx] Mat effects run on WAAPI keyframes sampled from pure pose functions (`sampleKeyframes`/`animateFrames`), not rAF style writes; hit/KO wait on an impact queue (design 026).
- D102 2026-09-22 [deck-builder] TCGdex JSON responses cache in IndexedDB (`core/tcgdex-cache.mjs`), TTL 7 d for single records, 1 d otherwise; stale served on network failure; images out of scope. (S257)
- D101 2026-09-22 [deck-builder] Legends Z-A / Mega Dimension Mega sprites are user fan art in the gen9 catalog, slug `<species>-mega[-x|-y|-z]`, no shiny. (S256)
- D100 2026-09-22 [deck-builder] Gen 9 sprites are user fan art in hand-kept `pokemon-sprite-catalog-gen9.mjs` + `gen9/regular/`; no shiny art, so shiny is never stored for them. (S255)
- D99 2026-09-22 [deck-builder] PC Box restyle: decks show exactly 2 sprites (auto-filled at render, never persisted), per-deck `wallpaperId`; layer `css/deck-builder-pc-box.css` scoped `.db-live:not(.db-light)`. (S254)
- D98 2026-09-22 [deck-builder] Per-card deck-list sprites come from one pure resolver `cardSpriteFor` (core/card-sprites.mjs); item art vendored by scripts/generate-item-sprites.mjs. (S254)
- D97 2026-09-22 [deck-builder] Pokémon sprites are VENDORED (pokesprite gen-8, scripts/generate-pokemon-sprites.mjs), not hotlinked; deck stores `sprites: [{slug, shiny}]` normalized by `parseLibrary`. (S253)
- D96 2026-09-22 [deck-builder] Presentation splits pure model (`core/deck-counter.mjs`, `core/card-filters.mjs`; OR within group, AND across) from DOM renderers. (S252)
- D95 2026-09-22 [deck-builder] PTCG Live restyle = one stylesheet `css/deck-builder-live.css`, every rule under `.db-live`; themes differ only by the top token block; dark default. (S252)
- D94 2026-09-21 [render] Mat cosmetic FX share ONE dispatch: `EVENT_FX` row → `mat-fx/dispatcher.mjs` → registry `mat-fx/index.js`; never a second handler (design 022).
- D93 2026-09-21 [board-ui] In-play Stadium renders in the board's tilted plane via pure `stadiumTilt` (table-tilt.mjs) reusing the mat transform; sized to an in-play card. (S235)
- D92 2026-09-21 [render] Coin material realism is one shared CSS system `css/coin/`; `resolveCoinEffect` derives finish from the catalog description; fixed light, never cursor. (S234)
- D91 2026-09-21 [netcode] Server-authority battle log is derived client-side from view `events` (`server-battle-log.mjs`); server adds `trainerPlayed`; `cardAttached` only for non-Pokémon (design 021). (S231)
- D90 2026-09-28 [rules] `parseAbility` emits the executor's filter contract (`energyOnly`/`basicOnly`/`energyTypes`) instead of the executor guessing from whole-card text. (S229)
- D89 2026-09-28 [rules] `prizesForKO`: modern "Mega … ex" gives 3 Prizes, Gen 6 "M …-EX"/"Primal …-EX" gives 2; `isModernMegaCard` rejects the legacy name form first. (S228)
- D88 2026-09-20 [netcode] Active-KO promotion settles at the command tail (`settlePromotionChoices` before `settlePrizeEntitlements`): lone Bench auto-promotes, 2+ raise a `source:'promote'` choice. (S226)
- D87 2026-09-20 [rules] Ancient Traits are identified only by the printed Δ/θ/Ω/α marker (or "Delta …" name) via `ancientTraitIn`, tagged once in `parseAbility` on every step; own audit script. (Supersedes D69, D72.) (S224)
- D86[rules] 2026-09-20 Special Energy effects get a dedicated parser `rules/special-energy-parse.mjs`; execution in pure `getSpecialEnergy*` helpers used by combat and `effectiveHp`. (S220)
- D86[deck-builder] 2026-09-20 Coin downloader auto-links scans to catalog entries by normalized description; `--no-link` keeps manual; placeholder detection stays by path. (Supersedes D85.) (S219)
- D84 2026-09-20 [deck-builder] Coin catalog normalized by committed `scripts/normalize-coin-catalog.mjs`; ids unique; the 202 Gen IX ids stay frozen (decks persist `coin.id`). (S219)
- D83 2026-09-20 [rules] Long-tail ability coverage is parse/classify only (~40 step types, 18 families); classifier order specific-before-broad, `discard-cost` last; execution announce-only. (S211)
- D82 2026-09-20 [rules] Last attack-classifier one-offs map to narrow families (`move-damage-counter`, `neutralize-opponent-energy`) or card-specific broadenings, never a generic bucket. (S210)
- D81 2026-09-20 [rules] `classifyAttackEffect` families: a compound card's PRIMARY mechanic wins; `first-turn-attack` checked last before `flat`; classifier announce-only. (S208)
- D80 2026-09-20 [rules] `parseAbility` gets distinct step types per corpus cluster (not reused neighbours); continuous ones go in `PASSIVE_ABILITY_STEP_TYPES`. (S208)
- D79 2026-09-20 [rules] Ability classifier `type-change` family for type changes; energy-provides wording stays in `energy-multiplier`. (S207)
- D78 2026-09-20 [rules] Stadium extra attacks merge printed → inherited → granted, de-duped by name, identically on server (`attackViewFor`) and client, so `attackIndex` resolves the same. (S207)
- D77 2026-09-20 [board-ui] Double-click in-play Stadium opens the card inspector with a Use panel gated by pure `stadiumActivationStatus` (design 018). (S193)
- D76 2026-09-19 [rules] One card-classification contract: `rules/card-classify.mjs` (`isRuleBoxPokemon`, `prizesForKO`, family predicates); VMAX/TAG TEAM/V-UNION = 3 Prizes. (Mega count: D89.)
- D75 2026-09-20 [harness] `pnpm test` runs globbed `node --test` patterns (explicit list overflowed the Windows command line). (S204)
- D74 2026-09-20 [rules] Team Flare Hyper Gear accepts modern ex as "Pokémon-EX" (`isExCard` unifies ex/EX engine-wide); tie keeps click-resolution, sudden-death mat deferred (I61). (S204)
- D73 2026-09-20 [rules] `isExCard`/`isGxCard` need a separator before the suffix (`/(?:^|[\s-])ex$/i`) — Toxapex is not ex. (S203)
- D71 2026-09-20 [rules] `handleKnockout` counts win WAYS per player; tiebreak only when both boards empty with equal ways; `isGameFrozen` (ended) vs `isGameConcluded` (ended or tiebreak). (Supersedes D62.) (S200)
- D70 2026-09-19 [rules] `addSpecialCondition` is Active-only; Pokémon LEGEND are non-Basic. (S198)
- D68 2026-09-19 [rules] Team Flare Hyper Gear is the one Tool attaching to the opponent's Pokémon-EX; `attachCard` pushes into the HOST zone. (S198)
- D67 2026-09-19 [rules] `cardHasRuleBox` aliases deleted; every site uses `isRuleBoxPokemon`. (S197)
- D66 2026-09-19 [rules] Two name normalisers stay distinct: evolution's `cleanPokemonName` vs deck-validation's `officialCardName`; only LV-stripping is shared. (S197)
- D65 2026-09-19 [rules] Prism Star cards go to the Lost Zone via `discardCardToPlayerZone` at every discard site. (S196)
- D64 2026-09-19 [deck] deck-validation groups by `officialCardName` for the 4-copy rule; ≥1 Basic; ACE SPEC/Radiant ≤1, Prism ≤1 per name; card-classify has TCGdex fallbacks. (S196)
- D63 2026-09-19 [rules] V-UNION/Restored/BREAK are non-Basic; mulligan bonus = net per-pair difference; server hand→play is Basic-only; BREAK is its own stage merging the prior evolution. (S195)
- D61 2026-09-19 [rules] GX/VSTAR limits are per-player per-game `player.oncePerGame {vstarUsed,gxUsed}` (not `flags`, which `advanceTurn` rebuilds); GX = name ends "GX". (design 018)
- D60 2026-09-19 [netcode] Mat picker serves single AND multi-pick in-play-Pokémon choices (retreat, Escape Rope, attack targets). (Supersedes D59.) (design 017)
- D57 2026-09-19 [rules-ui] Carousel never sets pointer capture over interactive slide content (`isSwipeBlockedTarget`); verify carousel UI with real input, not `el.click()`. (S189)
- D56 2026-09-19 [board-ui] Holofoil drift ignores prefers-reduced-motion; no holo opt-out (user decision). (S188)
- D55 2026-09-19 [board-ui] Holofoil drifts on every surface while not held by the cursor (user decision). (S186)
- D54 2026-09-19 [rules] Ability position gate is in the ENGINE: `requiresActiveSpot` + `listAbilities({zone})` + `validateLegality`; matches only the restrictive conditional wording (design 015). (S182)
- D53 2026-09-19 [rules-ui] Card inspection and attack/ability selection live on double-click in one inspector; single-click = select-to-move (design 013). (S174)
- D52 2026-09-18 [netcode] Client opening sequence is guarded by an `openingStarted` latch, never `rulesState.phase === 'setup'`. (S173)
- D51 2026-09-18 [netcode] Opening turn order is a server-owned coin call on GameRoom (not GameState); silent caller for 15 s is called for by the server. (S173)
- D50 2026-09-18 [rules] Grand Tree search-evolve (incl. chained Stage 2) runs in the SERVER engine via `searchEvolve` `step.chainStage2`, not a client exemption. (S173)
- D49 2026-09-18 [netcode] "Look at top/bottom N" is a `peekDeck` request to the asker only (no state change); picks leave as moveCard; opponent deck peek only with rules off. (S171)
- D48 2026-09-18 [netcode] Face-down card: `card.faceDown`, redacted for everyone until `revealShortcut`; flag swept off non-board cards each command; seeded pick. (S171)
- D47 2026-09-18 [netcode] Manual board tools are planned from the card registry (`manual-card-commands.mjs`) and sent as the acting player, never via the owner. (S171)
- D46 2026-09-17 [rules] Prize entitlement (`player.flags.prizesOwed`) is settled by a server pendingChoice (`effectType:'prizes'`) that pauses the game until picked. (Supersedes D43[rules] self-collect.) (S170)
- D45 2026-09-17 [rules] Rotation condition in `specialCondition`; Poison/Burn as `poisoned`/`burned` keys; all access via `rules/special-conditions.mjs` (design 011). (S169)
- D44[netcode] 2026-09-17 Socket commands carry integer `clientSeq ≥ 0` via `GameRoom.handleClientCommand`; replayed seq answers success+dedupe, never re-executes. (S168)
- D44[harness] 2026-09-17 Plan-driven work runs through exec-plan.md: section at a time, green before next, one commit per plan. (S159)
- D43[board-ui] 2026-09-17 Highlight palette: own side BLUE, far side RED; gold only means "attack payable"; "cannot act" = grey + red ✕. (user decision, S158)
- D42 2026-09-16 [board-ui] #bench is a 5-column CSS grid so bench Pokémon align to the 5 printed slots.
- D41 2026-09-16 [rules] Rare Candy traces lines through the player's own cards only; allowed when untraceable (bot sees the same). (S145)
- D40 2026-09-16 [rules] Evolutions stay attached under the Basic; rules read `evolvedView(zone, root)`; damage/conditions stay on the root. (S145)
- D39 2026-09-16 [engine] Multi-choice step progress lives in `resumeToken.context` keyed `<stepIndex>:<stepType>`; yes/no uses BARGAIN_YES/NO pseudo-ids. (S144)
- D38 2026-09-16 [rules] Trainer play restrictions live in one module `rules/trainer-play-conditions.mjs`, shared by server legality and the bot. (S144)
- D37 2026-09-16 [netcode] Rules-mode moveCard of an effect Item/Supporter to 'board' is promoted to `playTrainer` inside `applyCommand` from server card data. (S144)
- D36[netcode] 2026-09-16 Under server authority the legacy renderer never writes DOM for zones apply-view draws (`server-rendered-zones.mjs`); deck/pile covers stay legacy. (S142)
- D35 2026-09-16 [netcode] Opening deal runs only when both players pressed Set Up with decks loaded; a player Reset resets the server game for both seats.
- D34 2026-09-15 [ui] Holo foil lit by a fixed virtual light from card angle (`computeLightVars`), never the cursor; ink mask only for CORS-verified hosts (design 010). (Supersedes D31.)
- D33 2026-09-15 [rules] Gen 6 Mega turn-end fires `rules-mega-evolution-forces-turn-end`, which clicks the existing Pass button; legacy vs modern Mega by name prefix.
- D32 2026-09-14 [board-ui] Playmat art is aspect-locked and centered with a blurred bleed; zones scale via --mat-aspect/--mat-width/--mat-offset-x.
- D30 2026-09-11 [rules] Grand Tree's chained Stage 2 bypasses just-evolved gates (printed exception) via `bypassJustEvolvedGate`, not `forceEvolution`; legacy client path.
- D29 2026-09-11 [testing] `stampE2eCard` stamps only `e2eFixtureDeck()` cards (`set === 'e2e'`), never real decklists under ?e2e=1.
- D28 2026-09-11 [netcode] Legacy search effects await every move before shuffling; mirror tolerates wrong-length permutations and logs `shuffleZone.indices_mismatch`.
- D27 2026-09-11 [testing] Debug mode = the ?e2e=1 bridge + `window.__ptcg.debugMode(enabled)`; e2e mode is exempt from the 2P rules-forced-on lock.
- D25 2026-09-10 [deck-builder] Each generation pill gets a synthetic Energy tab (`__energy_gen<N>__`) from a plain per-set Energy sweep.
- D24 2026-09-10 [deck-builder] Generation pills source sets from TCGdex `series`; map 9=sv, 8=swsh, 7=sm, 6=xy, 5=bw, 4=dp+pl, 3=ecard+ex, 2=neo, 1=base+gym (user call).
- D22 2026-09-10 [board-ui] Energy-token art = 11 user PNGs in `assets/energy/tokens/`; selector `.play-container img.energy-token-3d` for specificity. (Supersedes D21[board-ui] coin photos.)
- D20 2026-09-10 [rules-ui] Pickers: either auto-move (no `pickOnly`) or `pickOnly:true` + manual move in `onPick` — never both (double-move).
- D19 2026-09-10 [rules-ui] Legacy trainer pickers whose candidate is an in-play Pokémon use `openMatPick()` (click the real card), not the carousel (user scope call).
- D18 2026-09-10 [deck-builder] Browse Sets Energy tab is synthetic (`ENERGY_SET_ID`); modern basic energy art hotlinked from pkmncards; gold basics via `EXTRA_ENERGY_CARD_REFS`.
- D17 2026-09-10 [netcode] Prod (render.yaml) sets SERVER_AUTHORITATIVE='1'; server.js default stays OFF for local/dev/tests. (Supersedes D8.)
- D16 2026-09-10 [netcode] reveal/hide commands only flip the sender's own cards; no cross-player reveal.
- D15 2026-09-09 [netcode] Printed card data reaches the server as its own `cardStats` command addressed by `syncInstance`, updating in place.
- D14 2026-09-09 [netcode] Server is sole turn authority (`reconcileTurnState`); cross-client parity compares public zones only, secret zones by count.
- D13 2026-09-09 [netcode] Client zone reads under server authority use apply-view's last-applied-view cache; `deck` excluded from hash comparison.
- D12 2026-09-09 [netcode] A `server_command` action gates its legacy body on `serverAuthoritative`; untranslatable commands fail OPEN to the legacy body.
- D11 2026-09-09 [netcode] Full authoritative rendering: client renders only from server views (O4 gate, user call). Closes I5, I6.
- D10 2026-09-09 [netcode] Card identity is translated, never punned: server mints instanceId, returns syncInstance→instanceId map; unresolvable hint drops the command.
- D9 2026-09-09 [netcode] Reconnect = bounded peer action-log catch-up (cap 200, 5 s) with a mandatory loud "reload and rejoin" failure branch.
- D6 2026-09-07 [netcode] Named-payload commands validate in a pure `applyCommand` reducer; undo = deterministic commandLog replay (design 001).
- D5 2026-09-07 [netcode] Pure Card/GameState models: instanceId, absolute player ids, Mulberry32 PRNG (design 001).
- D4 2026-09-07 [netcode] Shared engine in shared/ served at /shared, no bundler, no new deps (design 001).
- D1–D3 2026-09-07 [stack] Node ES modules + native `node --test`; Express + Socket.IO + SQLite3; pure rules logic tested on plain objects, no jsdom. (observed)
